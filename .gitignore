build/
.DS_Store
*.o
*.d
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile

# Pre-built plugin binaries (stored locally only — too large for git)
Archive/*/builds/

# Screenshots (local review/bug-report only, not needed in repo)
docs/screenshots/

# IEM reference source (large, kept locally but not tracked)
docs/references/IEM-PluginSuite-source/

# Design prototypes (historical, keep v6 reference only)
docs/design-prototypes/

# Node.js (manual generation toolchain)
docs/node_modules/

# Generated manual drafts and preview
docs/preview-manual.html
docs/preview-readme.html
docs/preview-wiki.html

# npm package files (doc generation toolchain)
docs/package.json
docs/package-lock.json

# Design prototypes (scratch work)
docs/prototypes/

# Claude tool state
.superpowers/

# Release packaging staging + output (generated by scripts/package_macos_release.sh)
dist/
OpenSpatialDelay-v*-macOS-*.zip

# Hero-video intermediate master (re-encodable from source; too large for git)
docs/assets/hero-video-build/
