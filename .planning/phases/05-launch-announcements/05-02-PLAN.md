---
phase: 05-launch-announcements
plan: 02
type: execute
wave: 1
depends_on: [05-00]
files_modified:
  - .planning/phases/05-launch-announcements/drafts/sml-newsletter.md
autonomous: false
requirements:
  - ANNC-02
must_haves:
  truths:
    - "A Mailchimp-ready newsletter draft exists (subject + preview + body + CTAs)"
    - "Subject line is ≤ 60 characters per 2026 cold-email/newsletter norms"
    - "Newsletter has exactly 2 CTAs — read blog post + get OSD (download)"
    - "Newsletter uses D-08 SML audience (spatialmedialab.org newsletter), NOT the andrewrahman.com OSD Sender list"
    - "Andrew's Mailchimp audience-owner role is confirmed (precheck gate) before the campaign is scheduled"
    - "Andrew confirms audience role + schedules send for 2026-04-28 10:15 CET per launch-day-runbook step 4"
  artifacts:
    - path: ".planning/phases/05-launch-announcements/drafts/sml-newsletter.md"
      provides: "Mailchimp campaign content (subject, preview text, HTML-ready body)"
      min_lines: 30
      contains: "each echo has a 3D position"
  key_links:
    - from: "sml-newsletter.md"
      to: "SML blog post URL (placeholder {SML_BLOG_POST_URL})"
      via: "CTA button 1 — Read the full post"
      pattern: "SML_BLOG_POST_URL|spatialmedialab\\.org"
    - from: "sml-newsletter.md"
      to: "andrewrahman.com/get-osd"
      via: "CTA button 2 — Download OpenSpatialDelay"
      pattern: "andrewrahman\\.com/get-osd"
---

<objective>
Draft the SML Mailchimp newsletter (ANNC-02). Short teaser format — subject + preview + 200–400
word body with 2 CTAs (Read full post / Get OSD). Per D-08, uses the separate spatialmedialab.org
newsletter (NOT the andrewrahman.com Sender list which is the OSD-confirmed subscriber tier).

Purpose: Amplify the launch to the established SML audience on launch morning. Kept deliberately
short — the blog post is the depth surface; the newsletter is the teaser that drives clicks to
the blog (which in turn drives downloads).

Output: `.planning/phases/05-launch-announcements/drafts/sml-newsletter.md` — a file Andrew
copies into Mailchimp's campaign editor at T-0 prep time.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-launch-announcements/05-CONTEXT.md
@.planning/phases/05-launch-announcements/05-RESEARCH.md
@.planning/phases/05-launch-announcements/drafts/sml-blog-post.md
@CLAUDE.md

<interfaces>
<!-- Mailchimp campaign template structure -->
Subject line: ≤ 60 chars (2026 newsletter norm)
Preview text: ≤ 90 chars (shown after subject in inbox list)
From: Andrew Rahman <andrew@spatialmedialab.org>  (OR SML default sender if Mailchimp requires)
Reply-to: andrew@spatialmedialab.org
Audience: SML main audience (confirmed in 05-00 precheck, D-08a)

<!-- Canonical links (blog URL placeholder because post isn't live until T-0) -->
{SML_BLOG_POST_URL} — filled at T-0 before Send; planner writes with the placeholder
https://andrewrahman.com/get-osd — Get OSD landing page
https://github.com/Spatial-Media-Lab/OpenSpatialDelay/releases/tag/v1.0.0 — direct download

<!-- Voice: SML-plural framing -->
OSD is "the first tool in the Spatial Media Library pipeline"
Capability-level language ONLY (per CLAUDE.md + feedback_marketing_copy_depth.md)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Draft sml-newsletter.md — teaser + 2 CTAs</name>
  <files>.planning/phases/05-launch-announcements/drafts/sml-newsletter.md</files>
  <read_first>
    - .planning/phases/05-launch-announcements/05-RESEARCH.md §Don't Hand-Roll (Mailchimp is the send-path)
    - .planning/phases/05-launch-announcements/05-CONTEXT.md §D-08 (separate SML newsletter, not Sender)
    - .planning/phases/02-email-capture-funding-infrastructure/drafts/patreon-post-04-project-links.md (cross-link pattern)
    - CLAUDE.md (voice)
  </read_first>
  <behavior>
    - Subject line length (the first line starting with `Subject:`): ≤ 60 characters
    - Preview text length (line starting with `Preview:`): ≤ 90 characters
    - Body word count: 200–400 words inclusive
    - Exactly 2 CTA blocks in body: one to blog post, one to /get-osd
    - First body sentence contains "each echo has a 3D position"
    - Contains the placeholder `{SML_BLOG_POST_URL}` (so T-0 fill-in is mechanical)
    - Contains `andrewrahman.com/get-osd`
    - Contains first-person voice opener (`I `, `I've`, `I just`, or similar)
    - No DSP jargon: no occurrences of FFT / phase vocoder / partition / paper names
    - No "buy" or "purchase"
  </behavior>
  <action>
Create `.planning/phases/05-launch-announcements/drafts/sml-newsletter.md`. Exact structure:

```markdown
# SML Newsletter — OpenSpatialDelay Launch
# Audience: SML main list (spatialmedialab.org)
# Send: Tuesday 2026-04-28 10:15 CET (launch-day-runbook step 4)
# From: Andrew Rahman <andrew@spatialmedialab.org>
# Reply-to: andrew@spatialmedialab.org

Subject: Free spatial delay — OpenSpatialDelay v1.0.0 is out

Preview: A VST3/AU plugin where each echo has a 3D position. Free, GPL-3.0.

---

## Body

Hi —

I just released **OpenSpatialDelay v1.0.0**, the first tool in the Spatial Media Library
pipeline. Each echo has a 3D position in space. Free, GPL-3.0, VST3 + AU, macOS + Windows.

[1 short paragraph — Berlin-solo-dev first-person, SML-pipeline framing. ~60–90 words. Mirror
tone of Patreon Post 4 (public, cross-link-focused). Keep capability-level only.]

[1 short paragraph — "what's inside" — hit the numbers: 12 delay taps in 3D space, 7
spatialization algorithms, 5 measured HRTF profiles plus a CPU-lite mode, 70 factory presets,
ADM-OSC. No DSP jargon. ~60–80 words.]

## [Read the full post →]({SML_BLOG_POST_URL})

## [Download OpenSpatialDelay →](https://andrewrahman.com/get-osd)

If you want to support the work, Patreon keeps the tools free:
[patreon.com/AndrewRahman](https://patreon.com/AndrewRahman)

— Andrew
Spatial Media Lab • Berlin
```

Fill in the two bracketed paragraphs with actual prose (~60–90 words each), keeping total body
word count in the 200–400 range. First-person. No jargon. One instance of the exact phrase
"each echo has a 3D position".

Mailchimp will render the Markdown-style button-links as HTML buttons in the visual editor —
Andrew can reformat as buttons at paste-time. The plain-link style is also valid (higher
inbox-deliverability score per 2026 newsletter research).
  </action>
  <verify>
    <automated>test -f .planning/phases/05-launch-announcements/drafts/sml-newsletter.md && bash -c 'WC=$(sed -n "/^## Body$/,/^— Andrew$/p" .planning/phases/05-launch-announcements/drafts/sml-newsletter.md | wc -w); [ "$WC" -ge 200 ] && [ "$WC" -le 400 ] && echo "body wc=$WC ok" || { echo "body word count $WC outside 200-400"; exit 1; }' && grep -q "each echo has a 3D position" .planning/phases/05-launch-announcements/drafts/sml-newsletter.md && grep -q "{SML_BLOG_POST_URL}" .planning/phases/05-launch-announcements/drafts/sml-newsletter.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/05-launch-announcements/drafts/sml-newsletter.md` exists
    - Subject line is `Subject:` + content totalling ≤ 60 chars after the word "Subject: " (verify: `grep '^Subject:' file | awk '{print length($0)-9}'` ≤ 60)
    - Preview line is `Preview:` + content ≤ 90 chars (verify similarly)
    - Body word count (between `## Body` and `— Andrew`) is 200–400
    - Contains the literal placeholder `{SML_BLOG_POST_URL}`
    - Contains the literal string `andrewrahman.com/get-osd`
    - Contains the literal string `patreon.com/AndrewRahman`
    - Contains the literal phrase `each echo has a 3D position`
    - Contains first-person marker `I ` or `I've` or `I just`
    - `grep -ciE '(FFT|phase vocoder|partition|Laroche|Dolson|Röbel|Gardner|Martin|2048-sample|STFT|AAX|VST2|buy|purchase)' file` returns 0
  </acceptance_criteria>
  <done>Newsletter draft committed, Mailchimp-paste-ready, all acceptance greps pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint — Mailchimp access + schedule send for launch day</name>
  <files>(human-verification checkpoint — no file writes; see what-built block for artefacts under review)</files>
  <action>Pause execution. Human follows the steps in <how-to-verify> below. Resume only when the <resume-signal> condition is met.</action>
  <what-built>
    - .planning/phases/05-launch-announcements/drafts/sml-newsletter.md (Mailchimp-ready)
  </what-built>
  <how-to-verify>
    1. CRITICAL PREREQUISITE (D-08a): Andrew confirms in 05-00 precheck Section 1 that his SML
       Mailchimp role is Manager or Admin. If not, this checkpoint BLOCKS until the owner delegates
       send permission or agrees to hit Send at 10:15 CET on 2026-04-28.
    2. Andrew opens Mailchimp → create campaign → paste subject, preview, and body from the draft.
    3. Replace `{SML_BLOG_POST_URL}` with the live blog URL AFTER the blog post is published at T-0
       step 1 (10:00 CET). Mailchimp send fires at 10:15 CET per runbook step 4.
    4. OPTION A (recommended if Andrew has Admin): schedule the campaign for 2026-04-28 10:15 CET
       with the placeholder still in place; edit campaign before send to swap the placeholder with
       the live URL.
    5. OPTION B (delegated): email the draft + final-URL instructions to the audience owner with
       a "please send at 10:15 CET 2026-04-28" ask. Confirm receipt before T-1.
    6. Verify test send: Mailchimp → Send Test Email → to andrew@spatialmedialab.org + a personal
       inbox. Confirm: subject preview looks right; both buttons clickable; no broken {placeholder}
       remaining after URL swap; Mobile render OK.
  </how-to-verify>
  <resume-signal>Type "newsletter scheduled" once Mailchimp campaign is either scheduled (Option A) or delegated with confirmation (Option B) AND a test send has been reviewed. Include the chosen option in the reply.</resume-signal>
  <verify>
    <automated>echo "checkpoint: awaiting human resume-signal"</automated>
  </verify>
  <done>Human types the resume-signal phrase, confirming the checkpoint conditions are satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mailchimp campaign → subscriber inboxes | Mailchimp handles send-reputation; Andrew's audience-role gates the send |
| Draft Markdown → Mailchimp HTML editor | Manual paste; visual render may differ from Markdown — test send mitigates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Spoofing | Mailchimp "From" address must match verified-domain; spatialmedialab.org DKIM may be incomplete | mitigate | Precheck Section 2 verifies SPF+DKIM; Mailchimp also auto-handles DKIM/SPF via its own domain. Test send confirms delivery. |
| T-05-02-02 | Information disclosure | Test send to Andrew's inbox + personal inbox leaks draft-stage URL | accept | URLs used are already public (blog, /get-osd, patreon); no sensitive data in newsletter. |
| T-05-02-03 | Denial of service | Mailchimp audience-owner unreachable at 10:15 CET → missed window | mitigate | Precheck Section 1 forces Admin confirmation by T-6 (2026-04-22); Option B delegation is the fallback. |
</threat_model>

<verification>
- Draft file passes all acceptance greps.
- Mailchimp campaign either scheduled-in-advance OR delegated-with-confirmation by T-1.
- Test send preview confirmed in Andrew's inbox.
</verification>

<success_criteria>
- `.planning/phases/05-launch-announcements/drafts/sml-newsletter.md` committed and passes automated verification.
- Andrew types "newsletter scheduled" with chosen option (A schedule / B delegate) in Task 2 resume signal.
- After launch: Mailchimp dashboard campaign status = "Sent" at 10:15 CET 2026-04-28 (±5 min).
</success_criteria>

<output>
After completion, create `.planning/phases/05-launch-announcements/05-02-SUMMARY.md` recording:
chosen send-path option (A or B), Mailchimp campaign name, test-send timestamp, audience subscriber
count (from Mailchimp dashboard), and any delegated-sender name/email.
</output>
