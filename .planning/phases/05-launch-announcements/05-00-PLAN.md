---
phase: 05-launch-announcements
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/phase5-smoke.sh
  - .planning/phases/05-launch-announcements/launch-precheck.md
  - .planning/phases/05-launch-announcements/launch-day-runbook.md
autonomous: false
requirements:
  - ANNC-01
  - ANNC-02
  - ANNC-03
  - ANNC-04
  - ANNC-05
  - ANNC-06
must_haves:
  truths:
    - "A precondition checklist exists and has been walked by Andrew before T-7"
    - "SML Mailchimp audience owner + send-path is confirmed (D-08a closed)"
    - "SPF/DKIM on spatialmedialab.org is verified OR the 34-contact review batch falls back to andrewjrahman@gmail.com"
    - "KVR Developer Account status is known (exists OR application submitted)"
    - "Phase 4 demo-content status is surfaced as a blocker by 2026-04-20 if unplanned"
    - "A single smoke script `scripts/phase5-smoke.sh` runs curl -I against every launch-day URL"
    - "A launch-day runbook documents the exact T-0 ordering within the 2-hour window (D-01)"
  artifacts:
    - path: "scripts/phase5-smoke.sh"
      provides: "One-command smoke over all launch-day URLs"
      min_lines: 15
    - path: ".planning/phases/05-launch-announcements/launch-precheck.md"
      provides: "T-7 through T-1 precondition checklist with verified/unverified status"
    - path: ".planning/phases/05-launch-announcements/launch-day-runbook.md"
      provides: "T-0 runbook: 8 sequenced steps within a 2-hour window per D-01"
  key_links:
    - from: "scripts/phase5-smoke.sh"
      to: "https://spatialmedialab.org, https://andrewrahman.com/get-osd, https://github.com/Spatial-Media-Lab/OpenSpatialDelay/releases/tag/v1.0.0, https://patreon.com/AndrewRahman"
      via: "curl -fsI"
      pattern: "curl.*-fsI.*https?://"
---

<objective>
Wave-0 launch orchestration. Produce the three coordination artefacts every downstream plan depends on:
(1) `scripts/phase5-smoke.sh` — the one-command URL smoke for T-1 and T-0; (2) `launch-precheck.md` — a
T-7 through T-1 checklist that closes all [UNKNOWN] rows from 05-RESEARCH.md Environment Availability
(SML Mailchimp owner, KVR Developer Account, SPF/DKIM on spatialmedialab.org, Phase 4 asset status);
(3) `launch-day-runbook.md` — the 8-step T-0 ordering Andrew follows on 2026-04-28.

Purpose: Plans 05-01..05-08 all assume these coordination preconditions are true. Wave 0 is the only
place to surface the Phase 4 blocker and the Mailchimp access blocker early enough to salvage the
April 28 launch date (or trigger the May 5 fallback) before content work has sunk effort.

Output: 3 files committed to the repo + an in-session walk of the precheck with Andrew so
[UNKNOWN] rows are closed or escalated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-launch-announcements/05-CONTEXT.md
@.planning/phases/05-launch-announcements/05-RESEARCH.md
@.planning/phases/05-launch-announcements/05-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- URL targets the smoke script must hit. Sourced from RESEARCH Environment Availability. -->

Launch-day canonical URLs:
- https://spatialmedialab.org/ (news index — post slug filled at launch: /openspatialdelay/ or /openspatialdelay-v1-0-0/)
- https://andrewrahman.com/get-osd
- https://andrewrahman.com/privacy
- https://github.com/Spatial-Media-Lab/OpenSpatialDelay/releases/tag/v1.0.0
- https://patreon.com/AndrewRahman
- https://kvraudio.com/product/openspatialdelay (URL known only post-listing-approval; smoke-script default-skips with a clear message if the env var KVR_URL is unset)

Known Mailchimp fact (from 05-RESEARCH §Standard Stack):
- SML Mailchimp audience exists (subscribe form verified active on spatialmedialab.org homepage)
- Owner-role: UNKNOWN as of 2026-04-17 — must be resolved this plan

Known SML WordPress fact:
- Andrew authored spatialmedialab.org/ambisonic-to-atmos-template/ (May 2024)
- Therefore Andrew has at least Author role — assume Editor; verify login still valid

Berlin-local contacts (handled in 05-07 but gating on D-07b timing of 2-weeks):
- Hainbach, Peter Kirn, Eric Horstmann, Ulli Scuda (AES Germany chair, re-scoped from "AES Berlin chair")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/phase5-smoke.sh — URL curl loop</name>
  <files>scripts/phase5-smoke.sh</files>
  <read_first>
    - scripts/build_version.sh (existing script for style reference — she-bang, set -eu, echo conventions)
    - scripts/package_macos_release.sh (existing script for error-handling conventions)
    - .planning/phases/05-launch-announcements/05-RESEARCH.md §Environment Availability (URL list)
    - .planning/phases/05-launch-announcements/05-VALIDATION.md §Wave 0 Requirements
  </read_first>
  <action>
Create `scripts/phase5-smoke.sh` as a standalone bash script. Requirements:

- She-bang: `#!/usr/bin/env bash` ; `set -euo pipefail`
- Usage printout if `--help` or `-h` passed
- Defines an array of 5 hard URLs: `https://spatialmedialab.org/`, `https://andrewrahman.com/get-osd`, `https://andrewrahman.com/privacy`, `https://github.com/Spatial-Media-Lab/OpenSpatialDelay/releases/tag/v1.0.0`, `https://patreon.com/AndrewRahman`
- Also probes env vars `SML_BLOG_POST_URL` and `KVR_URL` if set — if set, append to the URL list; if unset, print a yellow "skipped: KVR_URL/SML_BLOG_POST_URL not set — fill in T-1" line
- For each URL: run `curl -fsIL --max-time 10 "$url"` (follow redirects, fail on non-2xx, silent unless error). On success echo green "PASS  $url  (HTTP code)". On failure echo red "FAIL  $url  (reason)" and exit-code track.
- After all URLs checked, print summary "X of N URLs passed". Exit 0 only if all hard URLs pass; exit 1 if any hard URL fails (env-var URLs can fail without blocking exit 0 — they just print warn).
- No external dependencies beyond `curl` and `bash`.

Target length: 25–45 lines (no framework). Commit the script + chmod +x.
  </action>
  <verify>
    <automated>bash scripts/phase5-smoke.sh; EXIT=$?; if [ $EXIT -eq 0 ] || [ $EXIT -eq 1 ]; then echo "ran OK with exit $EXIT"; else echo "unexpected exit $EXIT" && exit 1; fi</automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/phase5-smoke.sh` exists and is executable: `test -x scripts/phase5-smoke.sh`
    - File contains the literal strings: `set -euo pipefail`, `spatialmedialab.org`, `andrewrahman.com/get-osd`, `github.com/Spatial-Media-Lab/OpenSpatialDelay/releases/tag/v1.0.0`, `patreon.com/AndrewRahman`, `curl -fsIL`
    - File contains env-var guard `KVR_URL` (literal)
    - `grep -c '^#' scripts/phase5-smoke.sh` returns ≥ 3 (script is documented)
    - Running `bash scripts/phase5-smoke.sh` produces per-URL PASS/FAIL lines (verified by grepping output for "PASS" or "FAIL")
    - Script total line count between 15 and 60 (verify with `wc -l`)
  </acceptance_criteria>
  <done>Smoke script committed, executable, runs without syntax error, prints per-URL status, exit code correct.</done>
</task>

<task type="auto">
  <name>Task 2: Write launch-precheck.md — T-7 through T-1 precondition checklist</name>
  <files>.planning/phases/05-launch-announcements/launch-precheck.md</files>
  <read_first>
    - .planning/phases/05-launch-announcements/05-RESEARCH.md §Environment Availability
    - .planning/phases/05-launch-announcements/05-RESEARCH.md §Common Pitfalls (Pitfall 1 Phase 4 + Pitfall 3 Mailchimp + Pitfall 5 SPF/DKIM)
    - .planning/phases/05-launch-announcements/05-CONTEXT.md §Decisions (D-08a Mailchimp blocker, D-03 launch date, D-04 travel)
    - .planning/ROADMAP.md §Phase 4 (verify still has no plans as of this write)
  </read_first>
  <action>
Create `.planning/phases/05-launch-announcements/launch-precheck.md` as a Markdown checklist. Include these EXACT sections and checkbox items (copy verbatim):

Title: `# Phase 5 Launch Precheck — T-7 through T-1`

Subtitle: `**Target launch:** Tuesday 2026-04-28, ~10:00 CET. Fallback: Tuesday 2026-05-05 (overlaps Andrew's travel — avoid if possible).`

Section 1: `## Hard blockers (any one unresolved = delay launch to 2026-05-05)`
- [ ] Phase 4 (Demo Content) has a 04-00-PLAN.md OR an explicit descope decision by 2026-04-20. Current status as of 2026-04-17: NO PLANS. Escalate at 2026-04-20 standup if unchanged.
- [ ] SML Mailchimp audience owner identified — Andrew (self) OR Timo Bittner (president) OR Basel Naouri (board). Role confirmed = Manager or Admin on the audience.
- [ ] andrewrahman.com is live on Netlify production domain (Phase 3-09 gate).
- [ ] GitHub release v1.0.0 downloadable — both macOS and Windows binaries attached.

Section 2: `## Email-deliverability preconditions (gates the 34-contact review batch in 05-06)`
- [ ] SPF record exists on spatialmedialab.org (check: `dig TXT spatialmedialab.org | grep spf1`)
- [ ] DKIM record exists on spatialmedialab.org (check: `dig TXT default._domainkey.spatialmedialab.org` or inspect a test send's Authentication-Results header)
- [ ] Decision: if SPF+DKIM not verified by 2026-04-25 (T-3), review-pitch batch sends from andrewjrahman@gmail.com instead of andrew@spatialmedialab.org. Document the chosen from-address in 05-06 outreach log.

Section 3: `## External-account preconditions`
- [ ] KVR Developer Account: login-check at https://www.kvraudio.com/ — if no existing account, apply at https://www.kvraudio.com/developer_application.php (free; < 1-day turnaround). Fallback: email press release to contactus@kvraudio.com.
- [ ] LinkedIn: Andrew logged in + bio-link field accessible (pre-launch bio will be updated to andrewrahman.com/get-osd on 2026-04-27 T-1).
- [ ] Instagram: Andrew's personal IG login verified. SML IG handle `@spatialmedialab` is a cross-post option (confirmed in RESEARCH sources).
- [ ] Bluesky: Andrew's handle exists (verify or create — target send to Hainbach and Peter Kirn requires Bluesky DM).
- [ ] Mastodon fallback for Kirn (mastodon.social/@pkirn) if Bluesky fails.

Section 4: `## URL smoke (run bash scripts/phase5-smoke.sh at T-1 and T-0)`
- [ ] T-1 morning (2026-04-27): `bash scripts/phase5-smoke.sh` returns 0, all 5 hard URLs pass.
- [ ] T-0 morning (2026-04-28, before 10:00 CET): re-run; all pass.
- [ ] Post-publish (2026-04-28 after step 1): `SML_BLOG_POST_URL=https://spatialmedialab.org/{slug}/ bash scripts/phase5-smoke.sh` passes.
- [ ] Post-listing (any day Apr 28-May 3): `KVR_URL=https://www.kvraudio.com/product/{slug} bash scripts/phase5-smoke.sh` passes.

Section 5: `## Berlin-local D-07 DM timing window`
- [ ] Berlin DMs to Hainbach, Peter Kirn, Eric Horstmann, Ulli Scuda sent in the 2026-04-21 through 2026-04-25 window (per RESEARCH Pitfall 6).
- [ ] See 05-07-PLAN.md for per-contact drafts.

Section 6: `## Precheck owner + cadence`
- Owner: Andrew Rahman.
- Review cadence: daily walk through this file 2026-04-21 (T-7) onward.
- Log: append a daily dated line under each item ("2026-04-22: Mailchimp role = Manager confirmed") so the audit trail is in-file.

Word count target: 400–700 words. Include the exact URLs / command strings above; do not paraphrase.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-launch-announcements/launch-precheck.md && grep -c '^- \[ \]' .planning/phases/05-launch-announcements/launch-precheck.md | awk '$1 >= 15 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/05-launch-announcements/launch-precheck.md` exists
    - File contains literal strings: `T-7`, `T-1`, `T-0`, `2026-04-28`, `2026-05-05`, `Phase 4 (Demo Content)`, `SML Mailchimp`, `SPF record`, `DKIM record`, `KVR Developer Account`, `scripts/phase5-smoke.sh`, `Hainbach`, `Peter Kirn`, `Eric Horstmann`, `Ulli Scuda`, `andrewjrahman@gmail.com`, `andrew@spatialmedialab.org`
    - Checkbox count: `grep -c '^- \[ \]' .planning/phases/05-launch-announcements/launch-precheck.md` returns ≥ 15
    - File word count between 400 and 900 (verify with `wc -w`)
    - File contains the exact command `dig TXT spatialmedialab.org | grep spf1`
  </acceptance_criteria>
  <done>Precheck file committed; Andrew can walk it daily from 2026-04-21; every [UNKNOWN] row from RESEARCH Environment Availability has a checkbox.</done>
</task>

<task type="auto">
  <name>Task 3: Write launch-day-runbook.md — T-0 8-step sequence within 2-hour window</name>
  <files>.planning/phases/05-launch-announcements/launch-day-runbook.md</files>
  <read_first>
    - .planning/phases/05-launch-announcements/05-RESEARCH.md §Architecture Patterns §Launch-Day Sequencing Diagram (8 steps)
    - .planning/phases/05-launch-announcements/05-CONTEXT.md §D-01 (2-hour window) §D-04 (Andrew travel)
    - .planning/phases/05-launch-announcements/05-VALIDATION.md §Per-Task Verification Map
    - memory/feedback_evidence_artifact_ceremony.md (user feedback: URL-live + user-confirm is enough, not per-step screenshot)
  </read_first>
  <action>
Create `.planning/phases/05-launch-announcements/launch-day-runbook.md` as a Markdown runbook. Structure:

Title: `# Launch Day Runbook — Tuesday 2026-04-28`

Subtitle: `**Window:** 10:00–12:00 CET (2 hours, per D-01). **Owner:** Andrew Rahman. **Fallback date:** 2026-05-05.`

Section: `## Pre-window (09:30–09:55 CET)`
- [ ] `bash scripts/phase5-smoke.sh` — all 5 hard URLs PASS
- [ ] Open these browser tabs in order: WordPress admin (spatialmedialab.org/wp-admin), Mailchimp (mailchimp.com/campaigns), KVR Developer dashboard, LinkedIn post composer, Instagram web/Business Suite, Gmail draft-folder with 34 review-pitch drafts
- [ ] Confirm coffee. No coffee, no launch.

Section: `## T-0 sequence (10:00–12:00 CET, per RESEARCH Launch-Day Sequencing Diagram)`

Copy the EXACT 8-step ordering from 05-RESEARCH.md §Launch-Day Sequencing Diagram:

1. `10:00` — **Publish SML blog post** (WordPress → Publish). Grab canonical URL. Update bio-link fields (LinkedIn, Instagram) to `andrewrahman.com/get-osd`.
2. `10:05` — **Submit KVR product listing** via KVR Developer dashboard (or email to contactus@kvraudio.com per fallback). Editorial review is hours-to-days; OK.
3. `10:10` — **Submit KVR news item** (press-release style) via the same dashboard / email.
4. `10:15` — **Send SML Mailchimp newsletter** — campaign already prepped in 05-02; just click Send or Schedule-now.
5. `10:20` — **Publish LinkedIn post** (text-only or document-carousel, per 05-03). Do NOT add link in first comment (2026 algorithm penalty).
6. `10:25` — **Publish Instagram Reel** (or Reel + carousel companion per 05-04). Caption hook first-line ≤ 140 chars before "more".
7. `10:35` — **Cross-post to forums**: Gearspace VR/Immersive, KVR Effects Forum, r/spatialaudio, VI-CONTROL (per 05-RESEARCH §Segment 6).
8. `10:45–12:00` — **Send 34-contact review-pitch batch** (05-06). Space over 2 hours (1 send every 3–4 min) per RESEARCH Pitfall 5 to dodge Gmail/Outlook spam filters.

Section: `## Post-window (12:00+ CET)`
- [ ] Re-run smoke with blog + KVR URLs: `SML_BLOG_POST_URL=<url> KVR_URL=<url> bash scripts/phase5-smoke.sh`
- [ ] Patreon cross-post (seed post 5, "v1.0.0 is OUT") — OPTIONAL, non-counted channel.
- [ ] Andrew updates STATE.md resume pointer to `Phase 5 launched; monitoring 7-day window`.

Section: `## If anything breaks mid-window`
- [ ] If WordPress Publish fails → submit-for-review route (Timo/Basel approves). Absorbs 0–24h slip. Continue with remaining steps; newsletter CTA can link directly to GitHub Release + andrewrahman.com/get-osd for 24h.
- [ ] If Mailchimp Send fails (role issue) → delegate send to audience owner. If owner unreachable, send via Andrew's gmail ListMonk (degraded plaintext) to top 20 personal-network recipients. Accept loss of the full SML audience for this launch.
- [ ] If LinkedIn algorithm throttle suspected post-publish (<200 impressions 2h in) → edit post to remove any URL and append "Link in bio" (per RESEARCH Pitfall 2).
- [ ] If KVR listing rejected → read rejection reason; fix missing field; resubmit. Product listing catches traffic whenever it goes live; news item is the optional launch-moment.

Section: `## Evidence requirements (per feedback_evidence_artifact_ceremony.md)`
- Live URL + Andrew self-confirmation is sufficient per-channel. No per-step screenshots required unless validation surface (a11y / hashtag cap / first-comment check) needs one.

Word-count target: 500–900 words. Include the literal times (10:00, 10:05, ...) and steps verbatim.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-launch-announcements/launch-day-runbook.md && grep -cE '10:(00|05|10|15|20|25|35|45)' .planning/phases/05-launch-announcements/launch-day-runbook.md | awk '$1 >= 8 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/05-launch-announcements/launch-day-runbook.md` exists
    - File contains the 8 time stamps: `10:00`, `10:05`, `10:10`, `10:15`, `10:20`, `10:25`, `10:35`, `10:45` (grep each)
    - File contains literal strings: `2026-04-28`, `2026-05-05`, `Gearspace VR/Immersive`, `KVR Effects Forum`, `r/spatialaudio`, `VI-CONTROL`, `do NOT add link in first comment`, `contactus@kvraudio.com`, `andrewrahman.com/get-osd`, `Mailchimp`, `WordPress`
    - File contains the 34-contact launch-morning batch ordering phrase `34-contact review-pitch batch` or `34-contact batch`
    - File word count between 500 and 1200 (`wc -w`)
  </acceptance_criteria>
  <done>Runbook file committed; Andrew has an 8-step sequenced Tue 2026-04-28 script he can execute from 09:30 start through 12:00 end plus contingency.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Checkpoint — walk launch-precheck.md with Andrew, close [UNKNOWN] rows</name>
  <files>(human-verification checkpoint — no file writes; see what-built block for artefacts under review)</files>
  <action>Pause execution. Human follows the steps in <how-to-verify> below. Resume only when the <resume-signal> condition is met.</action>
  <what-built>
    - scripts/phase5-smoke.sh (URL smoke script, executable)
    - .planning/phases/05-launch-announcements/launch-precheck.md (15+ checkboxes across 6 sections)
    - .planning/phases/05-launch-announcements/launch-day-runbook.md (8-step T-0 sequence + contingencies)
  </what-built>
  <how-to-verify>
    1. Andrew runs `bash scripts/phase5-smoke.sh` and confirms all 5 hard URLs PASS.
    2. Andrew opens `.planning/phases/05-launch-announcements/launch-precheck.md` and walks Sections 1–3:
       - Hard blockers: Is Phase 4 scheduled? Who owns SML Mailchimp? Is andrewrahman.com production-deployed?
       - Email deliverability: Does `dig TXT spatialmedialab.org | grep spf1` return a record? If no, which from-address for the review batch?
       - External accounts: Does Andrew have a KVR Developer Account?
    3. For each [UNKNOWN] → confirmed, Andrew appends a dated note under the checkbox ("2026-04-17: Mailchimp role = Manager confirmed after login test").
    4. If ANY hard blocker is red (Phase 4 unplanned by 2026-04-20, Mailchimp access impossible, andrewrahman.com not live) — Andrew decides: slip to 2026-05-05 OR descope a channel (likely IG Reel → static IG post).
    5. Confirm the launch-day-runbook.md 8-step ordering reads right for Andrew's Tuesday 28 morning.
  </how-to-verify>
  <resume-signal>Type "precheck walked" once every blocker status is either ✅ confirmed or has a documented mitigation. Include a one-line summary of any escalations ("Phase 4 still unplanned, escalating today").</resume-signal>
  <verify>
    <automated>echo "checkpoint: awaiting human resume-signal"</automated>
  </verify>
  <done>Human types the resume-signal phrase, confirming the checkpoint conditions are satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Launch-day URLs → public internet | Public DNS + CDN; no secrets cross this boundary |
| Mailchimp send → subscriber inboxes | Mailchimp handles auth; Andrew's dashboard access is the only sensitive surface |
| spatialmedialab.org → EU inboxes (review batch) | Cold-email sending; requires SPF/DKIM to survive spam filters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-00-01 | Spoofing | Cold-email from `andrew@spatialmedialab.org` without SPF/DKIM | mitigate | Precheck Section 2: verify SPF + DKIM by 2026-04-25; fallback to `andrewjrahman@gmail.com` if unverified. |
| T-05-00-02 | Information disclosure | `scripts/phase5-smoke.sh` accepts `KVR_URL` env var | accept | URL is public metadata; no secret leaks via env var. |
| T-05-00-03 | Denial of service | 34-send batch in short window trips spam-filter rate-limits → all sends burn | mitigate | Runbook Step 8: space 34 sends over 60–75 min (1 send per 3–4 min) per RESEARCH Pitfall 5. |
| T-05-00-04 | Repudiation | Mailchimp send delegated to board member Andrew doesn't own — audit trail unclear | accept | Solo-creator context; delegation email + Mailchimp activity log suffices. |
</threat_model>

<verification>
- `bash scripts/phase5-smoke.sh` exits 0 (all 5 hard URLs pass) — validates infra precondition.
- `grep -c '^- \[ \]' launch-precheck.md >= 15` — ensures checklist density.
- `grep -c '^10:' launch-day-runbook.md >= 8` — ensures 8 steps in runbook.
- Andrew walks precheck and types "precheck walked" with escalation summary — validates human precondition closure.
</verification>

<success_criteria>
- Smoke script runs green against all 5 hard URLs.
- Every [UNKNOWN] row from 05-RESEARCH §Environment Availability has either a "confirmed" annotation or a documented fallback in `launch-precheck.md`.
- Phase 4 status is explicitly logged (confirmed or escalated) by 2026-04-20.
- Andrew has a T-0 runbook with 8 sequenced steps within a 2-hour window and written contingencies for the 4 most likely failure modes.
</success_criteria>

<output>
After completion, create `.planning/phases/05-launch-announcements/05-00-SUMMARY.md` per template, recording: SPF/DKIM status outcome, Mailchimp owner resolution, KVR Developer Account status, Phase 4 blocker decision (proceed / slip / descope).
</output>
