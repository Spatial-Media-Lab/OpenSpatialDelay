---
phase: 05-launch-announcements
plan: 03
type: execute
wave: 1
depends_on: [05-00]
files_modified:
  - .planning/phases/05-launch-announcements/drafts/linkedin-post.md
autonomous: false
requirements:
  - ANNC-03
must_haves:
  truths:
    - "A LinkedIn post draft exists in the 2026-legal format: text-only with bio-link prompt OR native document carousel"
    - "The draft does NOT use 'link in first comment' (2026 algorithm penalty per RESEARCH)"
    - "Post body is between 1,301 and 2,500 characters (LinkedIn sweet spot per RESEARCH)"
    - "Hashtag count is ≤ 5 and uses the canonical set"
    - "First 210 characters (pre-'see more' cutoff on desktop) contain a compelling hook"
    - "Andrew updates LinkedIn bio-link to andrewrahman.com/get-osd before launch morning"
  artifacts:
    - path: ".planning/phases/05-launch-announcements/drafts/linkedin-post.md"
      provides: "LinkedIn-ready text (+ optional document carousel source spec)"
      min_lines: 40
      contains: "each echo has a 3D position"
  key_links:
    - from: "linkedin-post.md"
      to: "Andrew's LinkedIn profile bio-link field"
      via: "pre-launch bio update to andrewrahman.com/get-osd"
      pattern: "bio.*andrewrahman\\.com/get-osd|andrewrahman\\.com/get-osd.*bio"
---

<objective>
Draft the LinkedIn launch post (ANNC-03). CRITICAL PIVOT from CONTEXT.md: per 05-RESEARCH.md
finding #2, "link in first comment" is dead in 2026 — LinkedIn's algorithm now buries author-
authored first-comment links specifically. Draft follows the text-only-storytelling format
with a bio-link prompt, plus an optional native document-carousel variant for ~3× engagement.

Purpose: Drive the Berlin-indie-dev narrative per CONTEXT.md Claude's Discretion note. Reach
Andrew's professional network + audio-dev/immersive-audio community without the 60% reach
penalty LinkedIn applies to external links in post body or first comment.

Output: `.planning/phases/05-launch-announcements/drafts/linkedin-post.md` — contains both
a text-only-post format AND a document-carousel spec, so Andrew can pick at T-0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-launch-announcements/05-CONTEXT.md
@.planning/phases/05-launch-announcements/05-RESEARCH.md
@.planning/phases/02-email-capture-funding-infrastructure/drafts/patreon-page-about.md
@CLAUDE.md

<interfaces>
<!-- LinkedIn 2026 format rules (from 05-RESEARCH §Pattern 2 + §Anti-Patterns) -->
- Character sweet-spot: 1,301–2,500 (dataslayer.ai Feb 2026)
- First 210 chars visible before "see more" on desktop, 140 chars on mobile — hook must land here
- Hashtag count: 3–5 is ideal (not a hard cap, but more = spammy signal)
- External links in post body → ~60% reach reduction
- External link in author's first comment → also penalised (specifically buried by algorithm)
- Native LinkedIn document/PDF carousel → ~3× engagement vs text-only
- Bio-link CTA is the acceptable 2026 external-link route

<!-- Canonical hashtags -->
#SpatialAudio #DolbyAtmos #AudioPlugin #IndieDev #OpenSource

<!-- Bio-link target (to update LinkedIn profile BEFORE launch morning) -->
https://andrewrahman.com/get-osd

<!-- Voice: Berlin-indie-dev narrative (per CONTEXT §Claude's Discretion) -->
Per-channel positioning: LinkedIn leans on the Berlin-solo-dev story
Capability sentence lock: "Each echo has a 3D position in space."

<!-- Document carousel spec (if chosen over text-only) -->
Format: 5–7 page PDF, 1080×1080 square or 1080×1350 portrait
Page 1: hook slide (capability sentence + OSD wordmark + "free, GPL-3.0")
Pages 2-5: one capability per slide (12 taps / 7 algorithms / 5 HRTFs + Simple / 70 presets / ADM-OSC)
Page 6: screenshot — screenshot_full.png
Page 7: download CTA — "Link in bio" + andrewrahman.com/get-osd text (not a clickable link; LinkedIn docs don't support hyperlinks)
Tool: Figma or Canva
Attach via LinkedIn "Add a document" in the post composer
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Draft linkedin-post.md — text-only + document-carousel spec</name>
  <files>.planning/phases/05-launch-announcements/drafts/linkedin-post.md</files>
  <read_first>
    - .planning/phases/05-launch-announcements/05-RESEARCH.md §Pattern 2 (LinkedIn 2026 text-only template)
    - .planning/phases/05-launch-announcements/05-RESEARCH.md §Anti-Patterns (no link-in-first-comment rule)
    - .planning/phases/05-launch-announcements/05-RESEARCH.md §Pitfall 2 (confirming the dead tactic)
    - .planning/phases/02-email-capture-funding-infrastructure/drafts/patreon-post-04-project-links.md (Berlin / solo-dev voice reference)
  </read_first>
  <behavior>
    - Exactly 2 sections: Format A (text-only) and Format B (document carousel)
    - Format A body character count: between 1,301 and 2,500 (LinkedIn sweet-spot)
    - Format A first 210 chars contains hook + capability sentence
    - Format A ends with "Link in bio" (NOT "link in first comment")
    - Format A hashtag line: exactly 5 hashtags from canonical set
    - Format A contains first-person Berlin narrative (`I`, `Berlin`, `built`)
    - Format A contains literal phrase `each echo has a 3D position`
    - Format B contains a 7-page carousel spec (page-by-page)
    - Format B caption (short companion body): 400–800 chars, same "Link in bio" CTA
    - Zero occurrences of "link in first comment" (literal check fails = no such phrase in file)
    - Zero occurrences of DSP jargon (FFT / phase vocoder / paper names / 2048-sample)
    - Pre-launch bio-update instruction at top of file: "BEFORE PUBLISHING: change LinkedIn profile Contact info → Website → andrewrahman.com/get-osd (T-1 morning, 2026-04-27)"
  </behavior>
  <action>
Create `.planning/phases/05-launch-announcements/drafts/linkedin-post.md`. Structure:

```markdown
# LinkedIn Launch Post — OpenSpatialDelay v1.0.0

## PRE-LAUNCH CHECKLIST (T-1 morning, 2026-04-27)

- [ ] Open LinkedIn profile → Edit intro → Contact info → Website field → set to https://andrewrahman.com/get-osd
- [ ] Confirm bio text mentions "Berlin" somewhere so "Link in bio" CTA pays off
- [ ] Decide: Format A (text-only) OR Format B (document carousel). Research says B gets ~3×
      engagement; A is zero-friction if CONT-03 screenshots/PDF export isn't ready.
- [ ] Post at 2026-04-28 10:20 CET per launch-day-runbook step 5
- [ ] DO NOT add any link in the first comment. DO NOT add any link in the post body.
      (2026 LinkedIn algorithm penalises both — see 05-RESEARCH.md Anti-Patterns.)

---

## Format A — Text-only storytelling post

Character count target: 1,301–2,500 body + hashtag line.

```
I spent three years building a spatial delay plugin in my Berlin apartment.

Today it ships — free.

[Paragraph 1 — the hook expansion. Keep it story-shape, not a press release. Reference that OSD
is the first tool in the Spatial Media Library pipeline. Each echo has a 3D position in space —
that's the one-sentence technical summary. Capability-level only. 200–400 chars.]

[Paragraph 2 — why. The Berlin indie-dev story. Working solo, why open-source, why GPL-3.0,
why Patreon instead of a paid plugin. Mirror tone of Patreon About copy. 400–700 chars.]

[Paragraph 3 — what's in v1.0.0 at capability level. 12 delay taps in 3D space. 7 spatialization
algorithms. 5 measured HRTF profiles + a CPU-lite mode. 70 factory presets. ADM-OSC for talking
to Spat Revolution, Panoramix, Iannix, TouchDesigner. VST3 + AU, mac + Windows. No DSP jargon.
300–500 chars.]

[Paragraph 4 — the SML pipeline framing. The DSP kernel under OSD is the foundation for what
comes next in the Spatial Media Library. Build the kernel once, ship N tools on top of it.
Short — 150–300 chars.]

If you're into spatial audio, immersive workflows, or just curious what a free GPL-3.0 spatial
delay sounds like — Link in bio.

(Berlin folks: I'll be at Superbooth Messe May 7–10 if you want to see it in person.)

#SpatialAudio #DolbyAtmos #AudioPlugin #IndieDev #OpenSource
```

## Format B — Native document carousel (7 pages)

Export as a 1080×1350 PDF (7 pages). Tool: Figma or Canva.

### Page-by-page spec

**Page 1 (cover)** — Black background, OSD wordmark centre, tagline below:
> Each echo has a 3D position in space.
> Free. GPL-3.0. VST3 + AU. macOS + Windows.
> OpenSpatialDelay v1.0.0 — out today

**Page 2 — "12 taps"**
Headline: 12 delay taps, each with an independent 3D position
Body: One plain-English sentence on what that means for a producer. No DSP.

**Page 3 — "7 spatialization algorithms"**
Headline: Seven ways to put sound in space
Body: Pick the algorithm that fits speakers, headphones, or a dome. Names listed in a small
grid: Ambisonics, ConstantPower, DBAP, KNN, MDAP, VBAP, VBIP.

**Page 4 — "5 measured HRTFs + Simple mode"**
Headline: Binaural rendering from real measurements
Body: KU100, CIPIC, HUTUBS, MIT KEMAR, SADIE — plus a CPU-lite mode for live work.
Capability-level — no HRIR / partition / FFT talk.

**Page 5 — "70 presets + ADM-OSC"**
Headline: Starting points + external control
Body: 70 factory presets. ADM-OSC in + out so the plugin talks to Spat Revolution, Panoramix,
Iannix, and TouchDesigner.

**Page 6 — screenshot**
Full-bleed screenshot_full.png. Single caption: the plugin UI. No overlay text.

**Page 7 — download CTA**
Headline: Get OpenSpatialDelay
Body:
> Link in bio → andrewrahman.com/get-osd
> Source: github.com/Spatial-Media-Lab/OpenSpatialDelay
> Support: patreon.com/AndrewRahman

### Companion caption (400–800 chars, posted alongside the document)

```
After three years of building in my Berlin apartment, OpenSpatialDelay v1.0.0 ships today —
free, GPL-3.0. Each echo has a 3D position in space. It's the first tool in the Spatial
Media Library pipeline.

Swipe through the doc for the capability tour. Link in bio when you're ready to download.

(Berlin folks at Superbooth May 7–10 — find me and I'll show you in person.)

#SpatialAudio #DolbyAtmos #AudioPlugin #IndieDev #OpenSource
```

---

## Why no link in comments

LinkedIn's 2026 algorithm specifically demotes posts whose author drops an external link in the
first comment (see 05-RESEARCH.md Pitfall 2 — dataslayer.ai Feb 2026 and blog.linkboost.co
reports). Bio-link CTA is the compliant 2026 pattern.
```

Fill in the bracketed paragraphs in Format A with actual prose. Target total body char count
1,301–2,500. Keep hashtags to exactly 5. For Format B, keep the page-by-page spec verbatim
and make sure the companion caption is 400–800 chars.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-launch-announcements/drafts/linkedin-post.md && grep -q "each echo has a 3D position" .planning/phases/05-launch-announcements/drafts/linkedin-post.md && ! grep -q "link in first comment" .planning/phases/05-launch-announcements/drafts/linkedin-post.md && ! grep -q "Link in first comment" .planning/phases/05-launch-announcements/drafts/linkedin-post.md && grep -c "^#SpatialAudio\|^#DolbyAtmos\|^#AudioPlugin\|^#IndieDev\|^#OpenSource" .planning/phases/05-launch-announcements/drafts/linkedin-post.md | awk '$1 >= 1 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/05-launch-announcements/drafts/linkedin-post.md` exists
    - Contains "Format A" and "Format B" section headings
    - Format A body character count (measured from hook through hashtag line, excluding Markdown fences): 1,301–2,500. Verify with awk/wc:
      `awk '/^```$/{c++;next} c==1 && c<2' file | wc -c` returns a value in range (approximate — inclusive of whitespace)
    - File contains exact hashtag string `#SpatialAudio #DolbyAtmos #AudioPlugin #IndieDev #OpenSource`
    - File contains literal `each echo has a 3D position`
    - File contains literal `Link in bio`
    - File contains literal `andrewrahman.com/get-osd`
    - File contains "Berlin" (case-insensitive) ≥ 3 times
    - File contains literal `Superbooth` (the May 7–10 in-person mention)
    - `grep -iE 'link in (first |the first )?comment' file` returns 0 matches
    - `grep -iE '(FFT|phase vocoder|Laroche|Dolson|Röbel|Gardner|Martin|2048-sample|STFT|partition)' file` returns 0 matches
    - File contains the PRE-LAUNCH bio-update instruction line (literal `andrewrahman.com/get-osd` appearing in the Contact info bio context — grep for "Contact info" AND "andrewrahman.com/get-osd" within 5 lines)
    - File contains a "Page 7" section (Format B page-by-page spec complete)
  </acceptance_criteria>
  <done>LinkedIn draft committed with both formats + pre-launch bio-update checklist + the "no link in comment" rule surfaced.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint — Andrew picks Format A or B, approves, updates bio at T-1</name>
  <files>(human-verification checkpoint — no file writes; see what-built block for artefacts under review)</files>
  <action>Pause execution. Human follows the steps in <how-to-verify> below. Resume only when the <resume-signal> condition is met.</action>
  <what-built>
    - .planning/phases/05-launch-announcements/drafts/linkedin-post.md (Format A text-only + Format B document carousel spec + companion caption)
  </what-built>
  <how-to-verify>
    1. Andrew reads both formats.
    2. Pick: Format A (faster, zero design time) OR Format B (~3× engagement per RESEARCH, but
       requires PDF design — ~60–90 min Figma/Canva work).
    3. If Format B chosen: Andrew schedules the PDF design task for T-2 (2026-04-26) using
       screenshot_full.png (already in andrewrahman-com/public/assets/) as page 6.
    4. At T-1 (2026-04-27): Andrew updates LinkedIn profile Contact info → Website to
       https://andrewrahman.com/get-osd. Verify by viewing own profile public-mode.
    5. Andrew drafts the chosen format into LinkedIn's post composer at T-0 prep time. Do NOT
       schedule or publish until launch-day-runbook step 5 (2026-04-28 10:20 CET).
    6. Confirm: the post does NOT have any external URL in the body OR in a pre-drafted first
       comment. Only CTA is "Link in bio" (Format A) or the "Link in bio" text on Page 7 (Format B).
  </how-to-verify>
  <resume-signal>Type "linkedin approved — format A" or "linkedin approved — format B" once the chosen format is in LinkedIn's post composer (not yet published) AND the bio-link is updated on Andrew's profile.</resume-signal>
  <verify>
    <automated>echo "checkpoint: awaiting human resume-signal"</automated>
  </verify>
  <done>Human types the resume-signal phrase, confirming the checkpoint conditions are satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LinkedIn post → Andrew's network + public | Algorithm-governed distribution; external-link behavior is the main risk |
| Bio-link field → profile visitors | Public; no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Denial of service (reach) | Post reach throttled if external link snuck into body or first comment | mitigate | Acceptance criterion grep forbids "link in first comment" phrase in draft; Task 2 checkpoint confirms no URL in body; bio-link is the only CTA. |
| T-05-03-02 | Spoofing | Document carousel PDF attachment could be replaced pre-upload | accept | Andrew drives the upload; solo creator; low risk. |
| T-05-03-03 | Information disclosure | Profile bio-link change is visible to all network contacts pre-launch | accept | Bio-link is a public field; pre-launch update is expected behavior. |
</threat_model>

<verification>
- Draft file passes acceptance greps (especially the `link in first comment` negative grep).
- Bio-link verified updated on Andrew's profile by T-1 evening.
- Post composer has the chosen format queued; Andrew types "linkedin approved" with format choice.
</verification>

<success_criteria>
- `.planning/phases/05-launch-announcements/drafts/linkedin-post.md` committed.
- Pre-launch: LinkedIn profile bio-link = `https://andrewrahman.com/get-osd`.
- Launch morning: Post published at 10:20 CET ±5 min in compliant 2026 format (no URL in body, no URL in first comment).
- 7 days post-launch: Andrew logs impressions/reactions in launch-day-results.md (no hard minimum; signal that Format chosen didn't trigger the LinkedIn Pitfall 2 throttle — impressions ≥ 500 is a healthy floor).
</success_criteria>

<output>
After completion, create `.planning/phases/05-launch-announcements/05-03-SUMMARY.md` recording:
chosen format (A or B), body char count, publish timestamp, bio-link pre-launch update
confirmation, day-1 reach snapshot (if available).
</output>
