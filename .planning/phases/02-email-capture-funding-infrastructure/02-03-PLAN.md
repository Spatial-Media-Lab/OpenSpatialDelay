---
phase: 02-email-capture-funding-infrastructure
plan: 03
type: execute
wave: 2
depends_on: ["02"]
files_modified:
  - site/app/privacy/page.tsx
  - site/tests/privacy-content.spec.ts
autonomous: false
requirements: [DIST-02]
must_haves:
  truths:
    - "The data-controller designation (D-16) has been explicitly confirmed by the user — the planner's default recommendation is Spatial Media Lab; the confirmed value is recorded in the plan summary and hardcoded into privacy/page.tsx"
    - "The privacy policy at /privacy contains complete, hand-authored prose for all 9 GDPR-required sections tailored to the actual Phase 2 data flow: email collected via Tally → stored in Tally dashboard → manual newsletter use in Phase 5+ (D-13)"
    - "Policy explicitly addresses GDPR (EU) + CCPA (California) + UK DPA jurisdictions (D-14)"
    - "Policy states retention = indefinite until user-initiated unsubscribe (D-15)"
    - "Policy lists `andrewjrahman@gmail.com` as the rights-request contact"
    - "Effective date line is populated with the actual publish date (YYYY-MM-DD), replacing the `[DATE TO BE CONFIRMED]` placeholder"
    - "A Playwright content test asserts every critical policy string (data controller, retention, contact email, rights enumeration) appears in the rendered page"
  artifacts:
    - path: "site/app/privacy/page.tsx"
      provides: "Fully-authored privacy policy — no `[Prose pending Plan 03.]` placeholders remain"
      contains: "Spatial Media Lab"
    - path: "site/tests/privacy-content.spec.ts"
      provides: "Assertion that the privacy page contains all jurisdictional clauses and the right contact email"
      contains: "andrewjrahman@gmail.com"
  key_links:
    - from: "site/app/privacy/page.tsx"
      to: "D-16 data controller confirmation (recorded in summary)"
      via: "explicit 'Data Controller:' line in Who-we-are section"
      pattern: "Data Controller.*Spatial Media Lab"
---

<objective>
Author the full GDPR + CCPA + UK DPA privacy policy prose for `andrewrahman.com/privacy`, replacing the 9 `[Prose pending Plan 03.]` placeholders in the page skeleton from Plan 02. Includes the mandatory user-checkpoint task to confirm the data-controller designation before prose is committed (D-16).

Purpose: Deliver DIST-02 (privacy policy page publicly accessible and linked from the Tally form). Prose must be tailored to the actual Phase 2 data flow, not a generator-bloat template (D-13). Links from the Tally consent checkbox and the /get-osd page footer will target this page.

Output: A completed `app/privacy/page.tsx` whose rendered HTML satisfies the required-clauses audit + a content test that enforces the critical strings.

Wave 2 — runs after Plan 02 (site scaffold exists) and in parallel with Plan 04 (hosting bake-off).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-email-capture-funding-infrastructure/02-CONTEXT.md
@.planning/phases/02-email-capture-funding-infrastructure/02-UI-SPEC.md
@.planning/phases/02-email-capture-funding-infrastructure/02-RESEARCH.md
@.planning/phases/02-email-capture-funding-infrastructure/02-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: DECIDE — Data Controller designation for privacy policy (D-16)</name>
  <decision>Who is the legal Data Controller for emails collected via the Tally form at andrewrahman.com/get-osd?</decision>
  <context>
    Under GDPR (Art. 4(7)), the Data Controller is "the natural or legal person … which … determines the purposes and means of the processing of personal data." The privacy policy must name this entity explicitly. Under CCPA and UK DPA the equivalent role (Business / Controller) must also be named.

    This is a legal designation, not a UX choice. CONTEXT.md D-16 flagged it for explicit user confirmation. The planner's recommended default is **Spatial Media Lab** because:
    - Phase 2 positioning is "funding the Spatial Media Library pipeline, developed through Spatial Media Lab" (D-22)
    - Source code attribution on all artifacts already points to SML (D-02)
    - SML is the stable org identity; Andrew Rahman personally might prefer not to name himself as legal controller on a public page accessible without auth

    Alternatives to consider:
    - **Andrew Rahman (natural person)** — simpler (no legal-entity paperwork implications), but exposes personal name as data controller on a public URL
    - **Both jointly (Andrew Rahman as natural person + SML as legal entity)** — permitted under GDPR Art. 26 (joint controllers) but requires a documented arrangement; probably overkill for this scope

    **Whichever the user picks, it must match SML's actual legal standing** (is SML a registered legal entity? a trade name? something else?). If SML has not been formally registered as an entity in a GDPR-relevant jurisdiction, "Spatial Media Lab" as sole controller may be legally dubious and falling back to "Andrew Rahman" may be safer.
  </context>
  <options>
    <option id="option-a">
      <name>Spatial Media Lab (recommended default per D-16)</name>
      <pros>Matches org identity across all other artifacts; keeps Andrew's personal name off the public policy</pros>
      <cons>Only valid if SML is actually a registered legal entity in a GDPR-relevant jurisdiction; unclear liability if SML is an unincorporated collective</cons>
    </option>
    <option id="option-b">
      <name>Andrew Rahman (natural person)</name>
      <pros>Unambiguous legal standing (any natural person can be a GDPR controller); simpler</pros>
      <cons>Personal name published on policy; less consistent with "pipeline funded through SML" framing elsewhere</cons>
    </option>
    <option id="option-c">
      <name>Joint: Andrew Rahman as natural person + Spatial Media Lab as co-controller</name>
      <pros>Most conservative</pros>
      <cons>Requires a documented joint-controller arrangement (GDPR Art. 26); probably disproportionate for a Phase 2 email list</cons>
    </option>
  </options>
  <resume-signal>Reply with "option-a", "option-b", or "option-c" (or a custom answer with a specific entity name). The plan summary and `app/privacy/page.tsx` Who-we-are section will hardcode the chosen string verbatim.</resume-signal>
  <files>N/A — checkpoint task (no file changes authored by Claude; user performs dashboard/decision work per resume-signal)</files>
  <action>Checkpoint task. The meaningful content is in the checkpoint-specific elements above (decision/context/options for decision checkpoints; what-built/how-to-verify/resume-signal for verify/action checkpoints). Claude MUST pause here and await the user response described in &lt;resume-signal&gt; before continuing the plan.</action>
  <verify>
    <automated>MISSING — checkpoint task; verification is the user's manual response per the &lt;how-to-verify&gt; or &lt;options&gt; block above. Executor records the user's resume-signal text as the verification artifact.</automated>
  </verify>
  <done>User has responded per the &lt;resume-signal&gt; above. Executor has recorded the response verbatim in the plan summary (for audit trail on decision checkpoints) or as evidence path (for verify checkpoints with screenshots).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author full privacy-policy prose for all 9 GDPR-required sections (D-13, D-14, D-15)</name>
  <files>&lt;SITE_ROOT&gt;/app/privacy/page.tsx</files>
  <read_first>
    - &lt;SITE_ROOT&gt;/app/privacy/page.tsx (current skeleton from Plan 02 — preserve all JSX structure, replace only the `[Prose pending Plan 03.]` text and the `[Data-controller designation pending user confirmation. Default per D-16: Spatial Media Lab.]` line)
    - .planning/phases/02-email-capture-funding-infrastructure/02-UI-SPEC.md §andrewrahman.com /privacy Page + §Required GDPR minimum sections
    - .planning/phases/02-email-capture-funding-infrastructure/02-CONTEXT.md §decisions D-13, D-14, D-15, D-16
    - .planning/phases/02-email-capture-funding-infrastructure/02-RESEARCH.md §Pitfall 6 (no generator-bloat clauses)
    - .planning/PROJECT.md §Brand Identity (confirm SML vs Spatial Media Library disambiguation when both appear in the policy)
  </read_first>
  <action>
    Let `&lt;CONTROLLER&gt;` = the entity name chosen in Task 1 (e.g., "Spatial Media Lab" for option-a, "Andrew Rahman" for option-b). If `&lt;CONTROLLER&gt;` = "Spatial Media Lab", also include the clarifying address line per UI-SPEC: "This policy is issued by Spatial Media Lab, the organization responsible for OpenSpatialDelay and the Spatial Media Library." If `&lt;CONTROLLER&gt;` = "Andrew Rahman", use: "This policy is issued by Andrew Rahman (sole data controller), operating andrewrahman.com in connection with OpenSpatialDelay and the Spatial Media Library pipeline."

    Let `&lt;EFFECTIVE_DATE&gt;` = today's date in YYYY-MM-DD (the day the policy is being authored). Record this in the summary.

    Replace the contents of `&lt;SITE_ROOT&gt;/app/privacy/page.tsx` to preserve all JSX wrappers (the `&lt;article&gt;`, `&lt;section&gt;`, `&lt;h1&gt;`, `&lt;h2&gt;` elements and Tailwind classes from Plan 02) but substitute each section's `&lt;p&gt;[Prose pending Plan 03.]&lt;/p&gt;` with the authored prose below. Also update the effective-date line and the data-controller line (the latter replaces the pending-confirmation placeholder).

    **Effective date line** (replaces the `[DATE TO BE CONFIRMED BEFORE PUBLISHING]` placeholder):
    ```
    Effective: &lt;EFFECTIVE_DATE&gt;
    ```

    **Data-controller line** (replaces the pending-confirmation placeholder, first paragraph under the header):
    ```
    [controller-issuance sentence based on Task 1 choice, per the mapping above]
    ```

    **Section 1 — Who we are:**
    ```
    &lt;CONTROLLER&gt; is the data controller for personal data collected through this website. You can reach the controller at andrewjrahman@gmail.com. "Spatial Media Lab" refers to the open-source organization at github.com/Spatial-Media-Lab. "Spatial Media Library" refers to the pipeline of spatial-audio tools we build. "OpenSpatialDelay" is the first tool in that pipeline.
    ```

    **Section 2 — What data we collect and why:**
    ```
    We collect your email address and your consent signal when you submit the form at andrewrahman.com/get-osd. We collect these items for one purpose only: to give you the OpenSpatialDelay installer and to send occasional updates about OpenSpatialDelay and other Spatial Media Library tools. The lawful basis for this processing is your consent (GDPR Art. 6(1)(a); UK GDPR Art. 6(1)(a); CCPA equivalent: your direct opt-in at the form).

    We do not collect any other personal data on this site. No cookies are set by andrewrahman.com itself. No analytics, advertising trackers, or session recorders run on this site.
    ```

    **Section 3 — How we use your data:**
    ```
    We use your email to (a) deliver the OpenSpatialDelay installer and (b) send occasional updates about new Spatial Media Library tools. We do not sell your email. We do not share it with advertisers. We do not profile you or make automated decisions based on your data.
    ```

    **Section 4 — Who we share your data with:**
    ```
    We use Tally.so (Tally Technologies SRL, Belgium) as a data processor to run the email-capture form. Submissions are stored in the Tally dashboard, which only the controller can access. Tally acts on our instructions only; their processing terms are published at tally.so/dpa.

    We may, in the future, transfer your email to a newsletter-sending service (for example, Mailchimp, Buttondown, or an equivalent). If and when that happens, this policy will be updated to name the processor and the transfer will only occur under an appropriate data-processing agreement. You will be notified before any such change takes effect.

    We do not transfer your data outside the EEA/UK to jurisdictions without an adequacy decision. Tally stores EU submissions in the EU.
    ```

    **Section 5 — How long we keep your data:**
    ```
    We retain your email indefinitely until you ask us to delete it, or until you unsubscribe. This is because the list serves a long-running purpose (occasional updates about a continuing pipeline of tools) and there is no natural expiry. You can delete your record at any time — see "How to exercise your rights" below.
    ```

    **Section 6 — Your rights:**
    ```
    Under GDPR and UK GDPR you have the right to:
    - access your personal data (Art. 15)
    - ask us to correct inaccurate data (Art. 16)
    - ask us to delete your data (Art. 17, "right to be forgotten")
    - restrict or object to our processing (Art. 18, 21)
    - receive a portable copy of your data (Art. 20)
    - withdraw consent at any time (Art. 7(3))
    - lodge a complaint with a supervisory authority (your national data-protection authority; in the UK, the ICO)

    If you are a California resident, you have equivalent rights under the CCPA/CPRA, including:
    - the right to know what personal information we hold about you
    - the right to delete that information
    - the right to opt out of any "sale" or "sharing" (we do not sell or share your data, so nothing to opt out of)
    - the right to non-discrimination for exercising these rights

    Under the UK Data Protection Act 2018 and the UK GDPR your rights mirror the EU GDPR rights above.
    ```

    **Section 7 — How to exercise your rights:**
    ```
    Email andrewjrahman@gmail.com from the address on file. We will respond within 30 days (GDPR Art. 12(3)). There is no charge for exercising your rights. You do not need a form or a specific subject line — a plain-text request naming the right you want to exercise is sufficient.
    ```

    **Section 8 — How to unsubscribe:**
    ```
    To unsubscribe, email andrewjrahman@gmail.com with the word "unsubscribe" in the subject or body. Once we start sending newsletters (Phase 5 of the OpenSpatialDelay roadmap), every message will include an unsubscribe link in the footer; clicking it immediately removes you from the list. Unsubscribing has the same effect as withdrawing consent: your record is deleted, not just flagged.
    ```

    **Section 9 — Changes to this policy:**
    ```
    If we materially change this policy — for example, by adding a newsletter processor, changing the lawful basis, or starting to use analytics — we will update the effective date at the top of this page and notify everyone currently on the list by email before the change takes effect. Minor editorial changes (fixing typos, clarifying language) will not trigger a notification but the effective date will still be bumped.
    ```

    After substitution, run `npm run build` and confirm `out/privacy/index.html` contains every one of: `andrewjrahman@gmail.com`, `&lt;CONTROLLER&gt;`, `Tally Technologies SRL`, `indefinitely until you ask us to delete`, `CCPA/CPRA`, `UK GDPR`, `supervisory authority`, `Spatial Media Library`.

    Also run `npm run test` — Playwright smoke test should still pass since section headings are unchanged.
  </action>
  <verify>
    <automated>cd &lt;SITE_ROOT&gt; &amp;&amp; npm run build &amp;&amp; ! grep -q "Prose pending Plan 03" app/privacy/page.tsx &amp;&amp; ! grep -q "Data-controller designation pending user confirmation" app/privacy/page.tsx &amp;&amp; ! grep -q "&lt;CONTROLLER&gt;" app/privacy/page.tsx &amp;&amp; grep -q "andrewjrahman@gmail.com" out/privacy/index.html &amp;&amp; grep -q "Tally Technologies SRL" out/privacy/index.html &amp;&amp; grep -q "CCPA/CPRA" out/privacy/index.html &amp;&amp; grep -q "UK GDPR" out/privacy/index.html &amp;&amp; npm run test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Prose pending Plan 03" &lt;SITE_ROOT&gt;/app/privacy/page.tsx` returns 0 (all 9 placeholders replaced)
    - `grep -c "Data-controller designation pending user confirmation" &lt;SITE_ROOT&gt;/app/privacy/page.tsx` returns 0 (confirmation placeholder replaced)
    - `grep -q "DATE TO BE CONFIRMED BEFORE PUBLISHING" &lt;SITE_ROOT&gt;/app/privacy/page.tsx` returns 1 (effective date populated)
    - `grep -q "&lt;CONTROLLER&gt;" &lt;SITE_ROOT&gt;/app/privacy/page.tsx` returns 1 (literal placeholder replaced with actual entity name from Task 1 decision)
    - `grep -q "andrewjrahman@gmail.com" &lt;SITE_ROOT&gt;/app/privacy/page.tsx` returns 0 (rights-contact present)
    - `grep -q "Tally Technologies SRL" &lt;SITE_ROOT&gt;/app/privacy/page.tsx` returns 0 (processor named per Pitfall 6)
    - `grep -q "GDPR Art. 15" &lt;SITE_ROOT&gt;/app/privacy/page.tsx` returns 0 (rights enumeration is specific, not generic)
    - `grep -q "CCPA" &lt;SITE_ROOT&gt;/app/privacy/page.tsx` returns 0 AND `grep -q "UK GDPR" &lt;SITE_ROOT&gt;/app/privacy/page.tsx` returns 0 (three-jurisdiction coverage per D-14)
    - `grep -q "indefinitely until you ask us to delete" &lt;SITE_ROOT&gt;/app/privacy/page.tsx` returns 0 (retention per D-15)
    - `npm run build` exits 0; `out/privacy/index.html` exists and contains all the above strings
    - `npm run test` exits 0 (Plan 02 smoke tests still pass)
  </acceptance_criteria>
  <done>Privacy page has complete, actual-data-flow prose for all 9 GDPR sections; no placeholders remain.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Playwright content test enforcing critical policy strings</name>
  <files>&lt;SITE_ROOT&gt;/tests/privacy-content.spec.ts</files>
  <read_first>
    - &lt;SITE_ROOT&gt;/tests/smoke.spec.ts (follow the same Playwright patterns)
    - &lt;SITE_ROOT&gt;/app/privacy/page.tsx (confirm the exact strings the test should match)
  </read_first>
  <behavior>
    - The privacy page contains the confirmed data-controller name (from Task 1)
    - The privacy page contains the rights-request contact email `andrewjrahman@gmail.com`
    - The privacy page explicitly names `Tally Technologies SRL` as the data processor
    - The privacy page mentions all three jurisdictions: GDPR, CCPA/CPRA, UK GDPR (or UK DPA)
    - The privacy page states retention is "indefinitely until you ask us to delete"
    - Regression: if any of these strings are accidentally removed by a future edit, the test fails
  </behavior>
  <action>
    Let `&lt;CONTROLLER&gt;` = the exact entity name chosen in Task 1 (e.g. "Spatial Media Lab").

    Create `&lt;SITE_ROOT&gt;/tests/privacy-content.spec.ts`:
    ```typescript
    import { test, expect } from '@playwright/test';

    test.describe('privacy policy content', () =&gt; {
      test.beforeEach(async ({ page }) =&gt; {
        await page.goto('/privacy');
      });

      test('names the confirmed data controller', async ({ page }) =&gt; {
        await expect(page.locator('body')).toContainText('&lt;CONTROLLER&gt;');
      });

      test('lists rights-request contact email', async ({ page }) =&gt; {
        await expect(page.locator('body')).toContainText('andrewjrahman@gmail.com');
      });

      test('names Tally as a data processor (not a generic third-party reference)', async ({ page }) =&gt; {
        await expect(page.locator('body')).toContainText('Tally Technologies SRL');
      });

      test('covers all three jurisdictions per D-14', async ({ page }) =&gt; {
        await expect(page.locator('body')).toContainText(/GDPR/);
        await expect(page.locator('body')).toContainText(/CCPA/);
        await expect(page.locator('body')).toContainText(/UK GDPR|UK DPA/);
      });

      test('states indefinite retention until unsubscribe per D-15', async ({ page }) =&gt; {
        await expect(page.locator('body')).toContainText('indefinitely until you ask us to delete');
      });

      test('enumerates specific GDPR rights articles, not generic language', async ({ page }) =&gt; {
        await expect(page.locator('body')).toContainText('Art. 15');
        await expect(page.locator('body')).toContainText('Art. 17');
        await expect(page.locator('body')).toContainText('Art. 20');
      });
    });
    ```

    Run `npm run test` — the new 6 tests must pass in addition to the Plan 02 smoke tests.
  </action>
  <verify>
    <automated>cd &lt;SITE_ROOT&gt; &amp;&amp; npm run test</automated>
  </verify>
  <acceptance_criteria>
    - `&lt;SITE_ROOT&gt;/tests/privacy-content.spec.ts` exists with exactly 6 tests under a single `describe` block
    - Each test asserts one of the critical strings
    - `npm run test` exits 0 (all Plan 02 + Plan 03 tests pass — expected total: 4 + 6 = 10 passing tests)
  </acceptance_criteria>
  <done>Privacy policy content is regression-protected; any future edit that removes a required clause fails CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Policy page → legal exposure | The policy makes claims about how data is handled. If prose contradicts reality (e.g. claims "we don't use cookies" while a component adds one, or claims "EU-only storage" while Tally stores in US), project is legally exposed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | R (Repudiation) | Privacy policy prose | mitigate | Effective date is populated with YYYY-MM-DD at publish time and can be git-blamed to a specific commit. Section 9 ("Changes to this policy") commits the controller to updating the effective date on any material change, giving users a verifiable history. |
| T-02-09 | I (Information Disclosure) | Controller's personal email `andrewjrahman@gmail.com` on public page | mitigate | This is intentional — GDPR Art. 13(1)(a)/(b) requires the controller's contact details be published. Personal email is Andrew's existing public address. Accept with documentation. |
| T-02-10 | T (Tampering) | Critical clauses removable by future edits | mitigate | Playwright content test in Task 3 fails CI if any of: controller name, contact email, processor name, retention statement, or GDPR article references are removed. |
| T-02-11 | I (Information Disclosure) | Policy claims about Tally EU storage | mitigate | Cite Tally Technologies SRL (Belgium) explicitly. Plan 04 manual UAT MUST confirm Tally form's data-region setting. If it's US-only, Plan 03 Task 2 prose must be revised before Plan 04 DNS cutover goes live. |
| T-02-12 | S (Spoofing) | Controller identity misrepresentation | mitigate | Task 1 checkpoint forces explicit user confirmation. The entity named must match actual legal standing — plan summary must record Task 1 answer verbatim for audit. |

**High-severity threat present: T-02-11 (policy claims must match reality).** Mitigation: Plan 04 execution MUST verify Tally's data-region setting before production cutover.
</threat_model>

<verification>
- `npm run test` exits 0 (4 smoke tests + 6 content tests = 10 passing)
- `grep -c "Prose pending Plan 03" &lt;SITE_ROOT&gt;/app/privacy/page.tsx` returns 0
- `grep -c "Data-controller designation pending" &lt;SITE_ROOT&gt;/app/privacy/page.tsx` returns 0
- Manual UAT: `cd &lt;SITE_ROOT&gt; &amp;&amp; npm run dev`, visit `/privacy`, read the full policy — confirm it describes the actual data flow (Tally → dashboard → manual newsletter later) and does NOT mention cookies, analytics, ads, or third-party trackers (Pitfall 6)
</verification>

<success_criteria>
- [ ] Task 1 data-controller checkpoint resolved with a specific entity name
- [ ] All 9 `[Prose pending Plan 03.]` placeholders replaced with authored prose
- [ ] Effective date populated with actual YYYY-MM-DD
- [ ] All three jurisdictions (GDPR, CCPA, UK GDPR) addressed
- [ ] Retention policy reflects D-15 (indefinite until unsubscribe)
- [ ] Rights-contact email is `andrewjrahman@gmail.com`
- [ ] Playwright content tests added and passing
- [ ] `npm run build` + `npm run test` pass
- [ ] Site-repo commit created: "feat(privacy): author GDPR+CCPA+UK DPA policy prose"
</success_criteria>

<output>
After completion, create `.planning/phases/02-email-capture-funding-infrastructure/02-03-SUMMARY.md` capturing:
- The Task 1 data-controller answer verbatim (for audit trail)
- The effective date used
- Confirmation that all 9 placeholders are replaced
- Confirmation that Plan 04 (DNS cutover) now has a live privacy page to point at from the Tally consent link and Patreon footer
- Any legal-review follow-ups the user identified during Task 1 checkpoint
</output>
