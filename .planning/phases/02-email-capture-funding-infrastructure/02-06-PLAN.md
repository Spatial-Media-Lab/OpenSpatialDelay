---
phase: 02-email-capture-funding-infrastructure
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-email-capture-funding-infrastructure/drafts/patreon-page-about.md
  - .planning/phases/02-email-capture-funding-infrastructure/drafts/patreon-tier-copy.md
  - .planning/phases/02-email-capture-funding-infrastructure/drafts/patreon-post-01-welcome.md
  - .planning/phases/02-email-capture-funding-infrastructure/drafts/patreon-post-02-technical.md
  - .planning/phases/02-email-capture-funding-infrastructure/drafts/patreon-post-03-roadmap.md
autonomous: false
requirements: [DIST-03]
user_setup:
  - service: patreon
    why: "Required for DIST-03 — hosts the creator page, tiers, and posts. Patreon is the sole funding channel per PROJECT.md constraints"
    dashboard_config:
      - task: "Create Patreon creator account as 'Andrew Rahman' (personal, not SML) per D-21"
        location: "patreon.com/signup/creator"
      - task: "Claim the vanity URL `patreon.com/andrewrahman` (verify availability — first-come-first-served)"
        location: "Patreon creator dashboard → Page settings → URL"
      - task: "Choose billing model = Standard 10% plan (default for new creators post-2025-08-04 per RESEARCH.md)"
        location: "Patreon creator dashboard → Payouts"
      - task: "Configure 4 monthly tiers ($3/$10/$25/$100) using the copy drafted in this plan"
        location: "Patreon creator dashboard → Membership → Tiers"
      - task: "Populate About section, source code callout, and Berlin one-liner using drafts from this plan"
        location: "Patreon creator dashboard → Page settings"
      - task: "Draft + publish 3 seed posts per D-28 (Post 1 public, Post 2 patron-only $3+, Post 3 public)"
        location: "Patreon creator dashboard → Posts"
      - task: "Click 'Launch page' to make the creator page publicly accessible (RESEARCH.md Pitfall 3 — posts aren't visible until page is launched)"
        location: "Patreon creator dashboard → Launch"
      - task: "Do NOT enable annual billing (not eligible until page is live 3+ months AND earning $200+/mo per RESEARCH.md Pitfall 4)"
        location: "Patreon creator dashboard"
must_haves:
  truths:
    - "A Patreon creator page exists at `patreon.com/andrewrahman` (vanity URL), identified as 'Andrew Rahman' (D-21 — personal identity, not SML, not Spatial Media Library)"
    - "The page About / tagline text uses the exact UI-SPEC copy: tagline 'Funding the Spatial Media Library — spatial audio tools for musicians and sound designers'; About summary references Spatial Media Library pipeline AND Spatial Media Lab source-code org distinctly (D-22, Pitfall 8)"
    - "4 monthly tiers exist exactly as specced in UI-SPEC Patreon Page tier table: $3 Stargazer, $10 Astronaut, $25 Commander, $100 Mission Control (D-23) — NO annual billing enabled (RESEARCH.md Pitfall 4)"
    - "Page includes the sidebar/footer Berlin one-liner (D-25): 'In Berlin? Come by Spatial Media Lab — we run events and collaborate with local artists.' linking to SpatialMediaLab.org"
    - "Page cross-links (D-26): (a) github.com/Spatial-Media-Lab/OpenSpatialDelay as source, (b) SpatialMediaLab.org as org identity, (c) andrewrahman.com as creator home, (d) andrewrahman.com/privacy in footer"
    - "3 seed posts are published with visibility per D-28: Post 1 (Welcome / Why Patreon / Pipeline Vision) = PUBLIC; Post 2 (OSD Technical Deep-Dive) = PATRON-ONLY ($3+ tier); Post 3 (Spatial Media Library Roadmap) = PUBLIC"
    - "The page is LAUNCHED — visiting `patreon.com/andrewrahman` in a logged-out browser shows the page, tier structure, and the 2 public posts (RESEARCH.md Pitfall 3)"
    - "DIST-03 satisfied: at least 2 public posts AND patron-value framing 'spatial audio tools pipeline, not just one plugin' is visible without a login"
  artifacts:
    - path: ".planning/phases/02-email-capture-funding-infrastructure/drafts/patreon-page-about.md"
      provides: "Hand-authored About / tagline / source-code callout / Berlin one-liner copy — source of truth for what's pasted into the Patreon dashboard"
      contains: "Spatial Media Library"
    - path: ".planning/phases/02-email-capture-funding-infrastructure/drafts/patreon-tier-copy.md"
      provides: "Tier-by-tier name, price, and benefits copy (D-23 baseline for user revision) — space-themed labels: Stargazer/Astronaut/Commander/Mission Control"
      contains: "Stargazer"
    - path: ".planning/phases/02-email-capture-funding-infrastructure/drafts/patreon-post-01-welcome.md"
      provides: "Public seed post 1 — Welcome + Why Patreon + Pipeline Vision"
      contains: "Spatial Media Lab"
    - path: ".planning/phases/02-email-capture-funding-infrastructure/drafts/patreon-post-02-technical.md"
      provides: "Patron-only ($3+) seed post 2 — HRTF + Phase Vocoder + Trajectory Engine technical deep-dive"
      contains: "HRTF"
    - path: ".planning/phases/02-email-capture-funding-infrastructure/drafts/patreon-post-03-roadmap.md"
      provides: "Public seed post 3 — Spatial Media Library roadmap (pipeline themes, no timelines)"
      contains: "pipeline"
    - path: "docs/phase-02-evidence/patreon-page-live.png"
      provides: "Logged-out browser screenshot proving Pitfall 3 is mitigated — page actually launched"
    - path: "docs/phase-02-evidence/patreon-tiers.png"
      provides: "Screenshot of tier configuration in dashboard (4 tiers, monthly only)"
  key_links:
    - from: "patreon.com/andrewrahman"
      to: "github.com/Spatial-Media-Lab/OpenSpatialDelay"
      via: "inline link in About section + source-code callout"
      pattern: "github.com/Spatial-Media-Lab"
    - from: "patreon.com/andrewrahman"
      to: "andrewrahman.com/privacy"
      via: "page footer link (consistency with Plan 03)"
      pattern: "andrewrahman.com/privacy"
    - from: "patreon.com/andrewrahman seed post 1"
      to: "Spatial Media Library pipeline pitch (D-22, satisfies DIST-03 framing criterion)"
      via: "post body text"
      pattern: "spatial audio tools.*pipeline"
---

<objective>
Stand up the Patreon creator page `patreon.com/andrewrahman` under Andrew Rahman's personal identity (D-21), configure 4 monthly tiers (D-23, UI-SPEC table), draft + publish 3 seed posts (D-27/D-28), and LAUNCH the page so it's publicly visible. This plan is independent of the andrewrahman.com site and runs in Wave 1 in parallel with Plans 01/02.

Purpose: Deliver DIST-03 — Patreon page published with 2+ posts and pipeline-funding framing. Plan 05's Tally thank-you page already links to `patreon.com/andrewrahman`; this plan makes that link a working destination.

Output: A live Patreon page with tier structure + 3 seed posts visible in a logged-out browser; 5 Markdown drafts committed to OSD repo under `.planning/phases/02-email-capture-funding-infrastructure/drafts/` as the canonical copy that was pasted into Patreon (so revisions can diff against them).

Wave 1 — independent of the site repo. Can execute as soon as CONTEXT.md decisions are locked.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-email-capture-funding-infrastructure/02-CONTEXT.md
@.planning/phases/02-email-capture-funding-infrastructure/02-UI-SPEC.md
@.planning/phases/02-email-capture-funding-infrastructure/02-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false" status="complete" completed_at="2026-04-16">
  <name>Task 1: Draft Patreon page About + tier copy + 3 seed posts as in-repo Markdown — COMPLETE 2026-04-16</name>
  <files>
    .planning/phases/02-email-capture-funding-infrastructure/drafts/patreon-page-about.md,
    .planning/phases/02-email-capture-funding-infrastructure/drafts/patreon-tier-copy.md,
    .planning/phases/02-email-capture-funding-infrastructure/drafts/patreon-post-01-welcome.md,
    .planning/phases/02-email-capture-funding-infrastructure/drafts/patreon-post-02-technical.md,
    .planning/phases/02-email-capture-funding-infrastructure/drafts/patreon-post-03-roadmap.md
  </files>
  <read_first>
    - .planning/phases/02-email-capture-funding-infrastructure/02-UI-SPEC.md §Patreon Page + §Patreon Seed Posts (exact copy baselines)
    - .planning/phases/02-email-capture-funding-infrastructure/02-CONTEXT.md §decisions D-21 through D-29
    - .planning/PROJECT.md §Brand Identity (Plan 01 added this — use SML vs Spatial Media Library disambiguation)
    - agent_docs/architecture.md (Progressive disclosure: load for seed post 2 technical details — HRTF, PartitionedConvolver, Phase Vocoder, dry path compensation)
  </read_first>
  <action>
    Create directory `.planning/phases/02-email-capture-funding-infrastructure/drafts/` if it does not exist.

    **Draft 1 — `drafts/patreon-page-about.md`** (paste into Patreon dashboard Page Settings):
    ```markdown
    # Patreon Page — About / Tagline / Footer Copy

    > Paste into corresponding Patreon dashboard fields. Do not edit Markdown formatting in this file
    > without updating what lives on Patreon (this file is the source of truth for what's published).

    ## Page Tagline (Patreon's "What are you creating?" field)

    Funding the Spatial Media Library — spatial audio tools for musicians and sound designers

    ## About Section

    I'm Andrew Rahman, an audio software developer based in Berlin. I build spatial audio tools under
    the **Spatial Media Library** project, developed open-source through **Spatial Media Lab**.

    **OpenSpatialDelay** is the first — a free GPL-3.0 spatial delay plugin for VST3 + AU on macOS and
    Windows. It places each echo at a distinct 3D position around the listener, using HRTF convolution
    for binaural rendering, a phase vocoder for pitch-independent time-stretching, and a trajectory
    engine for per-echo Doppler simulation.

    The tools are free. Your support keeps them free. Patreon funds the pipeline so I can keep
    shipping spatial-audio tools without chasing licensing revenue.

    ## Source Code Callout (inline in About, or in the "links" section)

    OpenSpatialDelay source code lives at
    [github.com/Spatial-Media-Lab/OpenSpatialDelay](https://github.com/Spatial-Media-Lab/OpenSpatialDelay).
    The Spatial Media Lab organization — the legal home of the code — is at
    [SpatialMediaLab.org](https://spatialmedialab.org).

    ## Future Plugins Soft-Mention (D-24)

    OpenSpatialDelay is GPL and free for everyone. Some future Spatial Media Library tools will launch
    on Patreon first or as patron-exclusive — those will be announced as they come. No specific
    timeframes committed.

    ## Annual Billing Soft-Mention (RESEARCH.md Pitfall 4)

    Annual membership at a ~15% discount is planned — coming once eligibility requirements are met
    (3 months live + sustained monthly earnings).

    ## Berlin One-Liner (D-25, sidebar or footer placement)

    In Berlin? Come by **Spatial Media Lab** — we run events and collaborate with local artists.
    [spatialmedialab.org](https://spatialmedialab.org)

    ## Cross-Links (D-26 — add to page's "Links" section or inline where Patreon allows)

    - Source code: [github.com/Spatial-Media-Lab/OpenSpatialDelay](https://github.com/Spatial-Media-Lab/OpenSpatialDelay)
    - Org identity: [SpatialMediaLab.org](https://spatialmedialab.org)
    - Creator home: [andrewrahman.com](https://andrewrahman.com)
    - Privacy: [andrewrahman.com/privacy](https://andrewrahman.com/privacy)
    ```

    **Draft 2 — `drafts/patreon-tier-copy.md`** (paste into Patreon dashboard Membership → Tiers):
    ```markdown
    # Patreon Tier Copy

    > All tiers are **monthly** at launch. Annual billing omitted (RESEARCH.md Pitfall 4 — not eligible
    > until page is 3 months live + earning $200+/mo).

    ## Tier 1 — $3/mo · Stargazer

    **Name:** Stargazer
    **Price:** $3 / month
    **Benefits:**
    - Newsletter updates on new Spatial Media Library tools
    - Public acknowledgment as a Spatial Media Library supporter

    ## Tier 2 — $10/mo · Astronaut

    **Name:** Astronaut
    **Price:** $10 / month
    **Benefits:**
    - All Stargazer benefits
    - Vote on feature priorities for OpenSpatialDelay and future Spatial Media Library tools
    - Access to early builds before public release

    ## Tier 3 — $25/mo · Commander

    **Name:** Commander
    **Price:** $25 / month
    **Benefits:**
    - All Astronaut benefits
    - 1:1 access via Discord or email
    - Early access to future patron-exclusive Spatial Media Library tools

    ## Tier 4 — $100/mo · Mission Control

    **Name:** Mission Control
    **Price:** $100 / month
    **Benefits:**
    - All Commander benefits
    - Permanent public thank-you credit in the OpenSpatialDelay About dialog
    ```

    **Draft 3 — `drafts/patreon-post-01-welcome.md`** (public post at launch):
    ```markdown
    # Seed Post 1 — Welcome to the Spatial Media Library Patreon

    **Visibility:** PUBLIC (per D-28)
    **Publish order:** 1st (anchor post)

    ---

    Hi — I'm Andrew. I build spatial audio software through **Spatial Media Lab**, a small organization
    based in Berlin. This Patreon funds the **Spatial Media Library** pipeline: a set of spatial audio
    tools for musicians and sound designers, released as GPL open-source.

    ## Why Patreon

    The tools are free and the source code is always public. But building them takes real time and
    hardware. Patreon lets supporters directly fund the pipeline so I can keep shipping tools without
    chasing licensing revenue. The first tool — **OpenSpatialDelay** — is already out. More are in
    progress. Your support determines the pace.

    ## Pipeline Vision

    The Spatial Media Library is a pipeline, not a single plugin. Spatial audio is a deep space —
    binaural rendering, convolution reverbs, trajectory-driven effects, immersive mixing tools. There
    is room for a dozen focused, free, open-source tools in this space, and I want to build them.

    OpenSpatialDelay is proof the approach works. What comes next is what patrons help fund.

    ## Source Code

    Source code lives at **Spatial Media Lab**:
    [github.com/Spatial-Media-Lab](https://github.com/Spatial-Media-Lab). GPL-3.0. Fork it, study it,
    improve it.

    ## Call to Action

    If you use OpenSpatialDelay or care about open-source spatial audio, consider becoming a patron.
    Every tier counts. $3/mo keeps one project alive; $100/mo keeps three going.

    — Andrew
    ```

    **Draft 4 — `drafts/patreon-post-02-technical.md`** (patron-only $3+ post at launch):
    ```markdown
    # Seed Post 2 — Inside OpenSpatialDelay: HRTF, Phase Vocoder, and 3D Trajectory

    **Visibility:** PATRON-ONLY ($3+ Stargazer tier, per D-28)
    **Publish order:** 2nd
    **Public teaser (shows above the paywall):** A look at the DSP architecture behind OpenSpatialDelay
    — for patrons who want to know how it actually works.

    ---

    OpenSpatialDelay is a spatial delay plugin where every echo lives at a distinct 3D position. This
    post walks through the DSP building blocks that make that possible.

    ## 1. HRTF Convolution — How Each Echo Lands at a Specific Position

    An echo gets its spatial location by convolving the tap's audio with a pair of HRIRs (head-related
    impulse responses) — one for the left ear, one for the right. The plugin ships with 6 HRTF
    profiles loaded from SOFA files: KEMAR dummy head, CIPIC subjects 20/21/27/40, and the MIT KEMAR
    set. Each profile is a 2 × 360 × N array of impulse responses indexed by azimuth.

    The convolution runs through a **partitioned convolver** — the impulse response is split into
    equally-sized blocks, FFT'd up-front, and the incoming audio is convolved block-by-block in the
    frequency domain. This keeps per-buffer CPU bounded for IRs that can be thousands of samples long.

    ## 2. Phase Vocoder — Pitch-Independent Time-Stretching

    The Doppler effect in OpenSpatialDelay needs to stretch or compress the echo's playback rate
    without changing pitch. Pure resampling couples the two — faster = higher pitch. The plugin uses a
    **phase vocoder** with WSOLA-Lite adjustments for tighter transient preservation:

    - STFT the signal at a fixed FFT size (see agent_docs/architecture.md for the current value)
    - Adjust inter-frame phase to match the target stretch ratio
    - Overlap-add with WSOLA correlation windowing to smooth discontinuities

    The dry path carries a matching 2048-sample latency compensation so wet-dry mixes stay phase-
    aligned. If you set the plugin to 100% dry, the output is still latency-compensated.

    ## 3. Trajectory Engine — Per-Echo 3D Position

    Each echo has an independent position in 3D space. The trajectory engine lets you define a path
    (line, circle, orbit, hand-drawn spline) that all echoes follow with configurable delay offsets
    between them. The engine drives the HRTF convolver's azimuth/elevation interpolation frame-by-
    frame, and separately feeds the Doppler engine an instantaneous velocity for pitch-free time-
    stretch.

    The **ITD/ILD model** adds interaural time and level differences on top of HRTF convolution for
    close-range source simulation (under 1.5m). Note: for the MIT KEMAR profile, ITD is baked into
    the HRIR waveform itself and the ITD delay line runs as a pass-through.

    ## 4. What This Architecture Enables Next

    The DSP kernel here — partitioned convolution, phase vocoder, trajectory engine — isn't delay-
    specific. It's a toolkit for a whole pipeline of spatial effects: spatial reverbs, convolution-
    based placement processors, binaural chorus, moving-source simulations. Subsequent Spatial Media
    Library tools will share these primitives.

    That's the technical case for the pipeline framing: we build the kernel once, then ship N tools
    on top of it.

    — Andrew
    ```

    **Draft 5 — `drafts/patreon-post-03-roadmap.md`** (public post at launch):
    ```markdown
    # Seed Post 3 — Where the Spatial Media Library Is Going

    **Visibility:** PUBLIC (per D-28)
    **Publish order:** 3rd

    ---

    OpenSpatialDelay is live. Here's the broader context for what the Spatial Media Library is trying
    to build.

    ## Pipeline Themes

    The Spatial Media Library is about three things:

    1. **Spatial processing** — effects where position in 3D space is a first-class parameter, not a
       post-hoc pan. OpenSpatialDelay is the first example: each echo is placed, not panned.
    2. **Binaural rendering** — making 3D audio work on headphones, which is how most people actually
       listen. HRTF convolution is the foundation; future tools will extend this to reverbs,
       ambisonic decoders, and close-range simulations.
    3. **Immersive mixing tools** — utilities that help you work with spatial audio during a session,
       not just print it at the end. This category is wide open and will grow fastest.

    ## No Specific Promises

    I'm not announcing specific plugins or timelines here. The Spatial Media Library is a commitment
    to keep shipping in this space, at a pace set by patron support. If this Patreon funds at a level
    that lets me work on it full-time, the pipeline ships fast. If it funds at a level that buys me
    a few focused weekends per month, the pipeline ships slower but still ships.

    Either way, everything will be GPL and free. That's non-negotiable.

    ## Why Open Source

    Free + open-source tools outlast their authors. If I stop shipping tomorrow, OpenSpatialDelay
    doesn't disappear. Anyone can fork it, improve it, release their own builds. The Spatial Media
    Library exists to grow that commons, not to lock anyone in.

    If you've found OpenSpatialDelay useful, or you care about open-source spatial audio generally,
    consider becoming a patron. The pace of the pipeline is the pace you set.

    — Andrew
    ```

    After writing all 5 files, verify with:
    ```bash
    ls .planning/phases/02-email-capture-funding-infrastructure/drafts/
    wc -l .planning/phases/02-email-capture-funding-infrastructure/drafts/*.md
    grep -c "Spatial Media Library" .planning/phases/02-email-capture-funding-infrastructure/drafts/*.md
    grep -c "Spatial Media Lab" .planning/phases/02-email-capture-funding-infrastructure/drafts/*.md
    ```

    All 5 files present; all have non-trivial line counts (100+); both brand names appear in each
    about/post file (proving the disambiguation from Pitfall 8 is enforced).
  </action>
  <verify>
    <automated>test -d .planning/phases/02-email-capture-funding-infrastructure/drafts &amp;&amp; test -f .planning/phases/02-email-capture-funding-infrastructure/drafts/patreon-page-about.md &amp;&amp; test -f .planning/phases/02-email-capture-funding-infrastructure/drafts/patreon-tier-copy.md &amp;&amp; test -f .planning/phases/02-email-capture-funding-infrastructure/drafts/patreon-post-01-welcome.md &amp;&amp; test -f .planning/phases/02-email-capture-funding-infrastructure/drafts/patreon-post-02-technical.md &amp;&amp; test -f .planning/phases/02-email-capture-funding-infrastructure/drafts/patreon-post-03-roadmap.md &amp;&amp; grep -q "Spatial Media Library" .planning/phases/02-email-capture-funding-infrastructure/drafts/patreon-page-about.md &amp;&amp; grep -q "Spatial Media Lab" .planning/phases/02-email-capture-funding-infrastructure/drafts/patreon-page-about.md &amp;&amp; grep -q "HRTF" .planning/phases/02-email-capture-funding-infrastructure/drafts/patreon-post-02-technical.md &amp;&amp; grep -q "PATRON-ONLY" .planning/phases/02-email-capture-funding-infrastructure/drafts/patreon-post-02-technical.md</automated>
  </verify>
  <acceptance_criteria>
    - Directory `.planning/phases/02-email-capture-funding-infrastructure/drafts/` exists
    - All 5 files exist with non-empty content
    - `grep -c "Spatial Media Library"` in each draft file returns >= 1
    - `grep -c "Spatial Media Lab"` in each post file returns >= 1 (disambiguation per Pitfall 8)
    - `patreon-post-02-technical.md` marked PATRON-ONLY and mentions HRTF, phase vocoder, trajectory
    - `patreon-post-01-welcome.md` and `patreon-post-03-roadmap.md` marked PUBLIC
    - `patreon-tier-copy.md` lists exactly 4 tiers ($3/$10/$25/$100) as H2/H3 section headers
    - `patreon-page-about.md` includes the Berlin one-liner and all 4 cross-links (D-26)
  </acceptance_criteria>
  <done>All Patreon copy is drafted in-repo and git-committable so the final published text can be diffed against the source-of-truth drafts.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User revises drafts for voice before Patreon-dashboard work (D-29, D-11)</name>
  <what-built>The 5 draft Markdown files from Task 1. This checkpoint is where Andrew revises the Claude-drafted copy for voice (D-29 explicit: "Claude drafts all three; user revises for voice before publishing").</what-built>
  <how-to-verify>
    Open each of the 5 draft files and edit in-place. Focus revisions on:
    - **Voice** — Claude's drafts aim for a direct, no-fluff tone. If anything sounds AI-generated, rewrite in your own voice. User-specific idioms welcome.
    - **Accuracy** — the technical post (post 2) may have details the drafter got wrong; correct any DSP claims that don't match the actual plugin (reference `agent_docs/architecture.md` for ground truth on FFT size, HRTF profile count, latency numbers, etc.)
    - **Tier benefit specificity** — the $25 "1:1 Discord or email access" and $100 "thank-you credit in About dialog" are baselines; if you'd rather pick different benefits, edit `patreon-tier-copy.md`
    - **Berlin one-liner wording** — D-25 describes it as "casual, low-urgency." Current draft meets that bar; revise if you have a better line.
    - **Omissions** — if anything in the drafts shouldn't go public (real names, contact info beyond what's already in the privacy policy), remove it.

    Commit revisions before proceeding:
    ```bash
    git add .planning/phases/02-email-capture-funding-infrastructure/drafts/
    git commit -m "docs(02): revise Patreon drafts for voice"
    ```
  </how-to-verify>
  <resume-signal>Reply "drafts revised, ready to publish" OR "no changes needed, drafts ready." If significant changes were made, note which files.</resume-signal>
  <files>N/A — checkpoint task (no file changes authored by Claude; user performs dashboard/decision work per resume-signal)</files>
  <action>Checkpoint task. The meaningful content is in the checkpoint-specific elements above (decision/context/options for decision checkpoints; what-built/how-to-verify/resume-signal for verify/action checkpoints). Claude MUST pause here and await the user response described in &lt;resume-signal&gt; before continuing the plan.</action>
  <verify>
    <automated>MISSING — checkpoint task; verification is the user's manual response per the &lt;how-to-verify&gt; or &lt;options&gt; block above. Executor records the user's resume-signal text as the verification artifact.</automated>
  </verify>
  <done>User has responded per the &lt;resume-signal&gt; above. Executor has recorded the response verbatim in the plan summary (for audit trail on decision checkpoints) or as evidence path (for verify checkpoints with screenshots).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Create Patreon creator account, vanity URL, tiers, About section (copy-paste from drafts)</name>
  <what-built>nothing in-repo — Patreon dashboard creation is entirely human work. Patreon has a REST API but not for creator-account or tier creation; those are dashboard-only.</what-built>
  <how-to-verify>
    **Step A — Create creator account:**
    1. Visit `patreon.com/signup/creator` → sign up with your personal email (NOT the SML org email) — per D-21 the identity is "Andrew Rahman (personal)"
    2. Set creator name = `Andrew Rahman`
    3. Set "What are you creating?" = paste the tagline from `drafts/patreon-page-about.md` (Page Tagline section): `Funding the Spatial Media Library — spatial audio tools for musicians and sound designers`

    **Step B — Claim vanity URL:**
    4. Page Settings → URL → set to `andrewrahman` (gives `patreon.com/andrewrahman`)
    5. **VERIFY AVAILABILITY.** If taken, try `andrew-rahman` or `andrewjrahman` as fallbacks; note whichever is used in the plan summary.
    6. Screenshot the URL settings page → `docs/phase-02-evidence/patreon-vanity-url.png`

    **Step C — Choose billing plan:**
    7. Patreon creator → Payouts → ensure "Standard" plan (10% fee) — this is the default for new creators post-2025-08-04

    **Step D — Configure 4 tiers (copy from `drafts/patreon-tier-copy.md`):**
    8. Membership → Tiers → "Create new tier" × 4:
       - Tier 1: Name = `Stargazer`, Price = `$3/mo`, paste benefits verbatim from draft
       - Tier 2: Name = `Astronaut`, Price = `$10/mo`, paste benefits
       - Tier 3: Name = `Commander`, Price = `$25/mo`, paste benefits
       - Tier 4: Name = `Mission Control`, Price = `$100/mo`, paste benefits
    9. **DO NOT enable annual billing** on any tier. Per RESEARCH.md Pitfall 4, the option won't work (requires 3+ months live + $200+/mo). Trying to enable will produce an error or silently no-op. Only enable monthly at launch. The "annual coming later" line in the About section sets user expectation.
    10. Screenshot the Tiers page showing all 4 tiers → `docs/phase-02-evidence/patreon-tiers.png`

    **Step E — Populate About section:**
    11. Page Settings → About → paste the About Section copy from `drafts/patreon-page-about.md`
    12. Add a Source Code Callout (either inline at the end of About, or as a dedicated link block if Patreon provides one) — paste the "Source Code Callout" from draft
    13. Add the Future Plugins Soft-Mention and Annual Billing Soft-Mention inline — these soften user expectations
    14. Add the Berlin One-Liner in the **sidebar or footer** area if Patreon allows a page-footer / about-side text (per D-25 placement)

    **Step F — Add cross-links (D-26):**
    15. Patreon's page has a "Links" section or "Social" links. Add all 4:
        - github.com/Spatial-Media-Lab/OpenSpatialDelay
        - spatialmedialab.org
        - andrewrahman.com
        - andrewrahman.com/privacy
    16. If Patreon only supports 3 social link slots, choose the first 3; add the privacy link inline in the About text as a plain hyperlink so it's still reachable.
  </how-to-verify>
  <resume-signal>Reply "page configured, not yet launched" + note any vanity-URL fallback used + confirm screenshots for vanity-url.png and tiers.png exist.</resume-signal>
  <files>N/A — checkpoint task (no file changes authored by Claude; user performs dashboard/decision work per resume-signal)</files>
  <action>Checkpoint task. The meaningful content is in the checkpoint-specific elements above (decision/context/options for decision checkpoints; what-built/how-to-verify/resume-signal for verify/action checkpoints). Claude MUST pause here and await the user response described in &lt;resume-signal&gt; before continuing the plan.</action>
  <verify>
    <automated>MISSING — checkpoint task; verification is the user's manual response per the &lt;how-to-verify&gt; or &lt;options&gt; block above. Executor records the user's resume-signal text as the verification artifact.</automated>
  </verify>
  <done>User has responded per the &lt;resume-signal&gt; above. Executor has recorded the response verbatim in the plan summary (for audit trail on decision checkpoints) or as evidence path (for verify checkpoints with screenshots).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Draft + upload 3 seed posts with correct visibility, then LAUNCH the page</name>
  <what-built>nothing in-repo — post creation and page launch are Patreon dashboard actions.</what-built>
  <how-to-verify>
    **Step A — Upload posts (in publish order):**
    1. Patreon → Posts → New post → title = `Welcome to the Spatial Media Library Patreon`, body = paste from `drafts/patreon-post-01-welcome.md` (below the frontmatter — everything after the `---` separator). Visibility = **Public**. Save as draft.
    2. New post → title = `Inside OpenSpatialDelay: HRTF, Phase Vocoder, and 3D Trajectory`, body = paste from `drafts/patreon-post-02-technical.md` (below frontmatter). Visibility = **Paid members only ($3+ Stargazer tier and above)**. Public teaser = paste the "Public teaser" line from the draft so the paywall shows a summary, not a blank card. Save as draft.
    3. New post → title = `Where the Spatial Media Library Is Going`, body = paste from `drafts/patreon-post-03-roadmap.md`. Visibility = **Public**. Save as draft.

    **Step B — Review drafts:**
    4. In preview mode, walk through the creator view of each post. Confirm:
       - Markdown rendering is correct (bold, links, bullet lists)
       - Public teaser on post 2 appears correctly in the locked preview
       - No draft artifacts left in (e.g. the `**Visibility:** PATRON-ONLY` meta-line from the draft's top — that's plan-file frontmatter, NOT body copy; paste starts AFTER the `---`)

    **Step C — Publish posts:**
    5. Publish post 1 (Welcome) — should publish immediately (page not yet launched so public visitors can't see it yet; visibility bits are just stored state until launch)
    6. Publish post 2 (Technical)
    7. Publish post 3 (Roadmap)

    **Step D — LAUNCH THE PAGE (CRITICAL — RESEARCH.md Pitfall 3):**
    8. Patreon → Page settings → Launch checklist → verify each pre-flight item is checked
    9. Click **"Launch page"** (this is the step that makes `patreon.com/andrewrahman` publicly visible to logged-out visitors; without it, public visitors see a "not yet launched" message regardless of how many posts exist)
    10. Screenshot the "Page launched" confirmation → `docs/phase-02-evidence/patreon-launch-confirmation.png`

    **Step E — Verify in logged-out browser:**
    11. Open a fresh incognito window (no Patreon cookies). Visit `patreon.com/andrewrahman`.
    12. Confirm:
        - ✅ Page loads (not "coming soon")
        - ✅ Tagline visible: "Funding the Spatial Media Library — spatial audio tools for musicians and sound designers"
        - ✅ About section visible with Spatial Media Library / Spatial Media Lab distinct references
        - ✅ 4 tiers visible ($3/$10/$25/$100) — NO annual option visible
        - ✅ Posts section shows 2 public posts (Welcome + Roadmap) and 1 locked post (Technical — with public teaser visible and a "Join to unlock" prompt)
        - ✅ Links to github.com/Spatial-Media-Lab, spatialmedialab.org, andrewrahman.com all visible and clickable
        - ✅ (If Patreon renders the Berlin one-liner in sidebar/footer) Berlin one-liner visible
    13. Screenshot the logged-out page view → `docs/phase-02-evidence/patreon-page-live.png`
    14. Read the visible About + post 1 text from end to end once. Confirm the reader can answer: "what is this Patreon for?" → answer should be "funding the Spatial Media Library pipeline of spatial audio tools, of which OpenSpatialDelay is the first." If the answer is ambiguous, revise the About copy and redeploy.

    **Step F — DIST-03 satisfaction check:**
    15. DIST-03 requires: (a) page published, (b) 2+ posts, (c) "spatial audio tools pipeline, not just one plugin" framing visible.
        - (a) ✅ if step 12 passed
        - (b) ✅ 2 public posts + 1 patron-only = 3 total posts
        - (c) ✅ if step 14 answer comes out clean
  </how-to-verify>
  <resume-signal>
    Reply "Patreon launched" + paste the output of:
    ```bash
    curl -sI https://patreon.com/andrewrahman | head -1    # expect: HTTP/2 200 or 301 (Patreon sometimes 301s to canonical)
    ```
    + confirm all 3 screenshots (vanity-url, tiers, launch-confirmation, page-live) exist in docs/phase-02-evidence/.
    + confirm the "what is this Patreon for?" reader test passes.
  </resume-signal>
  <files>N/A — checkpoint task (no file changes authored by Claude; user performs dashboard/decision work per resume-signal)</files>
  <action>Checkpoint task. The meaningful content is in the checkpoint-specific elements above (decision/context/options for decision checkpoints; what-built/how-to-verify/resume-signal for verify/action checkpoints). Claude MUST pause here and await the user response described in &lt;resume-signal&gt; before continuing the plan.</action>
  <verify>
    <automated>MISSING — checkpoint task; verification is the user's manual response per the &lt;how-to-verify&gt; or &lt;options&gt; block above. Executor records the user's resume-signal text as the verification artifact.</automated>
  </verify>
  <done>User has responded per the &lt;resume-signal&gt; above. Executor has recorded the response verbatim in the plan summary (for audit trail on decision checkpoints) or as evidence path (for verify checkpoints with screenshots).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Patreon creator dashboard → public page | Creator edits flow from dashboard to public page; compromise of creator credentials = full page takeover. |
| Patron PII (email, payment) → Patreon backend | Patreon handles all payment + tax + PII; we don't store any patron data ourselves. |
| Patron comments on posts → public view | User-generated content on posts could contain malicious links or spam; Patreon's moderation is our only defense. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-25 | S (Spoofing) | Fake Patreon impersonation page | mitigate | Claim the `andrewrahman` vanity URL in Task 3 Step B. First-come-first-served — if a squatter holds it, use the documented fallback. Link to `patreon.com/andrewrahman` from andrewrahman.com and from the Tally thank-you page so the official URL is anchored in multiple places. |
| T-02-26 | E (Elevation of Privilege) | Creator account compromise | mitigate | Document to user: enable 2FA on Patreon creator account before launch. Out-of-scope for this plan's verification but called out in Task 3 resume-signal. |
| T-02-27 | I (Information Disclosure) | Patron-only post 2 visibility leak | accept | Patreon's paywall is Patreon's responsibility. If it leaks, it's a Patreon bug, not ours. Our mitigation is to not put genuinely sensitive info in post 2 — it's a DSP overview, not secrets; worst case of a leak is free access to technical content that has no monetary value standalone. |
| T-02-28 | T (Tampering) | Draft-to-published divergence (what's live differs from what's in-repo) | mitigate | In-repo drafts in `drafts/` directory are the canonical source. Post-launch, a periodic manual diff of the live Patreon page against the draft files catches divergence. Add to the Phase 2 UAT checklist. |
| T-02-29 | R (Repudiation) | No audit trail of what was published when | mitigate | Task 2 commits revised drafts with a clear message before publication. The git log gives a tamper-evident record of what was (re-)drafted and when. Patreon's own post timestamps give a matching record on the publication side. |
| T-02-30 | I (Information Disclosure) | Berlin one-liner links SML to a specific city | accept | D-25 explicitly put this in scope. Public information about SML's operational base is already discoverable via SpatialMediaLab.org. Not a new disclosure. |

**No high-severity STRIDE threats introduced by this plan** — Patreon handles all payment + PII flows under their own security model; our surface is copy + configuration. Residual risk is in account-security (T-02-26) which is out-of-scope for this plan but surfaced to the user.
</threat_model>

<verification>
- 5 draft files in `.planning/phases/02-email-capture-funding-infrastructure/drafts/`, all committed to OSD repo
- Patreon page `patreon.com/andrewrahman` (or documented fallback) returns HTTP 200 in incognito
- Page shows: tagline, About, 4 tiers (no annual), 3 posts (2 public + 1 patron-only with teaser), cross-links (D-26), Berlin one-liner
- Screenshots stored in `docs/phase-02-evidence/`:
  - patreon-vanity-url.png
  - patreon-tiers.png
  - patreon-launch-confirmation.png
  - patreon-page-live.png
- Manual reader test: "what is this Patreon for?" answered clearly with pipeline framing
- DIST-03 satisfied (published, 2+ posts, pipeline framing)
</verification>

<success_criteria>
- [ ] 5 draft Markdown files created and committed
- [ ] User revision pass complete (Task 2)
- [ ] Patreon account created, vanity URL claimed, Standard plan chosen
- [ ] 4 tiers configured exactly as UI-SPEC (monthly only, NO annual)
- [ ] About / tagline / source-code callout / Berlin one-liner / cross-links populated
- [ ] 3 seed posts published with correct visibility (public/patron/public)
- [ ] Page LAUNCHED (Pitfall 3 mitigation)
- [ ] Incognito verification: page visible + 2 public posts visible + pipeline framing clear
- [ ] DIST-03 satisfaction confirmed
</success_criteria>

<output>
After completion, create `.planning/phases/02-email-capture-funding-infrastructure/02-06-SUMMARY.md` capturing:
- Final Patreon vanity URL (andrewrahman or documented fallback)
- Confirmation all 4 tiers configured; screenshot paths
- Confirmation 3 posts published with correct visibility
- Confirmation page is LAUNCHED (not in draft mode)
- Link to the incognito-verified URL
- Any revisions made during Task 2 that diverge from the Claude-drafted copy (noting the updated draft files are the new source of truth)
- Confirmation that DIST-03 is satisfied and the Tally thank-you page's Patreon link (Plan 05 Task 1 Step E.21) now points at a live, launched page
- **D-23 partial delivery:** Annual billing tier deferred — Patreon enforces 3-month + $200/mo eligibility window (RESEARCH.md Pitfall 4). Monthly tiers ($3/$10/$25/$100) launch in full; annual re-enables post-eligibility via Phase 5 revisit (D-30).
</output>
