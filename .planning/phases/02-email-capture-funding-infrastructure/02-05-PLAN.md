---
phase: 02-email-capture-funding-infrastructure
plan: 05
type: execute
wave: 3
depends_on: ["03", "04"]
files_modified:
  - site/app/get-osd/page.tsx
  - site/app/privacy/page.tsx
  - site/tests/sender-embed.spec.ts
  - site/_headers
autonomous: false
requirements: [DIST-01]
supersedes:
  - "Original 02-05-PLAN.md (Tally-based). Retired 2026-04-16 per STATE.md decision: Sender.net replaces Tally."
user_setup:
  - service: sender-net
    why: "Required for DIST-01 — hosts the email-capture form AND sends the double-opt-in confirmation email that contains the download buttons. Sender.net free-forever plan (2,500 subscribers, 15,000 emails/month) covers launch scale; EU-based processor (Lithuania) keeps the privacy-policy story clean."
    dashboard_config:
      - task: "Create Sender.net account (free plan) at https://www.sender.net — use the same email you'll list as the controller in the privacy policy"
        location: "sender.net/signup"
      - task: "Create two groups: 'osd-unconfirmed' (DOI holding group) and 'osd-confirmed' (the real newsletter list)"
        location: "Subscribers → Groups → New group"
      - task: "Create an embeddable signup form with a single email field, submit button 'Get the download', add hint text below the field linking to the privacy policy"
        location: "Forms → New form → Embedded form"
      - task: "Point form submissions at the 'osd-unconfirmed' group"
        location: "Form settings → Target group"
      - task: "Build the DOI automation: trigger = 'Subscriber added to osd-unconfirmed' → email (with {$double-optin-link} confirm button PLUS macOS/Windows download buttons PLUS soft Patreon CTA) → condition 'link clicked' → action 'move subscriber to osd-confirmed'"
        location: "Automation → New workflow"
      - task: "Publish the form, copy the FORM_ID from the embed snippet (second script includes data-sender-form-id=\"{FORM_ID}\")"
        location: "Form → Publish → Embed code"
      - task: "Set Netlify environment variable NEXT_PUBLIC_SENDER_FORM_ID = {FORM_ID}; REMOVE the old NEXT_PUBLIC_TALLY_FORM_ID if it was ever set"
        location: "Netlify site Settings → Environment variables"
      - task: "Trigger Netlify 'Clear cache and deploy site' so the new env var is inlined into the static build"
        location: "Netlify site → Deploys"
must_haves:
  truths:
    - "A Sender.net account exists under Andrew's control; Free Forever plan active; billing card NOT required (confirmed on Sender's pricing page)"
    - "Two groups exist: osd-unconfirmed (holding group for DOI) and osd-confirmed (the real newsletter list)"
    - "An embedded signup form with a single required email field exists; submit writes to osd-unconfirmed group"
    - "Form hint text (below the email field) reads verbatim: 'By subscribing, you agree to receive occasional updates about OpenSpatialDelay and other Spatial Media Library tools. See our [Privacy policy](https://andrewrahman.com/privacy).'"
    - "A DOI automation is live: trigger 'subscriber added to osd-unconfirmed' → send email 'Confirm your subscription and download OpenSpatialDelay' → condition 'link clicked on {$double-optin-link}' → action 'move subscriber to osd-confirmed'"
    - "The DOI confirmation email contains THREE blocks in order: (a) the confirm button linking to {$double-optin-link}, (b) download buttons for macOS (.pkg) and Windows (.exe) linking to versioned GitHub Release asset URLs for v1.0.0 (NOT releases/latest), (c) soft Patreon CTA linking to patreon.com/AndrewRahman"
    - "Sender.net account Data Region = EU (verified by emailing Sender support if the setting isn't user-visible in the dashboard — Lithuania-based company but hosting region worth confirming for privacy policy accuracy)"
    - "The FORM_ID has been set as NEXT_PUBLIC_SENDER_FORM_ID in Netlify env vars; the old NEXT_PUBLIC_TALLY_FORM_ID (if present) has been deleted; the production build has been redeployed with 'Clear cache and deploy site'"
    - "The production page https://andrewrahman.com/get-osd/ renders the live Sender.net form (not the 'Sender form ID not yet configured' placeholder)"
    - "The production privacy page https://andrewrahman.com/privacy/ names Sender.net as the processor (not Tally), references Vilnius, Lithuania, and carries an updated effective date per the Section 9 commitment"
    - "End-to-end manual UAT: incognito submit with a real email → DOI email arrives within 60s → email contains confirm button + macOS download + Windows download + Patreon CTA → clicking confirm moves subscriber to osd-confirmed group in Sender dashboard → clicking a download button downloads the v1.0.0 installer"
  artifacts:
    - path: "<SITE_ROOT>/app/get-osd/page.tsx"
      provides: "Rewritten to embed Sender.net signup form via two-script pattern; env var renamed NEXT_PUBLIC_SENDER_FORM_ID; placeholder card text updated; noscript fallback points at Sender's hosted form URL"
      contains: "NEXT_PUBLIC_SENDER_FORM_ID"
    - path: "<SITE_ROOT>/app/privacy/page.tsx"
      provides: "Section on third-party processors updated: 'Tally' replaced with 'Sender.net (UAB Sender.lt, Vilnius, Lithuania)'; SCC language (if any) removed since EU-to-EU transfer; effective date bumped per Section 9 commitment"
    - path: "<SITE_ROOT>/tests/sender-embed.spec.ts"
      provides: "Playwright spec (replaces the reverted tally-embed.spec.ts) validating (a) Sender form container renders with data-sender-form-id, (b) placeholder card is gone when env var is set, (c) noscript fallback links to Sender-hosted form URL"
    - path: "<SITE_ROOT>/_headers"
      provides: "Netlify CSP updated: removed tally.so + widgets.tally.so; added cdn.sender.net (script) and any Sender-specific domains the embed snippet uses"
    - path: "docs/phase-02-evidence/sender-form-config.png"
      provides: "Screenshot of Sender form editor showing email field + hint text"
    - path: "docs/phase-02-evidence/sender-doi-automation.png"
      provides: "Screenshot of the DOI automation workflow showing trigger → email → condition → action"
    - path: "docs/phase-02-evidence/sender-confirmation-email.png"
      provides: "Screenshot of the rendered DOI email (viewed in a real mail client) showing confirm button, both download buttons, Patreon CTA"
    - path: "docs/phase-02-evidence/sender-live-submission.png"
      provides: "Screenshot of incognito form submission showing the Sender 'check your email' success state"
  key_links:
    - from: "Sender.net form submission"
      to: "Sender.net backend → osd-unconfirmed group"
      via: "Sender's hosted POST endpoint (JS embed handles the POST)"
      pattern: "cdn.sender.net form-submit flow"
    - from: "osd-unconfirmed group membership"
      to: "DOI automation"
      via: "Sender automation trigger: 'subscriber added to group'"
      pattern: "Sender automation workflow"
    - from: "DOI email 'confirm' click"
      to: "Sender.net → moves subscriber to osd-confirmed"
      via: "{$double-optin-link} mergetag resolves to a per-subscriber tracked URL"
      pattern: "sender.net/confirm/{token}"
    - from: "DOI email download buttons"
      to: "github.com/Spatial-Media-Lab/OpenSpatialDelay/releases/download/v1.0.0/{asset}"
      via: "HTTP direct link, opens in new tab from the email client"
      pattern: "releases/download/v1.0.0"
    - from: "andrewrahman.com/get-osd"
      to: "cdn.sender.net form bundle"
      via: "Two-script pattern embedded in page.tsx useEffect"
      pattern: "cdn.sender.net/*"
---

<objective>
Switch Phase 2's email-capture stack from Tally to Sender.net. Build the form, DOI automation, and confirmation-email template in Sender.net's dashboard; rewire the Next.js site to embed the Sender form instead of the Tally iframe; update the privacy policy to name Sender.net as the processor; verify the full flow end-to-end from a live incognito submission.

Purpose: Deliver DIST-01 — an email-gated download where a single click in the confirmation email both confirms the subscription AND delivers the download links (Option A chosen 2026-04-16). This replaces the original Tally-based plan, which could capture emails but could not send newsletters or deliver downloads via email.

Output: A live Sender.net form at andrewrahman.com/get-osd that captures email, sends a double-opt-in email containing both the confirm button and the macOS/Windows download buttons inline, and promotes subscribers to the real newsletter list when they click confirm.

Wave 3 — runs after Plan 03 (privacy URL live) and Plan 04 (andrewrahman.com live on Netlify with configurable env vars).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-email-capture-funding-infrastructure/02-CONTEXT.md
@.planning/phases/02-email-capture-funding-infrastructure/02-UI-SPEC.md
@.planning/phases/02-email-capture-funding-infrastructure/02-RESEARCH.md
@.planning/phases/02-email-capture-funding-infrastructure/02-04-SUMMARY.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking" status="complete" completed_at="2026-04-16">
  <name>Task 1: Create the Sender.net account, form, groups, and DOI automation — COMPLETE 2026-04-16</name>
  <what-built>nothing in the repo — Sender.net form/automation creation is dashboard-driven on the free plan</what-built>
  <how-to-verify>
    **Step A — Account + groups:**
    1. Sign up at https://www.sender.net with the email you'll list as the controller in the privacy policy (same address used as the Andrew Rahman controller contact per D-16).
    2. Decline any paid-plan upsell; confirm the sidebar shows "Free Forever" and no credit card is required.
    3. Subscribers → Groups → New group. Create **two** groups: `osd-unconfirmed` and `osd-confirmed`. Descriptions can be "DOI holding group" and "Confirmed OSD newsletter subscribers".

    **Step B — Signup form:**
    4. Forms → New form → choose "Embedded form" template.
    5. Form title: `Get OpenSpatialDelay`.
    6. Field configuration: ONE field, type Email, label `Email address`, placeholder `you@example.com`, required = ON.
    7. Below the email field add a "Text block" with this EXACT text (Markdown link will render as a clickable link in Sender's form builder):

        ```
        By subscribing, you agree to receive occasional updates about OpenSpatialDelay and other Spatial Media Library tools. See our [Privacy policy](https://andrewrahman.com/privacy).
        ```

    8. Submit button label: `Get the download`.
    9. Form settings → Target group: `osd-unconfirmed`.
    10. Form settings → After-submit view: default success message is fine ("Check your email to confirm your subscription"). Confirm this shows in the preview.
    11. Save and publish. Copy the embed snippet (there will be two `<script>` blocks — one is the runtime loader, one contains `data-sender-form-id="..."`). Save the FORM_ID — Task 3 needs it.

    **Step C — DOI automation (the core of Option A):**
    12. Automation → New workflow. Name: `OSD DOI — confirm + deliver download`.
    13. Trigger: `Subscriber added to group` → select `osd-unconfirmed`.
    14. Add step: `Send email`. Configure:
        - Subject: `Confirm your subscription and download OpenSpatialDelay`
        - Preheader: `One click to confirm and grab your installer.`
        - From name: `Andrew Rahman`
        - From address: the controller email from Step A.1
        - Body (use Sender's block editor; paste each block verbatim):

          **Heading block:** `You're one click away from OpenSpatialDelay.`

          **Paragraph block:** `Thanks for signing up. Click below to confirm your subscription, then grab your download for macOS or Windows.`

          **Button block #1 (confirm):**
          - Label: `Confirm my subscription`
          - URL: `{$double-optin-link}` ← literal mergetag, Sender resolves it per-subscriber
          - Style: primary / brand color

          **Paragraph block:** `Your downloads:`

          **Button block #2 (macOS):**
          - Label: `Download for macOS`
          - URL: `https://github.com/Spatial-Media-Lab/OpenSpatialDelay/releases/download/v1.0.0/OpenSpatialDelay-macOS.pkg`
            (verify the real filename from the v1.0.0 release page; use .dmg if that's the actual asset)
          - Style: secondary

          **Button block #3 (Windows):**
          - Label: `Download for Windows`
          - URL: `https://github.com/Spatial-Media-Lab/OpenSpatialDelay/releases/download/v1.0.0/OpenSpatialDelay-Windows.exe`
            (verify similarly)
          - Style: secondary

          **Paragraph block (Patreon CTA):** `If you find OpenSpatialDelay useful, you can support the Spatial Media Library pipeline on [Patreon](https://patreon.com/AndrewRahman). Every tier helps keep these tools free and open-source.`

    15. Add step: `Wait` → 1 minute (gives the subscriber time to click without racing the condition check).
    16. Add step: `Condition` → `If link {$double-optin-link} was clicked in the previous email`.
    17. On "Yes" branch: `Action` → `Move subscriber to group` → `osd-confirmed`.
    18. On "No" branch: leave empty (subscriber stays in osd-unconfirmed; optional future cleanup automation can delete stale holders).
    19. Save automation and toggle it to ACTIVE.

    **Step D — Data region check:**
    20. Account settings → look for "Data region" or "Data processing region". If not visible, email Sender support: "For a GDPR privacy policy, I need to document where personal data is processed. What region/country hosts subscriber data for my account?" — record the answer; it goes into the privacy policy processor section.

    **Step E — Self-test:**
    21. Use the published form's share URL OR temporarily paste the embed snippet into a local HTML file and open it. Submit a real email (your own). Confirm:
        - Form shows success state ("Check your email to confirm your subscription")
        - Subscriber appears in `osd-unconfirmed` group in Sender dashboard
        - DOI email arrives within 60 seconds in your inbox
        - Email contains: confirm button + macOS button + Windows button + Patreon CTA (in that order)
        - Click confirm → subscriber moves to `osd-confirmed` in Sender dashboard
        - Click macOS button → downloads the v1.0.0 .pkg (or .dmg)
        - Click Windows button → downloads the v1.0.0 .exe
    22. Screenshots required:
        - `docs/phase-02-evidence/sender-form-config.png` (form editor)
        - `docs/phase-02-evidence/sender-doi-automation.png` (automation workflow)
        - `docs/phase-02-evidence/sender-confirmation-email.png` (rendered email in mail client)
        - `docs/phase-02-evidence/sender-live-submission.png` (form success state)
  </how-to-verify>
  <resume-signal>
    Reply with:
    1. `FORM_ID = {the-id-from-data-sender-form-id}`
    2. Exact macOS + Windows asset URLs used in the email buttons (so Task 2 can reference them)
    3. Sender.net data region (from Step D) — e.g. "EU (Lithuania)" or "awaiting reply from Sender support"
    4. Confirmation all 4 screenshots are in place
    5. Confirmation the self-test (Step E) succeeded end-to-end including the subscriber appearing in osd-confirmed after clicking confirm
  </resume-signal>
  <files>N/A — checkpoint task (no file changes authored by Claude).</files>
  <action>Checkpoint task. Claude MUST pause here and await the user response described in &lt;resume-signal&gt; before continuing.</action>
  <verify>
    <automated>MISSING — checkpoint task; verification is the user's resume-signal response.</automated>
  </verify>
  <done>User has responded per the &lt;resume-signal&gt;. Executor records the FORM_ID, asset URLs, and data region verbatim in the plan summary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update site repo — page.tsx, privacy page, CSP, Playwright test</name>
  <files>
    - &lt;SITE_ROOT&gt;/app/get-osd/page.tsx
    - &lt;SITE_ROOT&gt;/app/privacy/page.tsx
    - &lt;SITE_ROOT&gt;/_headers
    - &lt;SITE_ROOT&gt;/tests/sender-embed.spec.ts
  </files>
  <read_first>
    - &lt;SITE_ROOT&gt;/app/get-osd/page.tsx (current Tally iframe implementation — rewrite entirely)
    - &lt;SITE_ROOT&gt;/app/privacy/page.tsx (current processor text — find every Tally reference and swap to Sender.net)
    - &lt;SITE_ROOT&gt;/_headers (current CSP with tally.so + widgets.tally.so — swap for Sender's domains)
    - &lt;SITE_ROOT&gt;/tests/smoke.spec.ts (the get-osd placeholder test still uses 'Sender form ID not yet configured' text — update it to match the new placeholder copy)
    - The Sender embed snippet captured in Task 1 Step B.11 (two scripts; the second has `data-sender-form-id`)
  </read_first>
  <behavior>
    - `app/get-osd/page.tsx` reads `process.env.NEXT_PUBLIC_SENDER_FORM_ID`. When unset (or literal `REPLACE_ME`), renders a placeholder card with the same pattern as the Tally version. When set, renders a `<div class="sender-form-field" data-sender-form-id={FORM_ID}>` container AND injects the Sender loader script via a useEffect (two-script pattern captured in Task 1).
    - `noscript` fallback links to the Sender-hosted form URL (the form's public share URL from Step B.11 — format varies; capture it in Task 1 as well if not already).
    - `app/privacy/page.tsx` replaces every "Tally" reference with "Sender.net". The processor entry reads "Sender.net (UAB Sender.lt, Lvivo st. 25, Vilnius, Lithuania)". Data-region sentence updates based on Task 1 Step D result. Effective date bumps to today (per Section 9 commitment on material changes).
    - `_headers` CSP `script-src` drops `https://tally.so https://widgets.tally.so`; adds `https://cdn.sender.net https://*.sender.net` (wildcard covers whatever domain the two-script embed uses). `frame-src` similarly updated if Sender uses an iframe anywhere.
    - New spec file `tests/sender-embed.spec.ts` mirrors the shape of the reverted tally spec: 3 tests gated on `NEXT_PUBLIC_SENDER_FORM_ID=test_form_123` being set at build time. Placeholder smoke test in `tests/smoke.spec.ts` also updated to match the new placeholder text.
  </behavior>
  <action>
    1. Rewrite `app/get-osd/page.tsx` to read `NEXT_PUBLIC_SENDER_FORM_ID` and render the Sender two-script embed when set, placeholder card when unset. Placeholder text: `"Sender.net form ID not yet configured. Set NEXT_PUBLIC_SENDER_FORM_ID in the deploy environment."`. Inject the loader script via useEffect (clean up on unmount as in the Tally version).
    2. Update `app/privacy/page.tsx`: swap every "Tally" → "Sender.net", processor name → "Sender.net (UAB Sender.lt, Lvivo st. 25, Vilnius, Lithuania)", data-region sentence per Task 1 result, effective date → today.
    3. Update `_headers`: CSP script-src/frame-src/connect-src — remove Tally domains, add Sender domains (`cdn.sender.net` + wildcard `*.sender.net` for the runtime loader and any analytics beacons).
    4. Create `tests/sender-embed.spec.ts` with 3 tests:
       - `renders Sender form container with correct data-sender-form-id when FORM_ID is set`
       - `placeholder card is NOT visible when FORM_ID is set`
       - `noscript fallback links to the Sender-hosted form URL`
       Use the same `test.skip` pattern as the reverted tally version: describe-level skip when `process.env.NEXT_PUBLIC_SENDER_FORM_ID !== 'test_form_123'`.
    5. Update the existing `tests/smoke.spec.ts` line that asserts "Tally form ID not yet configured" text → change to "Sender.net form ID not yet configured".
    6. Update the existing privacy-content.spec.ts test `names Tally as a data processor` → rename to `names Sender.net as a data processor` and update the assertion.
    7. Run the verification commands (below). All pass.
    8. Commit the site repo as ONE atomic commit: `feat(site): switch email-capture from Tally to Sender.net`. Include body explaining the four file changes + why (reference STATE.md decision of 2026-04-16).
  </action>
  <verify>
    <automated>cd &lt;SITE_ROOT&gt; &amp;&amp; npm run build &amp;&amp; npm run test &amp;&amp; NEXT_PUBLIC_SENDER_FORM_ID=test_form_123 npm run build &amp;&amp; NEXT_PUBLIC_SENDER_FORM_ID=test_form_123 npm run test -- --grep "sender embed"</automated>
  </verify>
  <acceptance_criteria>
    - Plain `npm run test` after plain `npm run build`: 17+ tests pass, 3 sender-embed tests skip (no failures)
    - Stubbed `npm run test -- --grep "sender embed"` after `NEXT_PUBLIC_SENDER_FORM_ID=test_form_123 npm run build`: 3 pass
    - `grep -r "Tally" site/app site/tests site/_headers` returns zero matches
    - Privacy page renders processor as "Sender.net (UAB Sender.lt, ..., Vilnius, Lithuania)"
    - CSP no longer references tally.so; now references sender.net
    - Single commit on andrewrahman-com main with a clear message referencing the STATE.md decision
  </acceptance_criteria>
  <done>All Tally references are gone from the site repo; the Sender.net embed path is env-var-driven and regression-tested; the privacy policy names the new processor; CSP is updated.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Set NEXT_PUBLIC_SENDER_FORM_ID in Netlify, delete old Tally env var, trigger clear-cache redeploy</name>
  <what-built>nothing code-side — Netlify env var config + deploy trigger are dashboard-only</what-built>
  <how-to-verify>
    1. Netlify → Site → Site configuration → Environment variables.
    2. If `NEXT_PUBLIC_TALLY_FORM_ID` exists, delete it.
    3. Add variable: Key `NEXT_PUBLIC_SENDER_FORM_ID`, Value = the FORM_ID from Task 1 Step B.11. Scope "All scopes" (or at minimum "Builds").
    4. Site → Deploys → Trigger deploy → **"Clear cache and deploy site"** (required — normal deploys will use the cached build without the new env var inlined).
    5. Wait ~2 min for deploy.
    6. Verify from the terminal:
       ```bash
       curl -s https://andrewrahman.com/get-osd/ | grep -q 'data-sender-form-id="{FORM_ID}"'    # replace {FORM_ID} with actual
       curl -s https://andrewrahman.com/get-osd/ | grep -q "Sender.net form ID not yet configured"    # should NOT match (exit 1)
       curl -s https://andrewrahman.com/privacy/  | grep -q "Sender.net"                           # should match
       curl -s https://andrewrahman.com/privacy/  | grep -q "Tally"                                # should NOT match (exit 1)
       ```
  </how-to-verify>
  <resume-signal>Reply "env var set, redeploy complete" + paste the output of all four curl commands above.</resume-signal>
  <files>N/A — checkpoint task.</files>
  <action>Checkpoint task. Claude MUST pause here and await user response.</action>
  <verify>
    <automated>MISSING — checkpoint task; verification is the user's resume-signal response.</automated>
  </verify>
  <done>User has responded; executor records curl outputs in plan summary.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: End-to-end UAT on production — real submission, real DOI click, real download</name>
  <what-built>The full Sender.net email-capture + DOI + download-via-email flow is live at andrewrahman.com/get-osd. This checkpoint verifies the complete flow from a real browser + real mail client.</what-built>
  <how-to-verify>
    Fresh incognito window. No Sender/Netlify/GitHub cookies.

    1. Visit `https://andrewrahman.com/get-osd/`
       - ✅ Page renders with heading "Get OpenSpatialDelay"
       - ✅ Sender form renders inline (email field + "Get the download" button + hint text with privacy policy link)
       - ✅ Browser dev console: no CSP warnings, no mixed-content warnings, no 404 from sender.net domains
    2. Try submitting without an email — form should reject (HTML5 required-field validation).
    3. Try with an invalid email — form rejects.
    4. Submit with a real email (your own, different from the Step A.1 account email — use a throwaway or a +alias).
       - ✅ Form transitions to "Check your email to confirm your subscription" success state
    5. Open Sender dashboard → Subscribers → osd-unconfirmed group
       - ✅ New subscriber row exists with the email + timestamp
       - ✅ osd-confirmed group is EMPTY (the subscriber hasn't clicked yet)
    6. Open your real mail client (not Sender's preview) within 60 seconds
       - ✅ Subject: "Confirm your subscription and download OpenSpatialDelay"
       - ✅ From name shows "Andrew Rahman"
       - ✅ Body renders: confirm button + "Your downloads:" + macOS button + Windows button + Patreon CTA paragraph
       - ✅ Sender branding footer present (free-plan trade-off — acceptable)
    7. Click the confirm button in the email
       - ✅ Browser opens Sender's confirmation page (or a redirect target)
       - ✅ Back in Sender dashboard, the subscriber has moved from osd-unconfirmed to osd-confirmed
    8. Back in the email, click `Download for macOS`
       - ✅ Downloads `OpenSpatialDelay-macOS.pkg` (or .dmg) from github.com/.../releases/download/v1.0.0/
    9. Click `Download for Windows`
       - ✅ Downloads `OpenSpatialDelay-Windows.exe` from the same release
    10. Click the Patreon link
        - ✅ Opens patreon.com/AndrewRahman (launched 2026-04-17 per Plan 02-06; HTTP/2 301 → www.patreon.com/AndrewRahman)
    11. Visit `https://andrewrahman.com/privacy/`
        - ✅ Privacy page renders
        - ✅ Processor section names "Sender.net (UAB Sender.lt, ..., Vilnius, Lithuania)" — NOT Tally
        - ✅ Effective date shows today's date (post-update)
        - ✅ Hint text link on /get-osd/ navigates to this page when clicked
    12. Save final screenshot to `docs/phase-02-evidence/sender-production-submission.png` (the success state of the form + the rendered DOI email if your mail client can screenshot it).
  </how-to-verify>
  <resume-signal>
    Reply "UAT passed" + confirmation all 12 checks green. If any fail, document which and whether it's a Sender config fix (Task 1 retry), site code fix (Task 2 retry), or Netlify env var fix (Task 3 retry).
  </resume-signal>
  <files>N/A — checkpoint task.</files>
  <action>Checkpoint task. Claude MUST pause here and await user response.</action>
  <verify>
    <automated>MISSING — checkpoint task.</automated>
  </verify>
  <done>User has responded. Plan 02-05 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User email → Sender.net backend | PII crosses from andrewrahman.com into sender.net. Sender acts as processor. Privacy policy commits to this. |
| DOI email download buttons → GitHub release | Email links to immutable v1.0.0 release asset URLs. |
| andrewrahman.com form embed → sender.net CDN | CSP gates this. Misconfiguration could allow malicious script injection via the two-script embed. |

## STRIDE Threat Register (updated for Sender.net)

| ID | Category | Component | Disposition | Mitigation |
|-----|---------|-----------|-------------|------------|
| T-02-19 | I (Info Disclosure) | Email PII collected at submission | mitigate | Privacy policy (Plan 03, updated in Task 2 of this plan) names Sender.net as processor with Vilnius, Lithuania location. Retention per D-15 (until unsubscribe). Sender is EU-based — no SCCs required. |
| T-02-20 | T (Tampering) | Consent validity | mitigate | DOI design means consent is only recorded when the subscriber clicks the confirm button in a delivered email — stronger than a checkbox on a form. The hint text below the email field provides pre-submit informed-consent notice. Superior to the Tally-era checkbox pattern. |
| T-02-21 | E (Elevation of Privilege) | Download URLs are mutable | mitigate | DOI email buttons (Task 1 Step C.14) use `releases/download/v1.0.0/` — NOT releases/latest. A future v1.0.1 won't silently change what the email delivers. |
| T-02-22 | S (Spoofing) | Attacker creates a lookalike Sender form | accept | Attacker would also need to control andrewrahman.com's env var (Netlify dashboard) to impersonate — out of scope for this plan. Sender + Netlify account security (2FA) is a user responsibility. |
| T-02-23 | D (Denial of Service) | Sender.net free-plan rate limits | accept | Free plan: 2,500 subscribers, 15,000 emails/month, no documented daily send cap. Adversarial bot floods hit Sender's anti-spam before our form. Plan 06 should consider adding a honeypot field or hCaptcha if abuse becomes real. |
| T-02-24 | I (Info Disclosure) | CSP allows untrusted script source | mitigate | _headers CSP in Task 2 restricts script-src to `cdn.sender.net + *.sender.net` (and whatever was whitelisted before for self + fonts/images). Task 4 UAT Step 1 includes a dev-console check for CSP warnings. |
| T-02-25 (NEW) | I (Info Disclosure) | DOI email contains download links that non-subscribers could forward | accept | If a confirmed subscriber forwards the DOI email to someone else, that someone else can download without subscribing. Acceptable: the plugin is free and GPL-3.0; the "gate" is soft — its purpose is list-building, not content protection. Documented in privacy policy as "email capture is for list-building, not access control." |
| T-02-26 (NEW) | I (Info Disclosure) | Unconfirmed-but-downloaded subscribers in osd-unconfirmed group | accept | A subscriber may download via the DOI email without clicking confirm, staying in osd-unconfirmed indefinitely. Acceptable under GDPR: we have the pre-submit consent notice + the DOI email is itself a lawful processing step. Optional future hardening: add a "delete after 30 days unconfirmed" cleanup automation. |

High-severity threats T-02-19 and T-02-20 remain mitigated; the DOI-via-email pattern is actually a stronger consent record than the prior Tally checkbox design.
</threat_model>

<verification>
- `NEXT_PUBLIC_SENDER_FORM_ID=test_form_123 npm run test -- --grep "sender embed"` passes (3 tests)
- `curl -s https://andrewrahman.com/get-osd/ | grep -q 'data-sender-form-id="{real_FORM_ID}"'` returns 0
- `curl -s https://andrewrahman.com/get-osd/ | grep -q "Sender.net form ID not yet configured"` returns 1 (placeholder gone on production)
- `curl -s https://andrewrahman.com/privacy/ | grep -q "Sender.net"` returns 0; `grep -q "Tally"` returns 1 (no Tally residue on production)
- Task 4 UAT: all 12 checks green
- Screenshots in `docs/phase-02-evidence/`:
  - sender-form-config.png
  - sender-doi-automation.png
  - sender-confirmation-email.png
  - sender-live-submission.png
  - sender-production-submission.png
- Site-repo commit: `feat(site): switch email-capture from Tally to Sender.net` (single atomic commit)
</verification>

<success_criteria>
- [ ] Sender.net account + osd-unconfirmed + osd-confirmed groups live
- [ ] Embedded signup form published; FORM_ID captured
- [ ] DOI automation active with confirm button + macOS/Windows downloads + Patreon CTA in the email
- [ ] Sender account data region documented (via support email if not dashboard-visible)
- [ ] Site repo updated: page.tsx, privacy page, _headers CSP, sender-embed.spec.ts, smoke test (single atomic commit)
- [ ] Netlify env var `NEXT_PUBLIC_SENDER_FORM_ID` set; old `NEXT_PUBLIC_TALLY_FORM_ID` deleted; clear-cache redeploy successful
- [ ] End-to-end UAT passed (12/12)
- [ ] 5 evidence screenshots stored
- [ ] STATE.md decision line for Sender.net remains accurate (see entry dated 2026-04-16)
</success_criteria>

<output>
After completion, create `.planning/phases/02-email-capture-funding-infrastructure/02-05-SUMMARY.md` capturing:
- The Sender.net FORM_ID value (or note it's only in Netlify env vars + dashboard)
- The exact macOS + Windows asset URLs used in the DOI email
- Sender.net data region as confirmed in Task 1 Step D
- Confirmation all 12 UAT checks passed
- Any copy adjustments that Sender's form/email editor required vs the spec above
- Confirmation that Plan 06 (Patreon) can safely cross-link to `https://andrewrahman.com/get-osd` and `https://andrewrahman.com/privacy` — both live with Sender-based stack
- Update `.planning/phases/02-email-capture-funding-infrastructure/02-VALIDATION.md` setting `nyquist_compliant: true` and `wave_0_complete: true`
- Reference the `feat(site): switch email-capture from Tally to Sender.net` commit SHA on andrewrahman-com main
</output>
