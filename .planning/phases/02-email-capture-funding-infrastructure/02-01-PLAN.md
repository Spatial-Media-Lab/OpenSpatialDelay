---
phase: 02-email-capture-funding-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [DIST-01, DIST-02, DIST-03]
must_haves:
  truths:
    - "PROJECT.md distinguishes Spatial Media Lab (org) from Spatial Media Library (pipeline) so downstream agents do not conflate them (D-01, D-03)"
    - "ROADMAP.md Phase 2 success criterion 1 reads 'required email' not 'optional email' (D-04, D-05)"
    - "REQUIREMENTS.md has removed 'Download gate / mandatory email' from the anti-features table and DIST-01 uses required-email wording (D-06)"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Canonical brand definitions: SML (org), Spatial Media Library (pipeline), OpenSpatialDelay (first product), Andrew Rahman (creator identity)"
      contains: "Spatial Media Library"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 2 success criterion 1 with required-email framing"
      contains: "required email"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated DIST-01 + anti-features table without the obsolete download-gate row"
  key_links:
    - from: ".planning/PROJECT.md"
      to: "Spatial Media Lab vs Spatial Media Library disambiguation"
      via: "explicit glossary / brand block"
      pattern: "Spatial Media Library.*pipeline"
---

<objective>
Update the three OpenSpatialDelay planning docs that are contradicted or underspecified by Phase 2 CONTEXT.md decisions, before any external-facing Phase 2 artifact is produced.

Purpose: Prevent downstream brand confusion (Pitfall 8 in RESEARCH.md) and eliminate the contradiction between CONTEXT.md D-04 (email is the gate) and the existing REQUIREMENTS.md anti-features row ("Download gate / mandatory email"). This plan has no external dependencies and runs in Wave 1 in parallel with site-scaffold and Patreon work.

Output: 3 markdown files updated and committed to the OSD repo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-email-capture-funding-infrastructure/02-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Introduce Spatial Media Library pipeline brand in PROJECT.md (D-03)</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (entire file — need to see current "Spatial Media Lab" references and pick the right insertion location)
    - .planning/phases/02-email-capture-funding-infrastructure/02-CONTEXT.md §decisions D-01, D-02, D-03 (brand definitions)
  </read_first>
  <action>
    Edit `.planning/PROJECT.md`. Two edits required:

    1. Replace the line `A spatial delay audio plugin where each echo lives in 3D space. VST3 + AU for macOS, VST3 for Windows. 7 spatialization algorithms, 5 HRTF binaural profiles, phase vocoder pitch shifting, trajectory engine, and Doppler simulation. Built with JUCE 8.0.3, dual-platform CI. Dual-licensed GPL-3.0 / Commercial (Spatial Media Lab) — transitioning to GPL-3.0 only.`
       with: `A spatial delay audio plugin where each echo lives in 3D space. VST3 + AU for macOS, VST3 for Windows. 7 spatialization algorithms, 5 HRTF binaural profiles, phase vocoder pitch shifting, trajectory engine, and Doppler simulation. Built with JUCE 8.0.3, dual-platform CI. Licensed GPL-3.0. The first product in the Spatial Media Library pipeline, developed under Spatial Media Lab.`

    2. Add a new `## Brand Identity` section immediately after the top-of-file description (before the next existing section). Use exactly this content:

       ```
       ## Brand Identity

       Three distinct names appear in Phase 2+ artifacts and must never be conflated:

       - **Spatial Media Lab (SML)** — the open-source organization that owns the source code at `github.com/Spatial-Media-Lab/OpenSpatialDelay` and the site `SpatialMediaLab.org`. Abbreviation "SML" refers to this entity only.
       - **Spatial Media Library** — the pipeline brand for the set of spatial-audio tools funded via Patreon. OpenSpatialDelay is the first. Always written as three separate words. Do NOT abbreviate.
       - **OpenSpatialDelay (OSD)** — the first plugin product in the Spatial Media Library pipeline, developed under Spatial Media Lab.
       - **Andrew Rahman** — personal creator identity used on Patreon (`patreon.com/andrewrahman`) and `andrewrahman.com`.

       Every externally-published Phase 2 artifact (Tally form copy, privacy policy, Patreon page, andrewrahman.com pages, seed posts) MUST disambiguate "Spatial Media Lab" from "Spatial Media Library" on first reference in any given document.
       ```

    Do not change any other line in PROJECT.md.
  </action>
  <verify>
    <automated>grep -q "Spatial Media Library" .planning/PROJECT.md && grep -q "Brand Identity" .planning/PROJECT.md && grep -q "Licensed GPL-3.0" .planning/PROJECT.md && ! grep -q "Dual-licensed GPL-3.0 / Commercial" .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Spatial Media Library" .planning/PROJECT.md` >= 2 (pipeline brand defined and referenced)
    - `grep -q "## Brand Identity" .planning/PROJECT.md` returns 0
    - `grep -q "Dual-licensed GPL-3.0 / Commercial" .planning/PROJECT.md` returns 1 (phrase removed)
    - `grep -q "github.com/Spatial-Media-Lab/OpenSpatialDelay" .planning/PROJECT.md` returns 0
    - The three identities (Spatial Media Lab, Spatial Media Library, OpenSpatialDelay) all appear in the new Brand Identity section as bold bullet points
  </acceptance_criteria>
  <done>PROJECT.md contains the Brand Identity section with all three entities; GPL-3.0-only language replaces the dual-license sentence.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update ROADMAP.md Phase 2 success criterion 1 to required-email wording (D-05)</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (entire file — locate the Phase 2 block)
    - .planning/phases/02-email-capture-funding-infrastructure/02-CONTEXT.md §decisions D-04, D-05
  </read_first>
  <action>
    Edit `.planning/ROADMAP.md`. In the **Phase 2: Email Capture & Funding Infrastructure** section:

    1. Replace the Phase 2 short-line in the top phase list — find the line `- [ ] **Phase 2: Email Capture & Funding Infrastructure** - Tally form live, privacy policy published, Patreon seeded and ready` — leave unchanged (it's already accurate).

    2. In the "Phase 2" detail block, replace success criterion 1 — find:
       `  1. Submitting the Tally form with optional email + GDPR consent checkbox redirects to the GitHub Releases page`
       with:
       `  1. Submitting the Tally form (required email + GDPR consent checkbox) reaches a thank-you screen with working macOS + Windows download buttons linking to the v1.0.0 GitHub Release assets`

    This rewording:
    - Changes "optional email" → "required email" (D-04/D-05)
    - Replaces "redirects to the GitHub Releases page" with the thank-you-page-with-download-buttons flow from D-09

    Do not change any other ROADMAP.md content.
  </action>
  <verify>
    <automated>grep -q "required email + GDPR consent checkbox" .planning/ROADMAP.md && grep -q "thank-you screen with working macOS + Windows download buttons" .planning/ROADMAP.md && ! grep -q "optional email + GDPR consent checkbox" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "optional email" .planning/ROADMAP.md` returns 1 (phrase removed)
    - `grep -q "required email + GDPR consent checkbox" .planning/ROADMAP.md` returns 0
    - `grep -q "thank-you screen with working macOS + Windows download buttons" .planning/ROADMAP.md` returns 0
    - `grep -q "v1.0.0 GitHub Release assets" .planning/ROADMAP.md` returns 0
    - No other Phase 2 success criteria or other phases' text have changed (diff scoped to Phase 2 criterion 1)
  </acceptance_criteria>
  <done>ROADMAP.md Phase 2 success criterion 1 reflects required-email + thank-you-page-with-downloads flow.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update REQUIREMENTS.md — remove obsolete anti-feature row, update DIST-01 wording (D-06)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (entire file — locate the Anti-Features table and DIST-01)
    - .planning/phases/02-email-capture-funding-infrastructure/02-CONTEXT.md §decisions D-04, D-06
  </read_first>
  <action>
    Edit `.planning/REQUIREMENTS.md`. Two edits required:

    1. Replace the DIST-01 line — find:
       `- [ ] **DIST-01**: Tally email capture form live — optional email, GDPR consent checkbox, redirect to GitHub Releases`
       with:
       `- [ ] **DIST-01**: Tally email capture form live — required email, GDPR consent checkbox, thank-you screen with manual macOS + Windows download buttons linking to the v1.0.0 GitHub Release assets`

    2. In the Out of Scope / Anti-Features section, delete the entire table row:
       `| Download gate / mandatory email | Destroys GPL plugin adoption; community shares direct links |`
       This row contradicts D-04 (email IS the gate now). Keep every other row in that table unchanged. The row immediately preceding and following must be preserved.

    Do not modify any other requirement IDs, the traceability table, or the v2/future-milestone section.
  </action>
  <verify>
    <automated>grep -q "DIST-01.*required email" .planning/REQUIREMENTS.md && ! grep -q "Download gate / mandatory email" .planning/REQUIREMENTS.md && ! grep -q "DIST-01.*optional email" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DIST-01" .planning/REQUIREMENTS.md` >= 2 (ID still present in both definition and traceability table)
    - `grep -q "required email, GDPR consent checkbox" .planning/REQUIREMENTS.md` returns 0
    - `grep -q "thank-you screen with manual macOS + Windows download buttons" .planning/REQUIREMENTS.md` returns 0
    - `grep -q "Download gate / mandatory email" .planning/REQUIREMENTS.md` returns 1 (row removed)
    - `grep -q "optional email" .planning/REQUIREMENTS.md` returns 1 (phrase removed)
    - `grep -q "DIST-02\|DIST-03\|REPO-01\|WEB-01\|CONT-01\|ANNC-01" .planning/REQUIREMENTS.md | wc -l` unchanged from before (other requirements preserved)
  </acceptance_criteria>
  <done>REQUIREMENTS.md DIST-01 reflects required-email framing and the contradictory anti-feature row is removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | This plan only edits planning markdown inside the repo. No user input, no external service, no data flow, no deployed surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | I (Information Disclosure) | Planning docs | accept | These files are already tracked in the public GPL repo. No new sensitive data introduced by this plan. |
| T-02-02 | T (Tampering) | Planning docs | accept | Standard git workflow + commit history provides tamper-evidence. No elevated trust required. |

**Rationale:** This is a pure markdown-edit plan with no user-facing surface, no data handling, and no third-party script inclusion. No `high`-severity STRIDE categories apply.
</threat_model>

<verification>
All three markdown edits must pass the automated grep checks in each task's `<verify>` block. Additionally, confirm:

- `git diff --name-only` shows exactly: `.planning/PROJECT.md`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`
- `git diff --stat` shows reasonable line counts (PROJECT.md +~15 lines, ROADMAP.md +0/-0 net with one-line swap, REQUIREMENTS.md -1/-0 net after row removal + DIST-01 rewrite)
</verification>

<success_criteria>
- [ ] PROJECT.md has Brand Identity section distinguishing SML / Spatial Media Library / OSD / Andrew Rahman
- [ ] PROJECT.md top description no longer says "Dual-licensed GPL-3.0 / Commercial"
- [ ] ROADMAP.md Phase 2 success criterion 1 reads "required email + GDPR consent checkbox" and references thank-you page with downloads
- [ ] REQUIREMENTS.md DIST-01 reads "required email"
- [ ] REQUIREMENTS.md anti-features table no longer has "Download gate / mandatory email" row
- [ ] All three files committed to git under a single "docs(02): align planning docs with CONTEXT.md decisions" commit
</success_criteria>

<output>
After completion, create `.planning/phases/02-email-capture-funding-infrastructure/02-01-SUMMARY.md` capturing:
- Which specific phrases were replaced
- Line counts changed per file
- Confirmation that the three edits unblock downstream Phase 2 plans (02-02, 02-04 for privacy/Tally copy; 02-05 for Patreon pipeline-pitch language)
</output>
