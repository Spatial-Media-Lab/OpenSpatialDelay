---
phase: 01-repo-license-readiness
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - README.md
autonomous: true
requirements:
  - REPO-02
  - REPO-03
  - REPO-04

must_haves:
  truths:
    - "The GitHub v1.0.0 release body no longer contains dual-license or commercial license text"
    - "README.md has an accurate system requirements table listing macOS arm64, Windows x64, VST3/AU formats, and tested DAWs"
    - "README.md has complete installation instructions including the macOS Sequoia xattr command"
  artifacts:
    - path: "README.md"
      provides: "System requirements section with tested DAWs and installation instructions"
      contains: "System requirements"
  key_links:
    - from: "GitHub release v1.0.0 body"
      to: "LICENSE"
      via: "Release body links to LICENSE file"
      pattern: "GPL-3.0"
    - from: "README.md system requirements"
      to: "README.md installation"
      via: "System requirements informs installation instructions"
      pattern: "xattr"
---

<objective>
Update the GitHub v1.0.0 release body to remove the dual-license line, and review/expand the README system requirements and installation sections.

Purpose: Per D-10, REPO-02 for Phase 1 means "release prepared with macOS arm64 binary and metadata, ready to go public." The release body still says "Dual-licensed" and must be fixed. REPO-03 and REPO-04 require accurate system requirements and installation instructions.

Output: GitHub release body updated, README system requirements reviewed and expanded per D-08, installation section verified complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-repo-license-readiness/01-CONTEXT.md
@.planning/phases/01-repo-license-readiness/01-RESEARCH.md
@.planning/phases/01-repo-license-readiness/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update GitHub v1.0.0 release body to remove dual-license text</name>
  <files>None (GitHub API resource, not a git-tracked file)</files>
  <read_first>
    - .planning/phases/01-repo-license-readiness/01-RESEARCH.md (GitHub Release Body Edit Pattern section — exact commands and new license section text)
  </read_first>
  <action>
Per D-10 and REPO-02: The v1.0.0 release is already published with macOS arm64 binary attached. The release body contains a `### License` section ending with `Dual-licensed: GPL-3.0 / Commercial (Spatial Media Lab)` — this must be updated.

Per Research pitfall 2: NEVER use `--notes "string"` — always use `--notes-file` to avoid truncation of the ~450-word body.

Steps:

1. Capture the current release body to a temp file:
```bash
gh release view v1.0.0 --repo Spatial-Media-Lab/OpenSpatialDelay --json body \
  | python3 -c "import sys,json; print(json.load(sys.stdin)['body'])" > /tmp/release_body.md
```

2. Read `/tmp/release_body.md` to see the full current body.

3. Find the `### License` section (near the end). Replace:
```
### License
Dual-licensed: GPL-3.0 / Commercial (Spatial Media Lab)
```
with:
```
### License
[GPL-3.0](https://github.com/Spatial-Media-Lab/OpenSpatialDelay/blob/main/LICENSE)
```

4. Verify the rest of the body is unchanged (scan for accidental edits).

5. Apply the updated body:
```bash
gh release edit v1.0.0 --repo Spatial-Media-Lab/OpenSpatialDelay \
  --notes-file /tmp/release_body.md
```

6. Verify the edit took effect:
```bash
gh release view v1.0.0 --repo Spatial-Media-Lab/OpenSpatialDelay --json body \
  | python3 -c "import sys,json; print(json.load(sys.stdin)['body'])" | grep -i "commercial\|dual"
```
Expected: zero output (no commercial or dual-license references remain).
  </action>
  <verify>
    <automated>gh release view v1.0.0 --repo Spatial-Media-Lab/OpenSpatialDelay --json body | python3 -c "import sys,json; b=json.load(sys.stdin)['body']; hits=[l for l in b.splitlines() if 'commercial' in l.lower() or 'dual' in l.lower()]; print(f'Matches: {len(hits)}'); [print(l) for l in hits]"</automated>
  </verify>
  <acceptance_criteria>
    - `gh release view v1.0.0 --repo Spatial-Media-Lab/OpenSpatialDelay --json body` body text contains "GPL-3.0" in the License section
    - The body does NOT contain "Dual-licensed" or "Commercial (Spatial Media Lab)" anywhere
    - The body still contains the full release notes content (not truncated — verify body length is approximately the same as before, minus the replaced license line)
    - The release still shows 2 assets: `OpenSpatialDelay-v1.0.0-macOS-arm64.zip` and `OpenSpatialDelay-v1.0.0-source.zip`
  </acceptance_criteria>
  <done>GitHub v1.0.0 release body updated from dual-license to GPL-3.0. Release assets unchanged. REPO-02 (per D-10 redefinition) complete.</done>
</task>

<task type="auto">
  <name>Task 2: Review and expand README system requirements and installation sections</name>
  <files>README.md</files>
  <read_first>
    - README.md (lines 86-95 for installation/xattr section; lines 107-114 for system requirements table)
    - .planning/phases/01-repo-license-readiness/01-RESEARCH.md (README System Requirements and README Installation sections — current state and review notes)
    - .planning/phases/01-repo-license-readiness/01-CONTEXT.md (D-07 and D-08 decisions)
  </read_first>
  <action>
Per D-07: System requirements go in README.md (not a separate file). Per D-08: List tested DAWs explicitly plus a general "should work with any VST3/AU host" note.

The README already has a system requirements section (lines 107-114) and an installation/xattr section (lines 86-95). Review both for accuracy and completeness.

**System Requirements (REPO-03):**

Review the existing table at lines 107-114. The current table says:
```
| **macOS** | Apple Silicon (arm64), macOS 12+ |
| **Windows** | x64, Windows 10+ |
| **Formats** | VST3, AU (macOS only) |
| **DAWs** | Any VST3/AU host — tested in Reaper; expected to work in Logic Pro, Ableton Live, Cubase, Bitwig, and others |
```

Per D-08, update the DAWs row to explicitly list TESTED DAWs separately from expected-compatible DAWs:
```
| **DAWs** | Tested: REAPER. Should work with any VST3/AU host (Logic Pro, Ableton Live, Cubase, Bitwig, and others) |
```

Keep macOS 12+ as the minimum (per Research assumption A1 — conservative and likely correct). Keep Windows x64, Windows 10+. Keep VST3 and AU formats.

**Installation Instructions (REPO-04):**

Review the existing `### macOS security note` section (around lines 86-95). It already contains the xattr commands. Verify:
- The `xattr -cr` command is present and correct
- The section clearly applies to macOS Sequoia (and earlier versions with Gatekeeper)
- The instructions are accurate for the ZIP-based distribution

If the section heading is `### macOS security note`, consider adding a parenthetical for clarity:
`### macOS security note (Gatekeeper / Sequoia)`
This is Claude's discretion per CONTEXT.md.

Ensure the installation section covers:
1. Download the ZIP from GitHub Releases
2. Extract to a known location
3. For macOS: run the xattr command to clear quarantine
4. Copy VST3 to `/Library/Audio/Plug-Ins/VST3/` and AU to `/Library/Audio/Plug-Ins/Components/`
5. Restart or rescan in your DAW

If any of steps 1-5 are missing, add them. If they already exist, leave them as-is.

Commit changes with message: "docs(01-02): update release body, review system requirements and installation"
  </action>
  <verify>
    <automated>cd /Users/andrewrahman/conductor/repos/openspatialdelay && echo "=== System requirements section ===" && grep -A8 "## System requirements" README.md && echo "=== xattr present ===" && grep -c "xattr" README.md && echo "=== Tested DAW listed ===" && grep -i "reaper\|tested" README.md | head -3</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains a `## System requirements` section
    - The system requirements table lists macOS Apple Silicon (arm64), macOS 12+
    - The system requirements table lists Windows x64, Windows 10+
    - The system requirements table lists VST3 and AU formats
    - The system requirements table contains "Tested" followed by "REAPER" (or "Reaper")
    - The system requirements table contains language like "should work with any VST3/AU host" per D-08
    - README.md contains `xattr -cr` or `xattr -c` command for macOS Gatekeeper
    - README.md contains a section heading with "macOS" and either "security" or "Gatekeeper" or "Sequoia"
    - The system requirements table does NOT promise compatibility with untested DAWs (per D-08)
  </acceptance_criteria>
  <done>README system requirements section reviewed and updated with explicit tested DAW list per D-08. Installation section verified to include xattr command and macOS security instructions. REPO-03 and REPO-04 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Documentation-only phase. The only non-file operation is `gh release edit` which modifies a GitHub API resource.

| Boundary | Description |
|----------|-------------|
| Local -> GitHub API | `gh release edit` modifies the release body via authenticated API call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Tampering | GitHub release body | accept | Repo is private (D-09); only authenticated collaborators can edit; `gh` uses existing auth token; risk is negligible |
| T-01-03 | Information Disclosure | Release body edit | accept | The release body is documentation text that will become public in Phase 3; no secrets involved |

No ASVS categories apply. No code changes, no user input processing, no cryptographic operations.
</threat_model>

<verification>
- `gh release view v1.0.0 --json body` contains "GPL-3.0" and does NOT contain "Dual-licensed" or "Commercial"
- README.md `## System requirements` section exists with macOS, Windows, formats, and DAW rows
- README.md DAW row mentions REAPER as tested and includes "should work with any VST3/AU host" caveat
- README.md contains `xattr` command for macOS security
</verification>

<success_criteria>
REPO-02 (per D-10): GitHub release body updated with GPL-3.0 only license text, macOS arm64 binary confirmed present.
REPO-03: System requirements section in README.md is accurate, lists tested DAWs explicitly per D-08.
REPO-04: Installation instructions include macOS Sequoia xattr command and are complete.
</success_criteria>

<output>
After completion, create `.planning/phases/01-repo-license-readiness/01-02-SUMMARY.md`
</output>
