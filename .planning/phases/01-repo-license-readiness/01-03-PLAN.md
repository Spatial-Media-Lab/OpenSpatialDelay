---
phase: 01-repo-license-readiness
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
files_modified:
  - docs/generate_manual.js
  - docs/OpenSpatialDelay_Manual_v1.0.pdf
  - docs/OpenSpatialDelay_Manual_v1.0.docx
autonomous: true
requirements:
  - REPO-02
gap_closure: true

must_haves:
  truths:
    - "The user manual contains product documentation only — no third-party license text or project license text"
    - "Legal notices live exclusively in the separate OpenSpatialDelay_Legal_Notices document (produced by generate_legal_notices.js)"
    - "The regenerated PDF and DOCX manual artifacts on disk reflect the stripped source"
  artifacts:
    - path: "docs/generate_manual.js"
      provides: "Manual generator with buildLegalNotices call removed from assembly array"
      contains: "function buildLegalNotices"
    - path: "docs/OpenSpatialDelay_Manual_v1.0.pdf"
      provides: "Regenerated PDF with no legal section"
    - path: "docs/OpenSpatialDelay_Manual_v1.0.docx"
      provides: "Regenerated DOCX with no legal section"
  key_links:
    - from: "docs/generate_manual.js assembly array"
      to: "buildLegalNotices() function"
      via: "function definition remains but is never invoked from assembly"
      pattern: "\\.\\.\\.buildLegalNotices\\(\\)"
---

<objective>
Close gap G-01 (from 01-VERIFICATION.md) by removing the `buildLegalNotices()` spread and its preceding `PageBreak` from the user manual assembly array, then regenerating the manual PDF + DOCX so binary artifacts on disk match the stripped source.

Purpose: Per REPO-02, the user manual is product docs only. Legal notices are already produced separately by `docs/generate_legal_notices.js` (covered by plan 01-04). Leaving the legal section in the manual causes double-publication and makes the manual the de-facto license carrier — which violates the separation established in phase discussion.

Output: Edited `docs/generate_manual.js` with two lines removed, regenerated `OpenSpatialDelay_Manual_v1.0.pdf` and `.docx` with legal section absent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-repo-license-readiness/01-VERIFICATION.md
@docs/generate_manual.js
</context>

<tasks>

<task id="01-03-T1" autonomous="true">
  <description>Remove the legal notices spread and its preceding PageBreak from the user manual assembly array in docs/generate_manual.js.</description>
  <read_first>
    - docs/generate_manual.js (lines 1685-1695 specifically — the assembly array section around buildLegalNotices)
  </read_first>
  <action>
    1. First, confirm current line numbers have not shifted. Run:
       `grep -n "buildLegalNotices\|PageBreak" docs/generate_manual.js | tail -20`
       Expected: line 1689 contains `new Paragraph({ children: [new PageBreak()] }),` and line 1690 contains `    ...buildLegalNotices(),`
    2. Using the Edit tool, remove EXACTLY these two consecutive lines from the assembly array (the PageBreak that precedes the spread, and the spread itself):
       ```
           new Paragraph({ children: [new PageBreak()] }),
           ...buildLegalNotices(),
       ```
       To disambiguate (there are many PageBreak lines), use a multi-line match that includes the preceding `...buildTroubleshooting(),` and following `new Paragraph({ children: [new PageBreak()] }),` as anchor context in the Edit tool's old_string. The surrounding structure is:
       ```
           ...buildTroubleshooting(),
           new Paragraph({ children: [new PageBreak()] }),  // <-- DELETE
           ...buildLegalNotices(),                           // <-- DELETE
           new Paragraph({ children: [new PageBreak()] }),
           ...buildBackPage(),
       ```
       After edit it should read:
       ```
           ...buildTroubleshooting(),
           new Paragraph({ children: [new PageBreak()] }),
           ...buildBackPage(),
       ```
    3. Do NOT remove the `function buildLegalNotices(...)` definition at line 1414 — dead code is out of scope for this gap.
  </action>
  <acceptance_criteria>
    - `grep -c "\.\.\.buildLegalNotices()" docs/generate_manual.js` returns `0`
    - `grep -c "function buildLegalNotices" docs/generate_manual.js` returns `1` (definition remains — acceptable)
    - `node -c docs/generate_manual.js` exits 0 (syntax still valid)
    - The sequence `...buildTroubleshooting(), new Paragraph({ children: [new PageBreak()] }), ...buildBackPage()` appears with no `buildLegalNotices` between them: `grep -A 3 "buildTroubleshooting" docs/generate_manual.js | grep -q "buildBackPage"`
  </acceptance_criteria>
</task>

<task id="01-03-T2" autonomous="true">
  <description>Regenerate the manual DOCX and PDF artifacts and verify they contain no legal section.</description>
  <read_first>
    - docs/generate_manual.js (lines 1-30 to confirm invocation; lines 1770-1790 to see OUTPUT_PATH and writeFile call)
    - package.json (check scripts for "manual" or "docs" entries)
  </read_first>
  <action>
    1. Regenerate the DOCX. Try in this order (stop at first success):
       a. `node docs/generate_manual.js`
       b. If that fails, `cd docs && node generate_manual.js`
       c. Check `package.json` for a `"manual"` or `"docs"` script and run via `npm run <script>` if present.
       Capture the output path printed by the script (it writes `OpenSpatialDelay_Manual_v1.0.docx` in `docs/`).
    2. Convert the regenerated DOCX to PDF using LibreOffice headless (the established pattern for this repo, since neither generator produces PDF in-script — `Packer.toBuffer` only emits DOCX):
       `soffice --headless --convert-to pdf docs/OpenSpatialDelay_Manual_v1.0.docx --outdir docs/`
       If `soffice` is not on PATH, report to user — do not silently fall back. On macOS it's typically at `/Applications/LibreOffice.app/Contents/MacOS/soffice`.
    3. Verify no legal text remains in DOCX:
       `grep -a -i "third-party notices\|project license" docs/OpenSpatialDelay_Manual_v1.0.docx` must return empty (exit code 1 from grep is expected — means no match).
    4. Verify no legal text in PDF. Use pypdf if available:
       `python3 -c "from pypdf import PdfReader; r=PdfReader('docs/OpenSpatialDelay_Manual_v1.0.pdf'); print('\n'.join(p.extract_text() for p in r.pages))" | grep -i "third-party\|project license"`
       Expected: empty output. If `pypdf` is not installed, install with `pip install pypdf` or use `pdftotext docs/OpenSpatialDelay_Manual_v1.0.pdf - | grep -i "third-party\|project license"` as fallback.
    5. Confirm both artifacts have mtime newer than the edit in T1 and file sizes are smaller than the previous versions (stripped section => smaller file). Record pre/post sizes in the summary.
  </action>
  <acceptance_criteria>
    - `docs/OpenSpatialDelay_Manual_v1.0.docx` mtime is newer than `docs/generate_manual.js` mtime after T1's edit
    - `docs/OpenSpatialDelay_Manual_v1.0.pdf` exists with size > 0 and mtime after T1
    - `grep -a -i "third-party notices" docs/OpenSpatialDelay_Manual_v1.0.docx` returns empty
    - `grep -a -i "project license" docs/OpenSpatialDelay_Manual_v1.0.docx` returns empty
    - PDF text extraction contains no "Third-Party Notices" or "Project License" heading
    - Both regenerated artifacts are smaller than their pre-edit versions (demonstrates section was stripped)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Source is stripped: no `...buildLegalNotices()` call in the assembly array
- Regenerated DOCX and PDF contain no third-party or project license text
- Both artifacts regenerated successfully (mtime + size checks)
- Source syntax still parses (`node -c`)
</verification>

<success_criteria>
Gap G-01 is closed:
- User manual source no longer assembles the legal section
- Binary artifacts on disk (PDF + DOCX) reflect the stripped source
- `buildLegalNotices()` function definition is left intact (dead code, out of scope)
</success_criteria>

<output>
After completion, create `.planning/phases/01-repo-license-readiness/01-03-SUMMARY.md` documenting:
- Exact lines removed (with line numbers before edit)
- Commands used for regeneration (node invocation + soffice conversion)
- Pre/post file sizes for PDF and DOCX
- Confirmation that the legal section is absent from both binary artifacts
</output>
