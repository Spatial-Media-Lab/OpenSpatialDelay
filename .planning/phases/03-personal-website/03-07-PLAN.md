---
phase: 03-personal-website
plan: 07
type: execute
wave: 3
depends_on:
  - 00
  - 04
files_modified:
  - ../andrewrahman-com/app/layout.tsx
  - ../andrewrahman-com/tests/og-metadata.spec.ts
autonomous: false
requirements:
  - WEB-03
user_setup: []

must_haves:
  truths:
    - "app/layout.tsx metadata.openGraph.images[0].width and height match the actual dimensions of the new screenshot_full.png captured in Plan 04"
    - "og:image URL resolves to a real non-empty file on disk (/assets/screenshot_full.png)"
    - "All 4 og-metadata.spec.ts tests pass"
    - "Manual preview verification recorded: pasted final Netlify URL into opengraph.xyz and metatags.io; title/description/image render correctly"
  artifacts:
    - path: "../andrewrahman-com/app/layout.tsx"
      provides: "openGraph metadata with width/height matching new OG image (Plan 04 capture)"
      contains: "openGraph"
  key_links:
    - from: "app/layout.tsx openGraph.images[0]"
      to: "public/assets/screenshot_full.png"
      via: "static asset reference"
      pattern: "/assets/screenshot_full\\.png"
---

<objective>
Close out WEB-03. The hard work of creating the OG metadata was done in Phase 2; this plan verifies it survives the Plan 04 screenshot refresh, reconciles `layout.tsx` dimensions with the new capture, runs the full og-metadata.spec.ts assertions, and runs a human-verification checkpoint for actual social-card rendering.

Purpose: WEB-03 ("Open Graph meta tags configured for social link previews") is gated on two things — the metadata being correct AND the image being at usable size. Plan 04 captures a fresh `screenshot_full.png`; if its dimensions differ from the pre-Plan-04 baseline (1640×1160), layout.tsx needs updating.

Output: layout.tsx dimensions reconciled, automated og-metadata suite green, manual social preview recorded as verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-personal-website/03-CONTEXT.md
@.planning/phases/03-personal-website/03-UI-SPEC.md
@.planning/phases/03-personal-website/03-PATTERNS.md
@.planning/phases/03-personal-website/03-04-SUMMARY.md
@../andrewrahman-com/app/layout.tsx
@../andrewrahman-com/tests/og-metadata.spec.ts
@../andrewrahman-com/public/assets/screenshot_full.png

<interfaces>
<!-- Current layout.tsx metadata.openGraph (lines 27-52) -->
```tsx
openGraph: {
  title: '...',
  description: '...',
  images: [{
    url: '/assets/screenshot_full.png',
    width: 1640,
    height: 1160,
    alt: '...',
  }],
  type: 'website',
},
```

<!-- Requirement: width * height >= 1200*630; current 1640*1160 is well above minimum -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Reconcile layout.tsx OG dimensions with Plan 04 capture + re-run og-metadata.spec.ts</name>
  <read_first>
    - ../andrewrahman-com/app/layout.tsx lines 27-52 (current openGraph block)
    - ../andrewrahman-com/public/assets/screenshot_full.png (the Plan-04 capture)
    - ../andrewrahman-com/tests/og-metadata.spec.ts (scaffolded in Plan 00)
    - .planning/phases/03-personal-website/03-04-SUMMARY.md (records any dimension changes from recapture)
  </read_first>
  <files>
    ../andrewrahman-com/app/layout.tsx
    ../andrewrahman-com/tests/og-metadata.spec.ts
  </files>
  <action>
    Step A — Measure the actual dimensions of the new `screenshot_full.png`:

    ```
    sips -g pixelWidth -g pixelHeight /Users/andrewrahman/conductor/repos/andrewrahman-com/public/assets/screenshot_full.png
    ```

    Record output as `W` × `H`.

    Step B — Compare against `app/layout.tsx` `metadata.openGraph.images[0].width` / `.height`:

    ```
    grep -nE 'width: [0-9]+|height: [0-9]+' /Users/andrewrahman/conductor/repos/andrewrahman-com/app/layout.tsx
    ```

    If the captured W/H differ from the hard-coded values, edit layout.tsx to set the exact new values:

    ```tsx
    images: [{
      url: '/assets/screenshot_full.png',
      width: <W>,
      height: <H>,
      alt: 'OpenSpatialDelay plugin — 12 taps in 3D space, spatial map view',
    }],
    ```

    Guard: if W < 1200 OR H < 630, HALT — the capture is too small for social rendering. Record as BLOCKER in SUMMARY and ask the user to retake the screenshot. Do NOT ship an OG image below 1200×630.

    Also update `alt` to reference the new hero framing per UI-SPEC Copywriting Contract: "OpenSpatialDelay plugin — 12 taps in 3D space, spatial map view" (executor may revise within D-06 scope).

    Step C — If dimensions matched the pre-existing 1640×1160, no source edit is needed — skip to Step D.

    Step D — Run og-metadata.spec.ts:

    ```
    cd /Users/andrewrahman/conductor/repos/andrewrahman-com && npx playwright test tests/og-metadata.spec.ts
    ```

    All 4 tests green.

    Step E — If the og:image:width regex scaffolded in Plan 00 (`/^(1[2-9]\d{2,}|[2-9]\d{3,})$/`) rejects the new value (unlikely; covers 1200+), widen it to specifically match the new exact dimension. Do not weaken to `\d+`.
  </action>
  <verify>
    <automated>cd /Users/andrewrahman/conductor/repos/andrewrahman-com && npx playwright test tests/og-metadata.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `sips -g pixelWidth /Users/andrewrahman/conductor/repos/andrewrahman-com/public/assets/screenshot_full.png | awk '{print $2}'` returns a value >= 1200
    - `sips -g pixelHeight /Users/andrewrahman/conductor/repos/andrewrahman-com/public/assets/screenshot_full.png | awk '{print $2}'` returns a value >= 630
    - `grep -q 'width: [0-9]\+' /Users/andrewrahman/conductor/repos/andrewrahman-com/app/layout.tsx` exits 0 (numeric width present)
    - `grep -q 'height: [0-9]\+' /Users/andrewrahman/conductor/repos/andrewrahman-com/app/layout.tsx` exits 0
    - `grep -q "'/assets/screenshot_full.png'" /Users/andrewrahman/conductor/repos/andrewrahman-com/app/layout.tsx` exits 0
    - `cd /Users/andrewrahman/conductor/repos/andrewrahman-com && npx playwright test tests/og-metadata.spec.ts` exits 0
  </acceptance_criteria>
  <done>
    layout.tsx OG dimensions match the real screenshot_full.png file; og-metadata suite green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: CHECKPOINT — Verify social card rendering on opengraph.xyz + metatags.io</name>
  <what-built>
    Plan 00 created tests/og-metadata.spec.ts. Plan 04 refreshed screenshot_full.png to a v1.0.0 capture. Plan 07 Task 1 reconciled layout.tsx dimensions. The automated og-metadata suite is green. This checkpoint confirms actual social platforms render the card correctly — axe/Playwright cannot scrape live social previews due to auth + rate limits + TTL caches (per VALIDATION.md §Manual-Only Verifications).
  </what-built>
  <how-to-verify>
    1. If not yet deployed: push the current working branch to the Netlify preview URL (user's existing CI path — typically a push to `main` or a branch deploy). Record the URL in this plan's SUMMARY.
    2. Visit `https://www.opengraph.xyz/` — paste the Netlify URL — confirm: (a) title reads "Andrew Rahman — OpenSpatialDelay & the Spatial Media Library" or the current layout.tsx value, (b) description mentions VST3 + AU, (c) image preview is the new v1.0.0 capture (not stale), (d) image renders at full width without cropping issues.
    3. Visit `https://metatags.io/` — same paste — verify Facebook, Twitter, LinkedIn previews render correctly. Twitter card should show large image (summary_large_image).
    4. Optional but encouraged: paste the URL into a Twitter/X DM composer and a LinkedIn post composer (do not post) — verify platform-native preview renders.

    Record in SUMMARY:
    - Netlify URL used
    - opengraph.xyz result (pass/fail + any platform-specific notes)
    - metatags.io result (pass/fail)
    - LinkedIn/Twitter composer preview result (optional)

    If any platform renders a stale image (common Facebook/LinkedIn cache issue): use Facebook's Sharing Debugger (`https://developers.facebook.com/tools/debug/`) or LinkedIn Post Inspector (`https://www.linkedin.com/post-inspector/`) to force a cache refresh.
  </how-to-verify>
  <resume-signal>Type "verified" when previews look correct on opengraph.xyz and metatags.io. Type "needs-recapture: reason" if the image fails either platform's preview.</resume-signal>
</task>

</tasks>

<verification>
Maps to 03-VALIDATION.md task IDs 03-07-01, 03-07-02.

End-of-plan check:
```
cd /Users/andrewrahman/conductor/repos/andrewrahman-com && npm test
```
All suites green. Manual verification signed off in SUMMARY.
</verification>

<success_criteria>
- layout.tsx openGraph width/height match the actual new screenshot_full.png pixel dimensions
- og-metadata.spec.ts 4 tests green
- opengraph.xyz + metatags.io show correct title, description, and non-stale image
- Manual verification recorded in SUMMARY
</success_criteria>

<output>
After completion, create `.planning/phases/03-personal-website/03-07-SUMMARY.md` recording: actual screenshot_full.png dimensions, any layout.tsx changes, Netlify URL used for verification, social-platform preview outcomes.
</output>
