---
phase: 03-personal-website
plan: 09
type: execute
wave: post-launch
depends_on: ["07", "08"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/phases/02-email-capture-funding-infrastructure/02-05-SUMMARY.md
autonomous: false
requirements: [DIST-01]
cross_phase_closeout: "02-05"
blocked_by:
  - "Sender.net dashboard setup (02-05-SENDER-SETUP.md walkthrough — user completes when Sender.net service is back online; 2026-04-17 Sender.net was down)"
  - "andrewrahman-com site deployed to Netlify with Phase-3 visual work complete"
  - "DNS on andrewrahman.com pointed at the new Netlify deploy (currently still on the old host per user 2026-04-17)"
user_setup:
  - service: sender-net
    why: "Must exist before this plan runs — this plan only flips the env var and UATs; all Sender dashboard work happens in the Plan 02-05 walkthrough"
    resume_signal:
      - "A. FORM_ID captured from data-sender-form-id attribute"
      - "B. macOS asset URL from v1.0.0 GitHub release"
      - "C. Windows asset URL from v1.0.0 GitHub release"
      - "D. Sender.net data region value"
  - service: netlify
    why: "Holds the NEXT_PUBLIC_SENDER_FORM_ID env var and runs the production build that inlines it into the static export"
    dashboard_config:
      - task: "Delete NEXT_PUBLIC_TALLY_FORM_ID env var if present"
        location: "Netlify → Site config → Environment variables"
      - task: "Add NEXT_PUBLIC_SENDER_FORM_ID = {FORM_ID from 02-05-SUMMARY.md}"
        location: "Netlify → Site config → Environment variables"
      - task: "Trigger 'Clear cache and deploy site' — a normal deploy will NOT re-inline the env var"
        location: "Netlify → Deploys"
must_haves:
  truths:
    - "Plan 02-05-SUMMARY.md contains the 4 captured values (FORM_ID, macOS URL, Windows URL, data region)"
    - "Netlify environment variable NEXT_PUBLIC_SENDER_FORM_ID is set to the captured FORM_ID"
    - "Netlify NEXT_PUBLIC_TALLY_FORM_ID env var is absent (if it ever existed from the pre-Sender era)"
    - "Production deploy has been triggered via 'Clear cache and deploy site' — a normal deploy does not re-inline env vars into the static export"
    - "https://andrewrahman.com/get-osd/ renders the live Sender embed (not the 'form ID not yet configured' placeholder)"
    - "https://andrewrahman.com/privacy/ names Sender.net as the processor, references Vilnius Lithuania, shows updated effective date"
    - "End-to-end UAT passed: incognito submission → DOI email within 60s → confirm click moves subscriber in Sender dashboard → macOS and Windows download buttons deliver v1.0.0 installers → Patreon CTA works"
    - "REQUIREMENTS.md DIST-01 marked complete with today's date"
    - "02-05-SUMMARY.md frontmatter flipped plan_status: code-complete-UAT-deferred → plan_status: complete"
    - "Phase-02 verifier can now re-run and close cleanly (DIST-01 was previously deferred-by-design)"
  artifacts:
    - path: ".planning/phases/02-email-capture-funding-infrastructure/02-05-SUMMARY.md"
      provides: "Cross-phase closeout — 02-05 plan_status flipped to complete, UAT evidence appended"
    - path: ".planning/REQUIREMENTS.md"
      provides: "DIST-01 flipped to Complete with today's date"
    - path: ".planning/STATE.md"
      provides: "Phase-03 progress updated; Phase-02 cross-phase dependency resolved"
    - path: ".planning/ROADMAP.md"
      provides: "Plan 03-09 marked complete"
  key_links:
    - from: "Netlify build"
      to: "cdn.sender.net/form-builder"
      via: "NEXT_PUBLIC_SENDER_FORM_ID env var inlined at static-export time"
      pattern: "data-sender-form-id=\"{FORM_ID}\""
    - from: "DOI email confirm click"
      to: "Sender.net osd-confirmed group"
      via: "Sender DOI automation (set up in 02-05 Task 1)"
      pattern: "sender.net/confirm/{token}"
    - from: "DOI email download buttons"
      to: "github.com/Spatial-Media-Lab/OpenSpatialDelay/releases/download/v1.0.0/{asset}"
      via: "HTTP direct link from mail client"
      pattern: "releases/download/v1.0.0"
---

<objective>
Execute the cross-phase closeout for DIST-01 (email-gated download) that was deferred
from Plan 02-05 because (a) Sender.net was down on 2026-04-17 blocking dashboard setup,
and (b) the andrewrahman-com site had not yet re-deployed to production with DNS
flipped over to Netlify.

This plan runs AFTER:
- User completes the Sender.net dashboard walkthrough at
  `.planning/phases/02-email-capture-funding-infrastructure/02-05-SENDER-SETUP.md`
  (captures FORM_ID, macOS URL, Windows URL, data region)
- andrewrahman-com site is merged to main and deployed on Netlify
- DNS for andrewrahman.com is pointed at the Netlify deploy

This plan ONLY does three things:
1. Set the Netlify env var + trigger a clear-cache deploy
2. Run the live production E2E UAT from the 02-05-PLAN.md spec
3. Flip DIST-01 to Complete + close Plan 02-05's cross-phase-deferred status

No code changes. No asset changes. Pure production-flip + verification.
</objective>

<context>
@.planning/phases/02-email-capture-funding-infrastructure/02-05-PLAN.md
@.planning/phases/02-email-capture-funding-infrastructure/02-05-SENDER-SETUP.md
@.planning/phases/02-email-capture-funding-infrastructure/02-05-SUMMARY.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:precondition" gate="blocking">
  <name>Task 1: Confirm all upstream preconditions are met</name>
  <what-built>nothing — gate check only</what-built>
  <how-to-verify>
    Before doing any work in this plan, confirm all four preconditions are true. If ANY is
    false, STOP and report which is missing — do not proceed.

    **Precondition 1: Sender dashboard work complete.**
    `cat .planning/phases/02-email-capture-funding-infrastructure/02-05-SUMMARY.md`
    Should show frontmatter `sender_setup_complete: true` and contain the four captured
    values (FORM_ID, macOS URL, Windows URL, data region). If SUMMARY.md is still at
    `plan_status: code-complete-UAT-deferred` with placeholder values, the user needs to
    finish the Sender walkthrough first.

    **Precondition 2: andrewrahman-com deployed to production.**
    `curl -sI https://andrewrahman.com/ | head -3` should return 200 (not 404, not the old
    host's response). If DNS is still on the old host, STOP.

    **Precondition 3: Site carries the Sender embed code.**
    The sibling `andrewrahman-com` repo's main branch must have the commits `fce5663`
    (RED test) + `a4ef090` (GREEN component) + any prior Sender-migration commits.
    Confirm by cloning/pulling the sibling repo and checking `git log --oneline | head -10`.
    If those commits are missing, the site-side code isn't ready — STOP.

    **Precondition 4: Phase-3 Plan 03-07 resolved.**
    `grep "^status:" .planning/phases/03-personal-website/03-07-SUMMARY.md` should NOT
    say `checkpoint-paused`. If it does, the social-preview verify is still hanging and
    the user should resolve that before running this plan (or explicitly defer it).
  </how-to-verify>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Netlify env var + clear-cache deploy</name>
  <what-built>production build with the Sender FORM_ID inlined into the static export</what-built>
  <how-to-verify>
    Hand off to user with this checklist:

    1. Netlify → andrewrahman.com site → **Site configuration → Environment variables**.
    2. If `NEXT_PUBLIC_TALLY_FORM_ID` exists (legacy from pre-Sender era), **delete** it.
    3. **Add variable** (or update if it already exists):
       - Key: `NEXT_PUBLIC_SENDER_FORM_ID`
       - Value: {FORM_ID from 02-05-SUMMARY.md}
       - Scope: "All scopes" (or minimum: Builds)
    4. Save.
    5. Navigate to **Deploys** tab.
    6. **Trigger deploy → "Clear cache and deploy site"** (NOT the regular "Trigger deploy"
       button — static export inlines env vars at build time; a normal deploy reuses the
       old cached build).
    7. Wait for the deploy to finish (~2 min typical for this site).

    **Resume signal:** reply with the deploy URL + build log line showing the build succeeded
    (e.g., "Deploy succeeded in 1m 48s"). No screenshots required per
    `feedback_evidence_artifact_ceremony.md`.
  </how-to-verify>
</task>

<task type="automated-verify" gate="blocking">
  <name>Task 3: Production curl sanity check</name>
  <what-built>nothing — verification only</what-built>
  <how-to-verify>
    Run all four checks. All four must match the expected value. If any fail, STOP and
    investigate — do NOT proceed to live UAT with a broken build.

    ```bash
    # Sender embed rendered (not placeholder)
    curl -s https://andrewrahman.com/get-osd/ | grep -c 'data-sender-form-id='
    # Expected: >=1

    # No "not configured" placeholder text
    curl -s https://andrewrahman.com/get-osd/ | grep -c 'form ID not yet configured'
    # Expected: 0

    # Privacy policy names Sender.net
    curl -s https://andrewrahman.com/privacy/ | grep -c 'Sender.net'
    # Expected: >=1

    # No Tally references in privacy
    curl -s https://andrewrahman.com/privacy/ | grep -c 'Tally'
    # Expected: 0
    ```

    Paste all four results into SUMMARY.md as Task 3 evidence. Commit atomically.
  </how-to-verify>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Live production E2E UAT (from 02-05-PLAN.md must_haves)</name>
  <what-built>nothing — UAT only, but flips DIST-01 on pass</what-built>
  <how-to-verify>
    Run the full 12-step UAT in a fresh incognito browser (no Sender or Netlify cookies).
    If any step fails, STOP — do not mark DIST-01 complete, record the failure and cycle
    back to whichever plan's scope covers it.

    1. Open https://andrewrahman.com/ in incognito. Homepage loads.
    2. Click any "Get OSD" CTA → lands on `/get-osd/` (or scrolls to anchor if B-7 was
       implemented).
    3. The Sender embed renders: one email field, submit button labelled
       `Get the download`, hint text with privacy-policy link below.
    4. Enter a real email you control. Submit.
    5. Success state shows: "Check your email to confirm your subscription" (or the copy
       Sender configured).
    6. Check Sender dashboard: subscriber is in `osd-unconfirmed` group.
    7. Wait ≤60 seconds. DOI email arrives in inbox.
    8. Open email. Confirm four blocks in order:
       - Confirm button
       - Download for macOS button
       - Download for Windows button
       - Patreon CTA paragraph linking to patreon.com/AndrewRahman
    9. Click **Confirm**. Redirects to Sender confirmation page (or the site, if
       redirect configured). In dashboard, subscriber moves `osd-unconfirmed` →
       `osd-confirmed`.
    10. Click **Download for macOS**. Browser downloads the v1.0.0 .pkg/.dmg installer
        (whatever the actual asset is).
    11. Click **Download for Windows**. Browser downloads the v1.0.0 .exe/.msi installer.
    12. Click Patreon CTA. Redirects to patreon.com/AndrewRahman → page loads (live
        since Plan 02-06).

    **Resume signal:** reply `UAT passed` plus any deviations or notes. Include the real
    email address you used (so it can be deleted from Sender afterward if desired).
  </how-to-verify>
</task>

<task type="code" gate="blocking">
  <name>Task 5: Flip DIST-01 complete + finalize 02-05-SUMMARY.md</name>
  <what-built>documentation updates — cross-phase closeout</what-built>
  <how-to-verify>
    After Task 4 passes:

    1. **Flip DIST-01 complete:**
       ```
       node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" requirements mark-complete DIST-01
       ```

    2. **Update 02-05-SUMMARY.md:**
       - Flip frontmatter `plan_status: code-complete-UAT-deferred` → `plan_status: complete`
       - Update `completed:` to today's ISO date
       - Append "Task 3–4 Complete (via Plan 03-09)" section with:
         - Deploy URL + build log line from Task 2
         - 4 curl results from Task 3
         - UAT verdict from Task 4 (pass + any notes)
         - Cross-reference to 03-09-SUMMARY.md
       - In Decisions/Deviations section, note: "Cross-phase closeout — Tasks 3–4 executed
         from Plan 03-09 on {date} after Sender.net outage (2026-04-17) and Phase-3 site
         launch cleared the deferral blockers."

    3. **Create 03-09-SUMMARY.md** with the standard template + references to all
       evidence (curl outputs, UAT verdict, deploy URL, updated privacy policy effective
       date).

    4. **Update STATE.md:**
       - Phase-02 cross-phase dependency resolved
       - Phase-03 plan count 9/9 → 10/10 (including 03-09)
       - Last activity: today
       - stopped_at: note Plan 03-09 complete

    5. **Update ROADMAP.md:**
       ```
       node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" roadmap update-plan-progress 03 09 complete
       ```

    6. **Commit atomically** in this order:
       - `docs(03-09): close cross-phase Sender.net UAT — DIST-01 complete`
       - `docs(02-05): flip plan_status to complete — cross-phase closeout via 03-09`
       - `docs(phase-03): update STATE/ROADMAP/REQUIREMENTS after 03-09`

    7. **Return completion report to orchestrator** — do not auto-advance (Phase-02 and
       Phase-03 verifiers handle closure).
  </how-to-verify>
</task>

</tasks>

<deviations-allowed>
- If Sender.net dashboard layout has drifted and certain UI labels differ from
  02-05-SENDER-SETUP.md, update the walkthrough doc in a follow-up commit — do not
  fight the UI, record the drift.
- If Netlify env-var UI has moved (new nav structure, new "Environment variables" page
  location), use the current UI and note the path change in SUMMARY.md.
- Asset filenames in the DOI email MAY differ from `.pkg`/`.exe` if the v1.0.0 release
  ships `.dmg`/`.msi` instead — the walkthrough already flags this.
- If DIST-01 fails UAT on a specific step (e.g., macOS button 404s), do NOT flip DIST-01
  complete. Record the failure in SUMMARY.md and create a gap-closure plan or hand off
  to the next-session task list.
</deviations-allowed>

<out-of-scope>
- Any site content changes (copy, layout, components, images) — those belong in Phase-3
  or a dedicated polish plan.
- Any Sender.net dashboard work beyond setting up what 02-05-SENDER-SETUP.md already
  covers (adding new forms, changing automation trigger, etc.).
- DNS changes — assumed done before this plan runs.
- Any changes to REQUIREMENTS.md beyond flipping DIST-01.
- Phase-02 re-verification — that's a separate orchestrator action after this plan
  completes.
</out-of-scope>
