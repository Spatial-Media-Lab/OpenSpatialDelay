---
phase: 03-personal-website
plan: 06
type: execute
wave: 3
depends_on:
  - 00
  - 01
  - 02
  - 03
  - 04
  - 05
files_modified:
  - ../andrewrahman-com/app/globals.css
  - ../andrewrahman-com/tests/a11y.spec.ts
autonomous: true
requirements:
  - WEB-01
user_setup: []

must_haves:
  truths:
    - "tests/a11y.spec.ts runs against /, /privacy, and /get-osd and returns zero WCAG 2.1 AA violations for all three"
    - "Any colour pairing from UI-SPEC §Accessibility audit contract that failed 4.5:1 (normal text) or 3:1 (UI / large text) has been fixed in globals.css with a '/* Phase 3 AA fix */' comment"
    - "`--accent-regal` achieves 4.5:1 contrast with `--bg-void` dark text (required because PatreonCTA paints --bg-void text on --accent-regal background)"
    - "No token value changes outside those strictly required for AA"
  artifacts:
    - path: "../andrewrahman-com/app/globals.css"
      provides: "Potentially nudged --text-dim, --accent-regal OKLCH lightness, or other token values with inline /* Phase 3 AA fix */ comments recording original hex + reason"
      contains: "Phase 3 AA fix"
  key_links:
    - from: "tests/a11y.spec.ts"
      to: "every rendered token pairing on /, /privacy, /get-osd"
      via: "axe-core color-contrast rule"
      pattern: "wcag2aa"
---

<objective>
Close out D-08 — the only design-system task in Phase 3. Run the axe-core WCAG 2.1 AA scan scaffolded in Plan 00. For every reported violation (expected: `--text-dim` on both dark backgrounds is borderline, and `--bg-void` on `--accent-regal` needs confirmation), nudge the OKLCH lightness of the offending token by ≤2 points at a time until axe returns zero violations across all three routes. Annotate every change.

Purpose: accessibility is the one launch-risk item in CONTEXT.md's "Design system scope (Area 2 — deliberately narrowed)" section (D-08). Formal style-guide authoring is deferred to a post-launch phase; the audit is the only part of the design-system work that ships in Phase 3.

Output: axe scan green on 3 routes; any token fixes documented inline with reason + prior value.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-personal-website/03-CONTEXT.md
@.planning/phases/03-personal-website/03-UI-SPEC.md
@.planning/phases/03-personal-website/03-RESEARCH.md
@.planning/phases/03-personal-website/03-PATTERNS.md
@../andrewrahman-com/app/globals.css
@../andrewrahman-com/tests/a11y.spec.ts

<interfaces>
<!-- Pairings to verify (UI-SPEC §Accessibility audit contract) -->
<!--
| Foreground            | Background           | Role                      | Min ratio |
| --text-primary #e1e5ea| --bg-void #03060b    | Body headings             | 4.5:1 (pass expected) |
| --text-secondary #9fa5ae | --bg-void #03060b | Body prose                | 4.5:1 (verify) |
| --text-secondary #9fa5ae | --bg-panel #0a0d12| Card prose                | 4.5:1 (verify) |
| --text-dim #6d7279    | --bg-void #03060b    | Mono captions             | 4.5:1 (borderline) |
| --text-dim #6d7279    | --bg-panel #0a0d12   | Card labels               | 4.5:1 (borderline) |
| --accent-stellar #80d8ff | --bg-void #03060b | Links, CTA button text    | 4.5:1 (pass expected) |
| --bg-void #03060b     | --accent-regal ~#b49bd8 | PatreonCTA dark-on-light| 4.5:1 (verify) |
| --tap-N colors        | --bg-panel #0a0d12   | Card colour dots          | 3:1 UI min |
-->

<!-- Fix annotation pattern (PATTERNS.md §2) -->
<!-- --text-dim: #787d85; /* Phase 3 AA fix — was #6d7279, failed 4.5:1 on --bg-void */ -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Run axe-core scan + triage violations + nudge failing tokens until all 3 routes pass</name>
  <read_first>
    - ../andrewrahman-com/app/globals.css (current token values)
    - ../andrewrahman-com/tests/a11y.spec.ts (scaffold from Plan 00 — 3 route iterations)
    - .planning/phases/03-personal-website/03-UI-SPEC.md §Accessibility audit contract (8 pairings + protocol)
    - .planning/phases/03-personal-website/03-RESEARCH.md §Expected violations
  </read_first>
  <files>
    ../andrewrahman-com/app/globals.css
    ../andrewrahman-com/tests/a11y.spec.ts
  </files>
  <behavior>
    - Test 1: `/` passes axe with zero WCAG 2.1 AA violations
    - Test 2: `/privacy` passes axe with zero violations
    - Test 3: `/get-osd` passes axe with zero violations
  </behavior>
  <action>
    Step A — Run the full axe suite with JSON reporter:

    ```
    cd /Users/andrewrahman/conductor/repos/andrewrahman-com && npx playwright test tests/a11y.spec.ts --reporter=list
    ```

    Capture the full violation list from the failed tests' JSON output. Save the raw output to a temp file under `.planning/phases/03-personal-website/03-06-AXE-BASELINE.txt` for reference.

    Step B — Classify each violation. Expected failure categories (per RESEARCH.md):

    a) `color-contrast` on `--text-dim` text (#6d7279) over dark backgrounds. Likely fails 4.5:1 for normal text.
    b) `color-contrast` on `--bg-void` text over `--accent-regal` PatreonCTA background.
    c) Any other rule flagged (e.g., missing form labels, aria-label issues) — these require targeted fixes outside colour.

    Step C — For each colour-contrast failure, nudge the OKLCH lightness by +2 points toward white (for dark-background text) or -2 points toward black (for light-background text). Validate contrast using a runtime calculation in a small Node snippet or WebAIM's contrast formula. Example fixes to apply ONLY if axe reports them:

    If `--text-dim` on `--bg-void` fails: change `--text-dim: #6d7279;` to `--text-dim: #7a7f86;` (or whatever brings it to ≥4.5:1 against #03060b). Annotate inline:

    ```css
    --text-dim: #7a7f86; /* Phase 3 AA fix — was #6d7279, failed 4.5:1 on --bg-void */
    ```

    If `--bg-void` on `--accent-regal` fails: nudge `--accent-regal` lightness down 2 OKLCH points (keeps lavender hue, increases dark-text contrast). E.g.:

    ```css
    --accent-regal: oklch(70% 0.15 290); /* Phase 3 AA fix — was oklch(72% 0.15 290), failed 4.5:1 for bg-void text */
    ```

    If a pairing fails by >5 OKLCH lightness points, split the fix across two values (lighten fg AND darken bg) per UI-SPEC's "may nudge by ≤2 lightness points" discretion rule.

    Do NOT:
    - Change the 12-tap rainbow values (`--tap-1` … `--tap-12`). If any `--tap-N` fails 3:1 UI contrast against `--bg-panel`, instead adjust the dot size or add a subtle outline in page.tsx (out of scope — flag in SUMMARY for a post-launch task).
    - Touch `--accent-rose`, `--accent-stellar`, or `--accent-violet` unless axe flags them.
    - Introduce a new token — the audit adjusts existing tokens only.

    Step D — Re-run axe:

    ```
    cd /Users/andrewrahman/conductor/repos/andrewrahman-com && npx playwright test tests/a11y.spec.ts
    ```

    Iterate: if a pairing still fails, nudge by another 1-2 points and re-run. Maximum 3 iterations per token before declaring a BLOCKER (record in SUMMARY, escalate to user).

    Step E — Run the FULL Playwright suite to confirm no regressions in other tests (especially homepage-content.spec.ts "Patreon CTA uses the new regal-lavender token" rendered-style assertion — if you nudge `--accent-regal`, the RGB range regex in Plan 03's test may need its tolerance widened).

    ```
    cd /Users/andrewrahman/conductor/repos/andrewrahman-com && npm test
    ```

    If the Plan 03 colour regex fails because the nudged regal is outside the old range, widen the regex tolerance in `tests/homepage-content.spec.ts` to accept both the pre-nudge and post-nudge RGB. Do NOT weaken the assertion to `.toMatch(/rgb\(.+\)/)`.
  </action>
  <verify>
    <automated>cd /Users/andrewrahman/conductor/repos/andrewrahman-com && npx playwright test tests/a11y.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd /Users/andrewrahman/conductor/repos/andrewrahman-com && npx playwright test tests/a11y.spec.ts` exits 0 (all 3 route tests green)
    - If any token was changed: `grep -c 'Phase 3 AA fix' /Users/andrewrahman/conductor/repos/andrewrahman-com/app/globals.css` returns at least 1
    - Every `/* Phase 3 AA fix */` comment records both the prior hex/value AND the reason (e.g., "was #6d7279, failed 4.5:1 on --bg-void")
    - `cd /Users/andrewrahman/conductor/repos/andrewrahman-com && npm test` exits 0 (full suite green)
    - No new CSS tokens introduced (`grep -c '^\s*--' /Users/andrewrahman/conductor/repos/andrewrahman-com/app/globals.css` has no change vs pre-Plan-06 baseline except unchanged count)
  </acceptance_criteria>
  <done>
    All three axe route tests green. Every fix annotated. Full test suite green.
  </done>
</task>

</tasks>

<verification>
Maps to 03-VALIDATION.md task IDs 03-06-01 (+ optional 03-06-02 contrast script).

End-of-plan check:
```
cd /Users/andrewrahman/conductor/repos/andrewrahman-com && npm test
```
Expected: all 5 spec files green (homepage-content, privacy-content, smoke, og-metadata, asset-existence, a11y).
</verification>

<success_criteria>
- axe-core reports 0 WCAG 2.1 AA violations on /, /privacy, /get-osd
- Any token changes are minimal, annotated with /* Phase 3 AA fix */, and preserve the prior hex value in the comment
- No regression in other Playwright tests (Plan 03 colour assertion still green, with tolerance widened if needed)

**Pass-through acceptance (first-run green):** If axe returns ZERO violations on the very first run without any token changes, the `/* Phase 3 AA fix */` annotation MAY BE ABSENT — this is a pass, not a failure. Record "axe green on first run — no token nudges required" in 03-06-SUMMARY.md and skip to plan completion.

**Escalation gate:** If axe still reports failures after THREE iteration rounds (i.e., three successive nudges of the same token without reaching green), stop and ESCALATE as a BLOCKER in 03-06-SUMMARY.md. Do not continue nudging past the third round; record the final violation list, the tokens tried, and request user guidance.
</success_criteria>

<output>
After completion, create `.planning/phases/03-personal-website/03-06-SUMMARY.md` listing:
- Axe baseline violations (count + rules)
- Tokens changed (name → old value → new value → reason)
- Any tap-N UI-minimum violations flagged for post-launch follow-up
- Final test pass count
</output>
