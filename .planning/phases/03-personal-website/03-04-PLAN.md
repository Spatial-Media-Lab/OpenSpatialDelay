---
phase: 03-personal-website
plan: 04
type: execute
wave: 2
depends_on:
  - 00
  - 01
files_modified:
  - ../andrewrahman-com/public/assets/sml-logo.svg
  - ../andrewrahman-com/public/assets/andrew.jpg
  - ../andrewrahman-com/public/assets/screenshot_full.png
  - ../andrewrahman-com/public/assets/screenshot_spatial_map.png
  - ../andrewrahman-com/public/assets/screenshot_wobble.png
  - ../andrewrahman-com/public/assets/screenshot_orbit.png
  - ../andrewrahman-com/public/assets/screenshot_elevation_map.png
  - ../andrewrahman-com/public/assets/screenshot_header.png
  - ../andrewrahman-com/public/assets/screenshot_bottom_panel.png
  - ../andrewrahman-com/public/assets/screenshot_right_panel.png
  - ../andrewrahman-com/public/assets/screenshot_shimmer.png
  - ../andrewrahman-com/public/assets/screenshot.png
  - ../andrewrahman-com/public/assets/signal-flow.png
  - ../andrewrahman-com/app/page.tsx
  - ../andrewrahman-com/tests/homepage-content.spec.ts
autonomous: false
requirements:
  - WEB-01
user_setup:
  - service: reaper-daw
    why: "Capture v1.0.0 plugin UI screenshots in live DAW session"
    env_vars: []
    dashboard_config:
      - task: "Open REAPER, load OSD v1.0.0 AU, capture 11 screenshots with Cmd-Shift-4 per UI-SPEC asset list"
        location: "Local macOS desktop"

must_haves:
  truths:
    - "sml-logo.svg exists at public/assets/ and is the ~8.6KB white SVG from ~/Downloads/"
    - "sml-logo.png no longer exists at public/assets/"
    - "andrew.jpg exists at public/assets/ as a valid JPEG (magic bytes confirm)"
    - "All 11 screenshot PNGs + signal-flow.png have been recaptured from v1.0.0 (mtime newer than 2026-04-16)"
    - "AboutAndrew renders <Image src='/assets/andrew.jpg' width={560} height={560} .../> with the locked className"
    - "Pipeline section's SML logo Image src ends in .svg (not .png)"
    - "FEATURES[5] (Free & open-source card) image is /assets/sml-logo.svg"
    - "tests/asset-existence.spec.ts all 14 tests green"
  artifacts:
    - path: "../andrewrahman-com/public/assets/sml-logo.svg"
      provides: "White SVG Spatial Media Lab logomark (D-11)"
      min_size_bytes: 1000
    - path: "../andrewrahman-com/public/assets/andrew.jpg"
      provides: "Andrew Rahman headshot (D-15)"
      min_size_bytes: 10000
    - path: "../andrewrahman-com/app/page.tsx"
      provides: "AboutAndrew uses headshot Image; Pipeline SML logo src is .svg"
      contains: "/assets/andrew.jpg"
  key_links:
    - from: "page.tsx Pipeline()"
      to: "/assets/sml-logo.svg"
      via: "<Image src={...} />"
      pattern: "sml-logo\\.svg"
    - from: "page.tsx AboutAndrew()"
      to: "/assets/andrew.jpg"
      via: "<Image alt='Andrew Rahman' />"
      pattern: "andrew\\.jpg"
---

<objective>
Land all Phase 3 asset work. Copy the SML SVG logo from Downloads into the site repo, download Andrew's headshot from the SML site, capture 11 v1.0.0 plugin screenshots from a live REAPER session, swap source references in `app/page.tsx` (Pipeline logo `.png` -> `.svg`, AboutAndrew monogram `<div>` -> `<Image>`), and delete the legacy `sml-logo.png`. This plan contains the one unavoidable human checkpoint in Phase 3 (screenshot capture in REAPER).

Purpose: the page has been showing placeholder/stale imagery since Phase 2. WEB-01 success criteria (screenshots + hero imagery) cannot be met without fresh v1.0.0 captures. The OG image (`screenshot_full.png`) referenced by `app/layout.tsx` is also refreshed here.

Output: 13 new/updated assets in `public/assets/`, 2 `<Image>` swaps in `page.tsx`, legacy PNG deleted, asset-existence + new homepage-content assertions green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-personal-website/03-CONTEXT.md
@.planning/phases/03-personal-website/03-UI-SPEC.md
@.planning/phases/03-personal-website/03-PATTERNS.md
@.planning/phases/03-personal-website/03-RESEARCH.md
@CLAUDE.md
@agent_docs/version_registry.md
@../andrewrahman-com/app/page.tsx
@../andrewrahman-com/app/layout.tsx

<interfaces>
<!-- Current Pipeline logo pattern (page.tsx lines 438-445) -->
```tsx
<Image src="/assets/sml-logo.png" alt="Spatial Media Lab logomark" width={180} height={180} className="w-[140px] sm:w-[180px] h-auto" />
```

<!-- Locked AboutAndrew headshot replacement (D-15 + UI-SPEC) -->
```tsx
<Image src="/assets/andrew.jpg" alt="Andrew Rahman" width={560} height={560} className="aspect-square rounded-lg object-cover border" style={{ borderColor: 'var(--border-subtle)' }} />
```

<!-- OG constraint: screenshot_full.png must stay >=1200x630; current baseline 1640x1160 (layout.tsx lines 27-52). -->
<!-- v1.0.0 plugin commit hash: 768c248 per agent_docs/version_registry.md. -->
<!-- Build via: bash scripts/build_version.sh 768c248 v1.0.0 O100 — NEVER plain cmake per CLAUDE.md. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: CHECKPOINT — Capture 11 v1.0.0 plugin screenshots in REAPER</name>
  <read_first>
    - CLAUDE.md (entire file — conventions, gotchas, and build rules)
    - .planning/phases/03-personal-website/03-UI-SPEC.md §Asset Contract (canonical 11-screenshot list with naming)
    - .planning/phases/03-personal-website/03-RESEARCH.md §Screenshot workflow
    - .planning/phases/03-personal-website/03-00-SUMMARY.md (AU plugin install status recorded in Wave 0)
  </read_first>
  <what-built>
    Wave 0 Task 1 recorded whether OpenSpatialDelay v1.0.0 AU is installed. If not, the executor must first run `bash scripts/build_version.sh 768c248 v1.0.0 O100` from the plugin repo root — NEVER plain `cmake --build` per CLAUDE.md. Once the binary is in `~/Library/Audio/Plug-Ins/Components/`, this checkpoint presents capture instructions to the user.
  </what-built>
  <how-to-verify>
    Pre-checkpoint verification Claude performs automatically:

    1. Run `ls ~/Library/Audio/Plug-Ins/Components/ | grep -E 'OpenSpatialDelay v1\.0(\.component|$)'`. If empty, HALT with message "v1.0.0 AU missing. Run `bash scripts/build_version.sh 768c248 v1.0.0 O100` first." Do not ask the user to build manually.

    **Screenshot → feature mapping (sourced from UI-SPEC Asset Contract — canonical):**

    | # | Filename | Feature to frame in REAPER |
    |---|----------|----------------------------|
    | 1 | `screenshot_full.png` | Full plugin window — default state, `v1.0.0` title bar visible. Used as OG image (MUST be ≥1200×630). |
    | 2 | `screenshot_spatial_map.png` | Spatial Map view with 12 taps distributed in 3D space (for FEATURES card 1 + card 4). |
    | 3 | `screenshot_wobble.png` | Wobble modulation panel engaged (for Screenshots section). |
    | 4 | `screenshot_orbit.png` | Orbital trajectory editor with a visible orbit path (for FEATURES card 2). |
    | 5 | `screenshot_elevation_map.png` | Elevation map showing binaural hemisphere (for FEATURES card 3). |
    | 6 | `screenshot_header.png` | Top header strip: version, transport, global controls (for FEATURES card 5 OSC panel framing). |
    | 7 | `screenshot_bottom_panel.png` | Bottom panel: presets, I/O, master controls (for Screenshots section). |
    | 8 | `screenshot_right_panel.png` | Right panel: per-tap parameters (for Screenshots section). |
    | 9 | `screenshot_shimmer.png` | Shimmer algorithm engaged with visible feedback (for Screenshots section). |
    | 10 | `screenshot.png` | Legacy general-purpose screenshot — hero-quality full window. |
    | 11 | `signal-flow.png` | Signal flow diagram export or architecture screenshot (for SignalFlow section). |

    **User instructions to present verbatim:**

    1. Launch REAPER.
    2. Create blank project; insert `OpenSpatialDelay v1.0.0` on a track (AU or VST3). Do NOT load `OpenSpatialDelay v1.0` — that is a legacy pre-release binary that pre-dates the 2026-04-12 squash, not the shipped release.
    3. Confirm plugin window title reads `OpenSpatialDelay v1.0.0` (NOT `v1.0`, NOT v0.x, NOT v1.0.1/v1.0.2 post-release patches).
    4. For EACH row in the table above, configure the plugin UI to show that feature, then `Cmd-Shift-4 + Space + click plugin window` to save a PNG with the exact filename in the first column.
    5. Save ALL PNGs into `/Users/andrewrahman/conductor/repos/andrewrahman-com/public/assets/`, OVERWRITING the existing files with identical filenames.
    6. Verify `screenshot_full.png` >= 1200x630 via `sips -g pixelWidth -g pixelHeight /Users/andrewrahman/conductor/repos/andrewrahman-com/public/assets/screenshot_full.png`.
    7. Phase 4 CONT-03 needs the same captures — duplicate the full-size set to the Phase 4 staging folder in the same session per D-10 coordination rule.
  </how-to-verify>
  <resume-signal>Type "captured" when all 11 PNGs are in place and screenshot_full.png is >=1200x630. Type "blocked: reason" to halt.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Copy SML SVG + download headshot + delete legacy sml-logo.png</name>
  <read_first>
    - .planning/phases/03-personal-website/03-PATTERNS.md §13 (SML SVG copy command) and §14 (headshot download command)
    - .planning/phases/03-personal-website/03-CONTEXT.md §D-11 and §D-15
    - .planning/phases/03-personal-website/03-RESEARCH.md §Headshot workflow
  </read_first>
  <files>
    ../andrewrahman-com/public/assets/sml-logo.svg
    ../andrewrahman-com/public/assets/andrew.jpg
  </files>
  <action>
    Step A — Copy the SML SVG from Downloads. Quote the source path — it contains a space and a comma:

    ```
    cp "/Users/andrewrahman/Downloads/Web-Logo, white.svg" /Users/andrewrahman/conductor/repos/andrewrahman-com/public/assets/sml-logo.svg
    ```

    Verify: `file /Users/andrewrahman/conductor/repos/andrewrahman-com/public/assets/sml-logo.svg` reports SVG, `ls -la` shows size > 1000 bytes (expected ~8.6 KB).

    Step B — Download headshot. Try the direct URL first:

    ```
    curl -sSL -o /Users/andrewrahman/conductor/repos/andrewrahman-com/public/assets/andrew.jpg "https://spatialmedialab.org/wp-content/uploads/2026/04/Andrew-Rahman-819x1024.jpg"
    ```

    If 404, scrape the actual src from the About page:

    ```
    SRC=$(curl -sSL https://spatialmedialab.org/about/ | grep -oE 'src="[^"]*[Aa]ndrew[^"]*\.(jpg|jpeg|webp)"' | head -1 | sed 's/^src="//; s/"$//')
    curl -sSL -o /Users/andrewrahman/conductor/repos/andrewrahman-com/public/assets/andrew.jpg "$SRC"
    ```

    Verify: `file .../andrew.jpg` reports JPEG; `sips -g pixelWidth -g pixelHeight .../andrew.jpg` shows >= 500px on each side.

    Step C — Delete legacy sml-logo.png, but ONLY after the SVG is confirmed on disk:

    ```
    test -f /Users/andrewrahman/conductor/repos/andrewrahman-com/public/assets/sml-logo.svg && rm /Users/andrewrahman/conductor/repos/andrewrahman-com/public/assets/sml-logo.png
    ```

    Do NOT delete the PNG before the SVG exists — asset-existence.spec.ts enforces this ordering.
  </action>
  <verify>
    <automated>test -f /Users/andrewrahman/conductor/repos/andrewrahman-com/public/assets/sml-logo.svg && test -f /Users/andrewrahman/conductor/repos/andrewrahman-com/public/assets/andrew.jpg && test ! -f /Users/andrewrahman/conductor/repos/andrewrahman-com/public/assets/sml-logo.png</automated>
  </verify>
  <acceptance_criteria>
    - `test -f /Users/andrewrahman/conductor/repos/andrewrahman-com/public/assets/sml-logo.svg` returns 0
    - `file /Users/andrewrahman/conductor/repos/andrewrahman-com/public/assets/sml-logo.svg | grep -q 'SVG'` exits 0
    - `test -f /Users/andrewrahman/conductor/repos/andrewrahman-com/public/assets/andrew.jpg` returns 0
    - `file /Users/andrewrahman/conductor/repos/andrewrahman-com/public/assets/andrew.jpg | grep -q 'JPEG'` exits 0
    - `test -f /Users/andrewrahman/conductor/repos/andrewrahman-com/public/assets/sml-logo.png` returns 1 (file must be absent)
    - `stat -f %z /Users/andrewrahman/conductor/repos/andrewrahman-com/public/assets/sml-logo.svg` returns > 1000
    - `stat -f %z /Users/andrewrahman/conductor/repos/andrewrahman-com/public/assets/andrew.jpg` returns > 10000
  </acceptance_criteria>
  <done>
    SVG logo copied in, JPEG headshot downloaded, legacy PNG deleted.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Swap source references — Pipeline logo .png->.svg, AboutAndrew div->Image, add homepage-content assertions</name>
  <read_first>
    - ../andrewrahman-com/app/page.tsx lines 438-445 (Pipeline SML logo pattern)
    - ../andrewrahman-com/app/page.tsx lines 463-487 (AboutAndrew placeholder monogram div)
    - .planning/phases/03-personal-website/03-PATTERNS.md §1 "Pipeline SML logo swap" and "AboutAndrew headshot swap pattern"
    - .planning/phases/03-personal-website/03-UI-SPEC.md §Headshot image treatment (D-15 locked)
  </read_first>
  <files>
    ../andrewrahman-com/app/page.tsx
    ../andrewrahman-com/tests/homepage-content.spec.ts
  </files>
  <behavior>
    - Test (RED then GREEN): `getByRole('img', { name: 'Andrew Rahman' })` exists with `src` matching `/assets/andrew.jpg`
    - Test (RED then GREEN): all `getByRole('img', { name: /Spatial Media Lab/i })` images have `src` ending in `.svg`
    - Source: Pipeline SML logo Image src is `/assets/sml-logo.svg`
    - Source: AboutAndrew placeholder div replaced with the locked `<Image>` JSX
  </behavior>
  <action>
    Step A — Add failing tests FIRST to `../andrewrahman-com/tests/homepage-content.spec.ts`:

    ```ts
    test('headshot is a real image, not the AR monogram placeholder (D-15)', async ({ page }) => {
      await expect(page.getByRole('img', { name: 'Andrew Rahman' })).toHaveAttribute('src', /\/assets\/andrew\.(jpg|webp)/);
    });

    test('SML logo renders as SVG on every instance (D-11)', async ({ page }) => {
      const logoImgs = page.getByRole('img', { name: /Spatial Media Lab/i });
      const srcs = await logoImgs.evaluateAll((els) => els.map((el) => (el as HTMLImageElement).src));
      expect(srcs.length).toBeGreaterThan(0);
      for (const src of srcs) expect(src).toMatch(/\.svg(\?.*)?$/);
    });
    ```

    Run the test file — both should FAIL (source still uses .png + monogram div).

    Step B — Pipeline logo swap. In `../andrewrahman-com/app/page.tsx` around lines 438-445, change EXACTLY ONE character in the `src` prop:

    ```tsx
    <Image
      src="/assets/sml-logo.svg"
      alt="Spatial Media Lab logomark"
      width={180}
      height={180}
      className="w-[140px] sm:w-[180px] h-auto"
    />
    ```

    Do NOT add `unoptimized` prop (inherited from `next.config.ts`). Do NOT alter width, height, alt, or className.

    Step C — AboutAndrew placeholder swap. Locate the `<div>` with `aria-label="Headshot placeholder"` (approximately lines 463-487). Replace the ENTIRE placeholder `<div>` (including the rainbow grid and AR-monogram children) with:

    ```tsx
    <Image
      src="/assets/andrew.jpg"
      alt="Andrew Rahman"
      width={560}
      height={560}
      className="aspect-square rounded-lg object-cover border"
      style={{ borderColor: 'var(--border-subtle)' }}
    />
    ```

    This is the D-15 locked JSX. Do NOT wrap in `<figure>`, do NOT add a shadow, do NOT add a caption. Do NOT alter the outer grid column wrapper — only the inner placeholder `<div>` is replaced.

    Step D — Verify FEATURES[5].image (the Free & open-source card from Plan 01) already points to `/assets/sml-logo.svg`:

    ```
    grep -n "sml-logo" /Users/andrewrahman/conductor/repos/andrewrahman-com/app/page.tsx
    ```

    Expected: all occurrences end in `.svg`. If any `.png` remains, swap it.

    Step E — Run full test suite:

    ```
    cd /Users/andrewrahman/conductor/repos/andrewrahman-com && npm test
    ```

    All tests must pass: homepage-content (now 14 tests), privacy-content (9 tests), smoke (4 tests), og-metadata (4 tests), asset-existence (14 tests), a11y (3 tests — these may still FAIL in Wave 2 because Plan 06 has not run; that is expected and will turn green in Wave 3).
  </action>
  <verify>
    <automated>cd /Users/andrewrahman/conductor/repos/andrewrahman-com && npx playwright test tests/homepage-content.spec.ts tests/asset-existence.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'sml-logo\.png' /Users/andrewrahman/conductor/repos/andrewrahman-com/app/page.tsx` returns 0
    - `grep -c 'sml-logo\.svg' /Users/andrewrahman/conductor/repos/andrewrahman-com/app/page.tsx` returns at least 2 (Pipeline + FEATURES[5])
    - `grep -q "/assets/andrew.jpg" /Users/andrewrahman/conductor/repos/andrewrahman-com/app/page.tsx` exits 0
    - `grep -q "Headshot placeholder" /Users/andrewrahman/conductor/repos/andrewrahman-com/app/page.tsx` exits 1 (aria-label gone)
    - `grep -q 'alt="Andrew Rahman"' /Users/andrewrahman/conductor/repos/andrewrahman-com/app/page.tsx` exits 0
    - `grep -q 'width={560}' /Users/andrewrahman/conductor/repos/andrewrahman-com/app/page.tsx` exits 0
    - `grep -q 'aspect-square rounded-lg object-cover border' /Users/andrewrahman/conductor/repos/andrewrahman-com/app/page.tsx` exits 0
    - `cd /Users/andrewrahman/conductor/repos/andrewrahman-com && npx playwright test tests/homepage-content.spec.ts tests/asset-existence.spec.ts` exits 0
  </acceptance_criteria>
  <done>
    Pipeline logo + FEATURES[5] logo + AboutAndrew headshot all reference real files on disk. 14 asset-existence tests green. 2 new homepage assertions green.
  </done>
</task>

</tasks>

<verification>
Maps to 03-VALIDATION.md task IDs 03-04-01, 03-04-02, 03-04-03.

End-of-plan check:
```
cd /Users/andrewrahman/conductor/repos/andrewrahman-com && npm test -- tests/asset-existence.spec.ts tests/homepage-content.spec.ts
```
a11y suite may still be RED — this is intentional until Plan 06 fixes AA failures.
</verification>

<success_criteria>
- 13 asset files exist in public/assets/ (11 PNGs + sml-logo.svg + andrew.jpg)
- sml-logo.png deleted
- page.tsx has zero `.png` references for sml-logo; all use `.svg`
- AboutAndrew renders the locked `<Image>` (not the monogram div)
- asset-existence.spec.ts all 14 tests green
- homepage-content.spec.ts headshot + SML logo SVG assertions green
</success_criteria>

<output>
After completion, create `.planning/phases/03-personal-website/03-04-SUMMARY.md` documenting: headshot source URL actually used, any dimension concerns, test pass counts, any PNGs that differed in dimensions post-capture.
</output>
